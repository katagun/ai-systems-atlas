# Backlog

This is the source of truth for actionable repository work. Policy and field definitions belong in `docs/`; completed implementation detail belongs in Git history.

## Now

Capped at work that can be started today without waiting on anyone else. The first two items address evidence decay, which is the catalog's central promise and its least automated surface; the last three are one deliberate vocabulary pass rather than three separate ones.

- [ ] Add link checking for project, license/terms, and immutable-evidence URLs with rate-limit-aware caching. This is also the natural home for terms-drift detection: the weekly refresh only compares GitHub-detected SPDX against reviewed licenses, so license or terms changes for records without a GitHub-detected license — inference services above all — surface no signal today.
- [ ] Add a stale-review report that distinguishes editorial age from GitHub metadata age without changing either.
- [ ] Decide what the Atlas does with agent skill packs, as a deliberate question rather than a triage side effect. The 2026-08-31 sweep queued thirty survivors out of 279 candidates and recorded the classes it dropped only in aggregate — `docs/COVERAGE.md` names "several skill libraries" among them, and no per-item triage log exists, so the size of this class is unknown and would have to be re-derived; the class now includes some of the highest-adoption repositories in the ecosystem, and reviewed runtimes such as DeerFlow and LibreChat carry skills as first-class runtime constructs rather than as prose. One of the three cases is already closed: the authoring convention is published in the Specifications collection as Agent Skills and Agent Plugins, so a decision here must not re-decide it. A September 2026 skeptic refuted the obvious rule, that a pack earns a record only when its advertised outcome survives removal of the host harness, on the ground that it would condemn three published workflow records and empty the coding-agent-workflow role, whose definition is exactly a process layer over a host. It also found that this item and the Superpowers exclusion both cited a rule about collections that the curation document does not contain; those citations are corrected. The working discriminator, taken from the one reviewed pack that was published, is whether the candidate spawns and supervises the host itself rather than being run by it. Separate the remaining two cases: a skills runtime that resolves, installs, versions, or sandboxes packs, which could own an operational outcome; and a collection of skill documents, which the Superpowers exclusion already treats as a research input on the grounds that nothing runs when the host harness is removed. Adoption is not evidence either way, and Superpowers was the most-starred candidate in the sweep. hyperresearch is a fourth instance and the strongest built: it clears the research-artifact test outright, with hundreds of tests run across three language versions on every change and strict type checking, and it ships a substantial command-line application and tool server over its own vault, yet its research pipeline is eighteen documents the host model executes and its only model call in the whole tree is an embedding request for the local index. Two more vault packs are now held under this item alongside claude-obsidian, and one of them, obsidian-second-brain, is very likely its ancestor: it was created two weeks earlier, its own documents describe harvesting changes back from hundreds of forks and call itself the upstream, and the two carry near-identical topic sets while neither is flagged as a fork. ADR 020 states that add-ons are reserved here, so the skill-pack question is upstream of the derivative question for that pair and both should move together. claude-obsidian is held under this item as a fourth shape the three cases do not name: a skill pack whose knowledge loop is executed by the host model, wrapped around twenty-three thousand lines of tested, continuously integrated, dependency-free code that validates, locks, applies, and indexes what the host wrote but never synthesises or answers. Its proposed graph-engine role is wrong either way; the ledgers are readable files, not an engine other software queries.
- [ ] Review Liquid AI as individual model releases under ADR 025. The collection boundary is settled, but models.dev does not currently list the LFM releases, so the source-specific queue cannot hold them yet. Add a manual model-candidate path without weakening the pinned-source invariants, then review the official release identity, LFM Open License revenue threshold, weight distribution, and documented third-party deployment surfaces. Do not publish a lab/company umbrella record or score model quality, benchmarks, or parameter counts.
- [ ] Give the four vector-store records their own reviews. Chroma, Milvus, Qdrant, and Weaviate currently share identical strengths, weaknesses, and why-it-matters text, so four distinct products carry one boilerplate review between them; the only thing distinguishing them in the catalogue is a one-line description. This surfaced while writing their research rows, where the lesson column could be differentiated from the descriptions but the contribution and weakness columns could not. Either review each on its own evidence, or decide deliberately that the collection covers vector stores as one building block and say so in one record rather than repeating it four times.
- [ ] Publish dendron and vibe-kanban as archived, now that the archived status is documented as editorial. Both carry published maintainer declarations of stopped work with no successor named, which is the Continue and Flowise shape. Their reviews are already gathered in the batch reports; what remains is building the records.
- [ ] Decide how a browser extension is recorded as a deployment mode before publishing nanobrowser. It is a genuine browser agent running its own planner-and-navigator loop inside the user's own Chrome with the user's own keys, but no `deployment_modes` value describes an extension, and `desktop` misdescribes it. ADR 017 makes extending the vocabulary the remedy rather than distorting the classification. It has also been dormant since November 2025, which belongs in maturity, not in the decision.
- [ ] Decide whether the licence vocabulary should carry the Elastic License 2.0 as its own identifier. Open SWE pins an agent server published under it and gated on a licence key, and the record currently files that under the general commercial reference, which loses the fact that the terms are a specific published licence rather than bespoke vendor terms. A source-available coding agent already required the same compromise for a different named licence, so this is now two records deep.
- [ ] Decide how a record names a vendor's own models. `model_backends` has no identifier for a coding-agent vendor that also trains the model it sells, and three records published on 2026-09-03 now carry the gap: Cursor's Composer and Cursor Grok lines, Devin Desktop's SWE model family and its SWE-grep retrieval model, and Antigravity's self-served open-weight option. The taxonomy already carries `poolside` as a vendor-model identifier, so the precedent exists and the question is whether to keep minting them per vendor or to add one value meaning "the record's own operator". Cursor also documents Azure OpenAI and Bedrock as bring-your-own-key routes that `openai_compatible` describes wrongly. Until this is settled the affected records under-report rather than over-report, which is the correct direction but loses a real decision signal: on Cursor's Teams and Enterprise plans third-party models carry a per-token surcharge the first-party ones are exempt from.
- [ ] Decide whether hosting other vendors' agents is a recordable capability. `agent_capabilities` has `mcp` but nothing for the Agent Client Protocol, and the Devin Desktop record published on 2026-09-03 has that as its most distinctive property: five named third-party agents run as first-class sessions inside it, and the vendor states its own privacy policy and legal terms do not apply to them. Warp has the same unrecorded property today. ACP is already published in the Specifications collection, so the question is only whether a system record can say it implements one, which is a broader question than this one protocol and should not be settled by adding a single enum value in passing.
- [ ] Give a record somewhere to put a rename note. Both [ADR 016](docs/adr/016-superseded-predecessors-keep-their-record.md) and [ADR 020](docs/adr/020-derivative-records-turn-on-operational-boundary.md) require a rename to be carried as a note on one record, and the schema has no general note field, so `current_repo_note` — documented in `docs/DATA_MODEL.md` as repository-scoped — is being used for it. Devin Desktop is the clearest case: its note carries a rename, an acquisition, a redirect, a list of surfaces that kept the old brand, and the boundary against the sibling Devin record, none of which is about a repository, and the record has no repository at all. Cursor and Antigravity use the same field for the opposite purpose, to explain why their public repositories are not the product.
- [ ] Extend the runtime model-format vocabulary in one deliberate pass if more classical machine-learning serving systems are reviewed. ADR 017 makes extending the vocabulary an obligation when a modality is admitted; TensorFlow Serving needed `saved_model` added on its own, and Triton, KServe, and Seldon would each hit the same edge.
- [ ] Decide whether the Atlas gains a role for agent-to-physical-world systems, once three open gaps are closed. A 2026-09-04 robotics sweep holds thirteen candidates on `triage.held_by: "robotics scope decision"` pending this: `OpenMind/OM1` and `huggingface/lerobot` (software stacks with a documented LLM/VLM component driving a physical actuator), `dora-rs/dora` (a software stack whose documentation mixes LLM and YOLO model nodes with a ROS2 bridge to robot software, though not itself naming a reach to a physical actuator), `Physical-Intelligence/openpi` and `google-deepmind/gemini-robotics-sdk` (tested the same way as software rather than model artifacts), `NVlabs/alpamayo`, `NVIDIA/Isaac-GR00T`, and `openvla/openvla` (action-emitting model releases tested against the Models collection instead), `ros/urdfdom` (a specification whose boundary parties don't support admission, and with no `specification_scopes` value to land in either), and the four commercial hardware products Spot, Unitree G1, Figure, and 1X NEO (held on inclusion-gate condition 4, not on family fit). Applied to this boundary, [ADR 023](docs/adr/023-autonomous-science-systems-are-not-a-role.md)'s three-part reopening test is not satisfied yet, and the gaps are specific rather than a wholesale miss: a shared operational outcome no existing role names is real, but as found it is phrased as a mechanism ("a model's output driving a physical actuator") rather than a domain, which needs settling before it can carry weight; no candidate has had the licensing and source-model review the gate's fourth condition requires, and `CANDIDATE_REQUIRED` in `scripts/validate_directory.py` has no `source_model` or `licenses` field, so that review cannot happen inside `directory/candidates.json` — it needs full review, which is what promotion is for; and the distinguishing property has only been read from public repositories, never from an opaque system, so the four held hardware products should be run through the model-in-the-loop test before this reopens, not left untested on condition 4 alone. `docs/COVERAGE.md` batch 39 has the full evidence.

## Next

- [ ] Review the remaining 73 provisional records in small, evidence-backed batches, thirteen of which are held pending the robotics scope decision and several others pending the open questions below rather than awaiting research.
- [ ] Review xerj as retrieval infrastructure. A triage sweep found a genuine single-binary search engine with typed indices, several retrieval modes, a compatible query interface, and a conformance harness with hundreds of cases, which is not the general-database shape excluded twice. Two things need settling in the review: the repository is nine weeks old and still before its first stable release, and its headline efficiency claims are self-measured against material inside its own tree, so they are marketing rather than evidence. Its memory endpoint and tool server also mean the memory-service role deserves a second look before the role is fixed.
- [ ] Decide the reviewable unit and licence vocabulary for MindsHub before publishing anything under that name. The repository is the former MindsDB database repository hollowed out to a superproject with no code of its own; the product lives in four submodules under three licences, one of which, Elastic License 2.0, has no identifier in the taxonomy. The star count belongs to a product that no longer exists in the tree. If published, it should be recorded against the submodule repositories with the inheritance explained, cross-referenced to Hermes Agent, which one harness embeds, and its role decided between a stateful runtime and delegated general work.
- [ ] Re-check the LangGraph record's source model and deployment traits for consistency with Open SWE. That record is published as fully open on the strength of its permissively licensed library, which is defensible, but the agent server behind its self-hosted and managed deployment values is the same restricted runtime that made Open SWE mixed-source.
- [ ] Decide whether SeekrFlow's inference side warrants its own inference-service record. The published system record covers the hosted agent runtime only, but the same platform sells dedicated and serverless model endpoints with a per-token price list, an OpenAI-compatible client path, and a catalogue of open-weight models — the shape of a `cloud_model_platform` beside IBM watsonx.ai Runtime and Databricks Foundation Model APIs, whose vendors already hold both a service record and a system record. What blocks it today is the record unit: the service rules curate a named service boundary, and Seekr names its endpoints as components of SeekrFlow rather than as a separate service, so splitting one product across two collections on price-page headings alone would invent a boundary the vendor does not publish.
- [ ] Consider whether the publishing party belongs among the derivative rule's named fields. A skeptic proposing this noted that the archived predecessor's own record already carries the fact that its live fork ships under different governance, so the reviewed commitments no longer bind whoever distributes the code, and that ADR 018 already treats operating party as a trait. It would be narrow and testable where a lineage traversal was neither. Brief a skeptic before drafting.
- [ ] Review the remaining proprietary memory batch: NotebookLM, Microsoft Recall, and Limitless, preserving product boundaries from open references.
- [ ] Review the rest of the coding-agent second pass: GitHub Copilot coding agent, Jules, and T3 Code. The editor half is done — Cursor, Google Antigravity, and Windsurf are reviewed, the last published as Devin Desktop because windsurf.com now redirects there and the vendor states only the name changed. Replit Agent provides the managed-workspace baseline, and Roo Code is published as archived alongside Crush, Qwen Code, and Continue. Zoo Code, the community continuation of Roo Code, is queued. What remains are cloud-delegation and workflow boundaries, not editor ones.
- [ ] Review Sakana AI's end-user products as their own pass. Sakana Fugu is published as an inference service, but the same operator also ships Sakana Chat, Sakana Translate, and a Marlin analyst product, each of which needs its own product-terms and governance review; do not infer an assistant's boundary from the API record, which is the mistake `docs/COVERAGE.md` item 12 already warns about for Qwen and Kimi.
- [ ] Review the remaining source-model diversity candidates: Obsidian, Microsoft Recall, and Limitless. screenpipe is published as source-available after relicensing its whole tree in June 2026; AFFiNE and Onyx are published, and both confirmed the concern behind this item: each reports no recognised license to automated detection while carrying a real proprietary tier, and Supermemory reports a permissive license while its memory engine is absent from the repository entirely.
- [ ] Replace the five per-collection search boxes with one search across records, specifications, and taxonomy terms that still lands in the right collection scope and never mixes score profiles under ADR 013.
- [ ] Render `current_repo_note`, or decide it is not reader-facing. A verification pass on 2026-09-03 found that 105 of the 174 published records carry the field and that nothing renders it: it appears in neither `web/app.js` nor `web/app-core.js`, and `scripts/build_share_pages.py` never emits it. That is editorial prose written for readers that no reader can see, and it is load-bearing — the field carries the product-boundary notes that keep a vendor's assistant, coding agent, and platform records distinct, which `docs/CURATION.md` requires reviewers to write. The newly published Kosmos record is the immediate instance: its `description` carries the boundary against Edison Scientific's sibling agents and does render, but two facts recorded only in the note do not reach the reader — that the service is closed and hosted while only the Apache-2.0 `edison-client` is open, and that Kosmos reaches three of those sibling agents as sub-agents inside its own run. This is upstream of the rename-note item below, which asks where a note should live while assuming notes are shown at all; settle rendering first, then the field's scope.
- [ ] Show related records and previous/next navigation inside detail dialogs from data the records already carry: same role, same family, and declared successors.
- [ ] Surface local-runtime GitHub star counts in the Directory UI (card badge, sort option). `directory/local-runtimes.json` now carries `stars`/`stars_verified_at`, refreshed by `scripts/update_directory.py`, but the web app doesn't render them yet; data-only was a deliberate scope cut.
- [ ] Add a small review command that promotes a candidate only after all required editorial, source-model, license, and evidence fields are present.
- [ ] Document and test repository rename/transfer handling while preserving evidence history.
- [ ] Review cross-protocol authentication and authorization profiles, workflow-state exchange beyond task messaging, and conformance suites as the next specification batch.
- [ ] Review Amazon Q rules, Kiro steering, and JetBrains AI Assistant rules as the next bounded instruction-convention batch.
- [ ] Add automated accessibility checks to the existing browser end-to-end suite. The precondition this item was written against is already met: Playwright and Chromium are devDependencies and `npm run test:e2e` runs in CI as of #76, so an axe integration lives in the test path as a development dependency and leaves the shipped application dependency-free. This item previously deferred the work "when a browser test runtime can be introduced"; that blocker no longer exists.
- [ ] Guard the `docs/COVERAGE.md` snapshot against silent drift. #82 recomputed every count from the canonical files on 2026-09-03, closing an eleven-record gap that had opened unnoticed against the previous snapshot; nothing prevents the same gap reopening. A validation check comparing the snapshot's stated counts against the published files would make the drift fail loudly. The document itself now records the narrower residual problem, that its coverage-signal prose is older than its counts, which a count check cannot detect.
- [ ] Disable administrator bypass for the `github-pages` environment in GitHub's UI; the setting has no supported API mutation.
- [ ] Disposition Elicit, which was screened in this batch as a boundary probe against the `research_agent` line and never recorded anywhere machine-readable. It is a separable commercial product from Elicit Research, PBC, with its own terms, pricing from a free tier through $169 per month to enterprise agreements, an API and an MCP server, and a corpus of over 138 million papers, and it advertises data analysis and figure work beyond retrieval — which is why it probes the line rather than sitting comfortably on one side of it. It cannot wait in `directory/candidates.json` because that schema requires a `repo` and Elicit has none, which is the same shape as the Liquid AI item above: a real operational choice with no record able to hold it. Decide it here — publish, exclude, or record a boundary — rather than leaving it in prose.
- [ ] Review `Future-House/paper-qa` as a coverage gap surfaced while screening autonomous-science systems for ADR 023. FutureHouse no longer operates a platform — `platform.futurehouse.org` returns HTTP 301 to `platform.edisonscientific.com` — and what remains is Apache-2.0 source split across repositories that do not share one maturity profile: `paper-qa`, `aviary`, and `ldp` are actively released with tests directories, while `robin`, the repository that actually carries the discovery claim, has zero releases, no tests directory, and no commit in roughly four and a half months. `paper-qa` has never been screened and is the one worth reviewing first; its role is open between `research_agent` and a memory-family knowledge role.

## Watching

Items with no action available here. Either the record is complete and the next move belongs to a repository this project does not control, or the governing evidence sits behind a host this environment cannot reach. Nothing in this section is waiting on a decision; promote an item back to `Now` when its stated condition is met.

- [ ] Revisit vibe-kanban if commits resume or the repository is archived. The maintainer company shut down in April 2026, the last commit and package publish are from the same month, and the shipped version retires its kanban flows to an export page; no published status describes a dissolved maintainer with an unarchived repository, so it stays queued with this note rather than being published under a status that misreports it. Its shape is a process supervisor for coding-agent tools, not the workflow role the sweep proposed.
- [ ] Revisit dendron if the repository is archived. Its maintainer announced maintenance-only in February 2023 after the company failed to find product-market fit, and the readme repeats it; the last commit touching code is from August 2023, there are no releases, and the editor extension has not been updated since. Unlike the other holds every first-party host still resolves and the extension is still installable, so the reader is misled only by a directory that lists it as active. A search across its whole source tree finds no model or embedding code at all, so its role would be a plain personal knowledge tool with no agent relation, immediately adjacent to a published record of the same shape.
- [ ] Revisit plandex if commits resume or the repository is archived. It is a real self-hosted terminal coding agent whose hosted tier wound down in October 2025; there has been no commit since, pull requests from August 2026 are unanswered, and every first-party host fails to resolve, so the documented install path and wind-down post are unreachable. Like vibe-kanban, no published status describes a dissolved maintainer with an unarchived repository.
- [ ] Revisit LaVague if commits resume or the repository is archived. It is a genuine browser agent with its loop in the library, but the last source change is from September 2024, the last commit of any kind from January 2025, and neither package has been released in two years; the maintaining organisation is still active elsewhere, so this is the vibe-kanban and plandex shape without a wind-down announcement. An unaddressed security report about executing model output derived from untrusted page content has sat open since June 2026, which bears on recommending the code to readers.
- [ ] Revisit Claudable if commits resume or the repository is archived. It is a web-application builder that drives other vendors' coding tools as child processes, so the sweep's knowledge-app role is wrong and the workflow role is right; the candidate record is corrected. It has no continuous-integration directory, no test runner, and one release against a tree two major versions ahead, and its last change to the default branch is from March 2026. The maintaining organisation has since renamed and its most recent work is unrelated, with no successor declared.
- [ ] Revisit OpenCursor only when it has a correctness gate and adoption of its own. The repository was created in 2022 as an unrelated reverse proxy and rewritten in place in July 2026, so its stars and its unusually high fork count belong to that defunct product, which is the same problem already recorded against MindsHub; the extension it now ships has a few hundred installs. It has no continuous-integration directory at all and its single test file is the editor scaffold's sample assertion. Its subscription sign-in presents itself as three other vendors' first-party tools against their own endpoints, which bears on recommending the code. Do not publish a record carrying the repository's star count.
- [ ] Resolve GroqChat's product boundary only when first-party evidence establishes a durable workspace distinct from Groq Playground and GroqCloud.
- [ ] Revisit Flowise if a community fork gains maintainer endorsement or clear succession; the record is archived with no successor named, so ADR 016's superseded status does not apply.
- [ ] Revisit Xinference if its commercial terms are published; the enterprise edition is currently noted as an adjacent boundary because the vendor's terms page is a placeholder.
- [ ] Revisit Microsoft Discovery if commercial terms are published. It has been generally available since 2026-06-02 as a downloadable Windows application plus a REST API, and its loop reaches HPC and physical instruments under human oversight, but no product-specific terms appear on any first-party page, which fails inclusion-gate condition 4 in [`docs/CURATION.md`](docs/CURATION.md) — the condition asks for authoritative licence or terms sources establishing a reviewed source model and a complete licences list. The published pricing page, which renders a price of `$-`, corroborates that nothing commercial is published rather than being the failure itself. This is the Xinference hold: the MIT licence on `microsoft/discovery` covers a catalogue and an installer pointer, not the product, so it cannot stand in for the missing terms.
- [ ] Record the effective dates of the xAI terms governing Grok Bot. The host refuses both automated fetches and browser access from this environment, so the record cites the governing URLs without restating their dates; the Cursor terms it designates for authentication, retention, and deletion were read directly and are dated. Grok Bot is also the second record whose governance splits across two companies with no first-party page reconciling them.
- [ ] Re-read the Meta Business Agent terms and record their date and scope. The document renders only in a browser, and browser access to that host is blocked in the current environment, so this review cites the governing URL without restating its text.

- [ ] Revisit Google Co-Scientist and AlphaProof when either reaches general availability. Both are in scope by outcome and excluded on availability alone: Co-Scientist is documented as a Generative AI Preview whose page states access is restricted and routed through a Google account team, its individual track is an "Express interest" page, and AlphaProof reaches users only through the five-institution AI for Math Initiative. Neither has released code, an SDK, or an API. The sibling case is already decided in the other direction, which is what makes these worth watching rather than closing: AlphaEvolve sat in the same preview posture and is now generally available with a documented API, so it is queued as a candidate. Promote these when a self-serve or contractable boundary exists, not when a new paper or partnership is announced.

- [ ] Watch for Meta's announced "Harness" agent framework and record nothing until it exists. A September 2026 review found the framework named only in conference remarks reported second-hand, with no repository, documentation, or first-party page, which fails the inclusion gate's first condition outright and does not even meet the candidate schema's requirement for discovery facts. Meta's published work in this area is research: HyperAgents and Dr. Zero are both excluded with reasons, and the agent-environments platform is excluded on the benchmark boundary. Note that Meta already ships Muse Code, which is the harness role such an announcement would occupy.

## Later

- [ ] Build an explanatory layer on the Atlas's own diagram engine once the collection question above is settled. The reference site's engine is data-defined SVG with a fixed node vocabulary, labelled edges, step captions, and attack-versus-defence toggles, framework-free and theme-aware; it ships no licence, so nothing from it can be copied and the engine would be written here from scratch. The first diagrams should explain what readers of this directory most often misread: the three system families, why scores never cross profiles, and the boundaries between systems, inference services, and local runtimes. Keep the runtime dependency-free and keep every number on a diagram sourced or labelled illustrative.
- [ ] Review the hosted editions of self-hosted gateways as routing aggregators, starting with Portkey and Helicone. Their vendor-run tiers are inference services on the existing definition; the self-hostable software itself stays out under ADR 015, which excludes proxies and routing libraries by name.
- [ ] Decide whether a domain-specific scientific model API belongs in the inference-service collection, or state that it does not. AlphaGenome is the standing instance and it was screened out on a scope argument rather than a failed criterion, which is the weakest kind of exclusion this file should tolerate for long. On a literal reading of `docs/INFERENCE_SERVICES.md` it clears every inclusion condition: a named service with a documented endpoint and an Apache-2.0 SDK on PyPI, an identifiable operator, a free self-serve key, and dated model and API terms whose parameters, unlike AlphaFold 3's, may be redistributed. It was excluded because no memory, agent, or assistant record in this directory could route a workload to a genomics variant-effect endpoint, and admitting it on the literal reading would open the collection to every domain model API. Either the collection's stated purpose should say that it covers general inference substrate for the systems the Atlas scores, or the boundary should be widened deliberately; today the rule that decided it is not written anywhere. Note the profile would score such a service near zero on serving flexibility, regional control, traffic resilience, and customisation lifecycle, which is the same measurement-artifact problem [ADR 017](docs/adr/017-local-runtime-eligibility-ignores-modality.md) names in a different collection.
- [ ] Reassess API clients, adapters, and observability SDKs only after concrete user questions justify another bounded collection. A 2026-08-31 pass asked whether self-hosted AI gateways deserved a collection and concluded they do not: ADR 010 and ADR 015 already exclude proxies and routing libraries by name, so this is a settled decision rather than an unfilled gap, and reversing it would need its own record.
- [ ] Settle whether an aggregator that resells other aggregators is reviewable, and on what boundary. Several catalog entries route to OpenRouter and to each other, so a record could describe a service whose whole substance is another reviewed record.
- [ ] Reconcile the `operator` field convention across inference services. Recent records name legal entities while older ones name brands, and the field should say who the customer contracts with; Z.ai was corrected because its brand and its Singapore contracting entity share no words.
- [ ] Work the models.dev catalog for inference-service coverage in evidence-backed batches, collapsing regional and subscription variants to service boundaries first. It is a coverage-audit source rather than a discovery feed; `directory/discovery-sources.json` requires an announcement feed and models.dev publishes none, serving one HTML page for every path.
- [ ] Settle what Glama is before reviewing it. Its model gateway still serves a public catalogue, but the product now presents as an index of tool servers with a chat workspace attached, and no plan price is visible without signing up, so the reviewable boundary is unclear.
- [ ] Review Novita AI and Lambda Inference with governing terms in hand; one documentation pass did not establish their retention, residency, and delivery boundaries.
- [ ] Review Qwen Chat and Kimi as assistants, each with its own product-terms and governance pass.
- [ ] Allow license evidence to reference a pinned blob in a repository other than the record's own. Two records now need it: the GenieX proprietary component, and watsonx Orchestrate, whose MIT client lives in a repository the platform record does not claim as its own. Both are recorded as dated web terms with the blob hash named in the scope instead.
- [ ] Review a proprietary or managed self-hosted gateway; both reviewed compatibility gateways are open source.
- [ ] Decide whisper.cpp on its merits under the ADR 015 purpose test and the ADR 017 modality rule; it was screened without a decision when the rule was unwritten.
- [ ] Decide whether the CrewAI record covers the open-source framework alone or the framework together with the vendor's AMP suite. Its `self_hosted` value currently rests on AMP's on-premise option rather than on anything the open-source package ships.
- [ ] Decide whether the Codex record is scoped to the terminal agent alone. Its deployment now names the first-party IDE extension and desktop app from the same repository, which is wider than the record's own summary.
- [ ] Re-check the Khoj record's status and links. The desktop download on khoj.dev returns 404 and the homepage now leads with a different product, which is a status question rather than a deployment one.
- [ ] Decide whether Perplexity Personal Computer is a separate record from Perplexity Computer. It has its own product page and installs on the user's machine with access to local files and applications, so its execution boundary differs from the cloud product this record reviews.

## Completed on 2026-09-04

- [x] Closed the triage recheck bypass. `scripts/build_candidate_evidence.py` scopes `--recheck` to blocks whose `triage.proposed_at` equals today, but `proposed_at` is a field the triaging agent writes and nothing pins it: a block dated yesterday, carrying a `content_sha256` of 64 zeros and a URL of `https://example.invalid/anything`, passes recheck, the field guard and the validator with the network never touched. That nullifies the guarantee [ADR 024](docs/adr/024-candidate-triage-proposals-are-unaccepted-evidence.md) rests on, and it also fires innocently whenever a run and its `finish` straddle midnight. Scope recheck by "block absent from `origin/main`'s queue" instead, which `unexpected_field_changes` in `scripts/run_candidate_triage.py` already computes; that also fixes the runbook's hand-check, which today verifies zero citations on any day but the run day. In the same touch, run the blast-radius check over `git diff --name-status origin/main HEAD` as well as `git status --porcelain`, so an agent that commits an out-of-scope edit cannot slip it past a clean working tree. The routine is committed but not installed, so nothing is live until this lands.

Groomed out of the active sections on this date. Individual completion dates are in Git history, which is the canonical record.

- [x] Give the candidate queue a way to record what holds a record; [ADR 024](docs/adr/024-candidate-triage-proposals-are-unaccepted-evidence.md) and the schema's optional `triage` block on a candidate settle it. Both shapes asked for are now expressible: `held_by` names the decision that holds a record, and `proposed_system_family`/`proposed_primary_role` may be null while `held_by` is set, so a record can queue awaiting a collection that does not exist yet. The Liquid AI item below remains the standing instance of the second shape; nothing here decides it.
- [x] Add a guarded models.dev candidate-promotion command. It scaffolds no editorial conclusions, checks a completed review without writes, and promotes only after the proposed reviewed collection and remaining queue pass identity, immutable-source, cross-collection, license, evidence, date, taxonomy, and score guards together.
- [x] Curate the first models.dev expansion batch after the seed. Claude Sonnet 4.6 and GPT-4.1 establish proprietary API-only model boundaries; Llama 3.2 11B Vision Instruct records its gated weights, custom community license, AUP, and multimodal regional restriction; and Pixtral 12B adds a permissively licensed multimodal checkpoint. The four releases use only the model-access profile, leave imported source metadata attributed and unchanged, and reduce the unpublished queue from 355 to 351 records.
- [x] Establish provider-independent model releases as their own collection under ADR 025. The implementation pins and imports models.dev model TOMLs into an unpublished fail-closed queue, publishes a three-record license-diverse seed, uses a dedicated access-and-deployability score that excludes quality and volatile measurements, and carries Models through validation, taxonomy, filters, comparison, deep links, share pages, API docs, attribution, and the weekly refresh.

## Completed on 2026-09-03

Groomed out of the active sections on this date. Individual completion dates are in Git history, which is the canonical record.

- [x] Tighten the header, primary navigation, and footer without redesigning them; all three recommendations shipped with tests. A 2026-09-02 review, recorded in [docs/superpowers/specs/2026-09-02-header-nav-footer-review.md](docs/superpowers/specs/2026-09-02-header-nav-footer-review.md), found the primary nav styled as a nested pill widget that competes with the collection switcher and is the only capsule shape in the header, a footer whose single flex row has no gap so the wordmark collides with the score sentence at 768px, and fourteen literal corner radii beside one token. It recommends a plain underlined nav at every width, a two-zone footer grid on the existing content column, and three radius tokens with a test guard, in three sequenced pull requests. The GitHub link already renders as an icon; the text some browsers still show is a cached build from before #64.
- [x] Decide the response to the name collision with aisystemsatlas.com. `zoltlabs/ai-systems-atlas` launched on 2026-09-01 under the same name this repository adopted on 2026-08-25, holds the `.com` domain, and publishes a visual atlas of agent patterns rather than a directory, so search results will conflate a reviewed directory with a set of diagrams. The repository took the domain-of-our-own branch: the site is branded peacefulcoexistance and now serves from its own `peacefulcoexistance.com` domain via a published GitHub Pages CNAME, with the "reviewed directory" tagline carried in the page metadata rather than the name itself.
- [x] Write the derivative rule down; [ADR 020](docs/adr/020-derivative-records-turn-on-operational-boundary.md) settles it on the fields a record already carries rather than on repository lineage. One correction this item did not anticipate: Open Grok is not a precedent for the rule, because its upstream is not a published record and no exclusion branch was ever available. The base is oh-my-pi for inclusion and llamafile for packaging.
- [x] Stop the metadata refresh from overwriting an editorial status. `scripts/update_directory.py` derived `status` in both directions from the repository's archived flag, so the next refresh would have reverted both `superseded` records to active — GitHub reports neither predecessor as archived — and would equally have reverted a hand-set `archived`. The refresh now only promotes an active record to archived, and two tests cover it. Found while reviewing Continue, whose maintainers declared the repository read-only without archiving it.
- [x] Review mini-swe-agent, then publish SWE-agent as superseded by it; both are published, and the maintainers' boundary statement turned out to live in the successor's documentation rather than the predecessor's.
- [x] Decide whether the Atlas needs a status for a system whose maintainer has stopped work without archiving the repository; it does not, and `docs/CURATION.md` now says so. A skeptic found the case already decided and published: the Continue record carries `archived` on the maintainers' own statement while the platform does not flag the repository, and Flowise carries it with no successor named. The undeclared-dormancy cases are what ADR 016 routes to weaknesses and the maturity dimension, where a published record already sits scored and active. dendron and vibe-kanban both carry published declarations and can now be published as archived on their own merits; the cases without a declaration stay held.
- [x] Decide what the unused research-reference role is for, or remove it; [ADR 021](docs/adr/021-the-research-reference-role-is-removed.md) removes it. A skeptic confirmed the policy contradiction but knocked out the argument the item was framed on: emptiness is not this repository's removal criterion, since four other vocabulary values and seven licence identifiers are equally unused and are staying. What decided it is that the curation rules route this material to exclusions unconditionally, and that a separate collection under ADR 008 is the house answer if it is ever published.
- [x] Settle what happens when a derivative's ancestor is archived and supersession is unavailable, then decide Zoo Code; there was no gap, and it is excluded as packaging. A skeptic showed the rule already decides the case and already prescribes the remedy, which is to note the lineage on the record it folds into, and that the proposed fix would have reintroduced repository ancestry as the decider, produced two records identical on every named field, and readmitted language bindings the Atlas has excluded twice. The reader is served because the other fork in that lineage is published and active.
- [x] Add a deployment filter to the Systems scope and reconcile the `deployment` values behind it; both shipped, and ADR 019 later added the matching interface filter.
- [x] Review the former managed-platform candidates individually under the ADR 018 routing. Foundry Agent Service, Bedrock AgentCore, and the Gemini Enterprise Agent Platform are published as stateful agent runtimes and watsonx Orchestrate as a multi-agent orchestrator; Copilot Studio stays with the low-code builder batch.
- [x] Settle the product boundary for Salesforce Agentforce and the Meta Business Agent Platform; both are published as agent frameworks.
- [x] Emit per-record share metadata now that every record has a URL; `scripts/build_share_pages.py` generates a static landing page per record with title, description, canonical link, preview tags, and JSON-LD, plus a sitemap and robots file, and Copy link now shares that page. Share images are the remaining piece and would need a browser-rendering step this repository does not yet run in CI. This came out of the 2026-09-02 review of `zoltlabs/ai-systems-atlas`, together with the three items below.
- [x] Add a three-state theme control (system, light, dark). Every colour in `web/styles.css` now resolves through a token, the dark palette is defined once for the OS preference and once for an explicit choice with a unit test holding the two identical, and share pages follow the OS preference.
- [x] Add operator and project logos to the directory cards on the site. 119 of 174 records carry monochrome marks vendored into `web/logos.json` from `@lobehub/icons-static-svg` (MIT) and `simple-icons` (CC0) via `scripts/build_logos.mjs`; the rest show monogram fallbacks, the runtime stays dependency-free, and the footer carries a trademark notice.
- [x] Revisit the research-reference role, which [ADR 021](docs/adr/021-the-research-reference-role-is-removed.md) removed, as a question about an explanatory collection rather than a scored role; [ADR 022](docs/adr/022-general-pattern-content-is-not-a-collection.md) declines the collection. A skeptic independently re-fetched `zoltlabs/ai-systems-atlas`'s source and the paper it cites rather than trusting the prior review's characterization, and confirmed both premises: every existing collection here pins evidence to one steward's own artifact, which a cross-author pattern cannot do, and the site's Failure Taxonomy plate's seven categories and percentages do not match the three top-level categories and fourteen named failure modes of arXiv 2503.13657 (Cemri et al., 2025), the one paper it cites — a real citation next to figures it does not support. The narrower question of diagramming the Atlas's own taxonomy boundaries, sourced to this repository's own ADRs and JSON rather than external literature, is untouched and stays open below.
- [x] Reassess local inference runtimes as their own bounded collection; ADR 015 defines the local-runtime boundary and score profile, and the seed batch is published.
- [x] Decide how the Atlas represents a framework its maintainer has declared superseded while the successor is already scored; ADR 016 adds the `superseded` status and a validated `superseded_by` link, and AutoGen and Semantic Kernel are published under it.
- [x] Stop the automated updater from reformatting `projects.json`; the file now matches the serializer the updater writes with, so a metadata refresh produces no formatting churn.
- [x] Answer whether a low-code visual builder is an agent framework or a multi-agent orchestrator; ADR 019 makes authoring surface a trait, amends the framework definition to cover builders, and requires an interface filter before any is promoted.
- [x] Publish the reviewed builders under the ADR 019 routing; all five are published.
- [x] Review Meta's AI products; the consumer assistant, Muse Code, and the Meta Model API are published as three records across three collections.
- [x] Review Meta Business AI. The name is a marketing umbrella over advertising automation, creative tooling, and one operational product, the Meta Business Agent Platform, which is queued as a candidate. Its governing terms are real and dated, but it belongs to the deferred managed-agent-platform batch rather than a solo promotion.
- [x] Thicken the embedded-library and compatibility-gateway runtime types and screen Intel and NPU server engines; ONNX Runtime GenAI, Xinference, and OpenVINO Model Server are published.
- [x] Screen edge-oriented inference engines and read the inconclusive license files; both were Apache-2.0 despite GitHub reporting NOASSERTION. MLC LLM, Qualcomm GenieX, and TensorRT-LLM are published and llamafile is excluded.
- [x] Settle whether `self_hosted` belongs on a record that ships only a library. The definitions now turn on whether the project ships the server or a helper the user's own server calls, and all 59 records were audited against them.

## Completed on 2026-09-02

- [x] Paginate the directory grids now that the catalog has grown past 250 published records. Each grid (the mixed All view, Systems, Inference services, Local runtimes, Models, and Specifications) slices its filtered, sorted result set through a shared `AtlasCore.paginate` helper instead of rendering the full list into the DOM; a page-size control (24/48/96, persisted per visitor in `localStorage`) is shared across all six, while each grid keeps its own page number. Filtering, searching, sorting, and the reset buttons all return the affected grid to page 1. Share-page generation and the sitemap are untouched, since both are build-time and never read through the client render path.

## Completed on 2026-08-28

- [x] Expand Specifications with WebMCP, OASF, ANP, AP2, UCP, and Commerce ACP, adding explicit web-agent, identity/discovery, metadata-schema, and agent-transaction taxonomy boundaries.
- [x] Extend the Atlas Finder to inference-service jobs and priorities while preserving separate score profiles and collection-native Directory handoffs.
- [x] Repair the duplicate ADR 011 sequence and update task-routing links.
- [x] Define delegated general work as an agent-system role and publish Perplexity Computer and Claude Cowork with explicit boundaries from their vendors' conversational assistants.
- [x] Publish Zep Cloud separately from Graphiti and Slite as a knowledge system whose Agent maintains the product's human-owned knowledge base.
- [x] Reconcile Mem0, Letta Code, Graphiti, Sylph, Pletor, Gorgias Cortex, and Slite Agent with reviewed, provisional, or exclusion dispositions instead of forcing every name into a scored record.
- [x] Show reviewed provider relationship and model-backend traits in project details without adding a sparse directory filter.
- [x] Define managed Inference Services as service records rather than companies, models, or local runtimes in ADR 010.
- [x] Add a six-service inference pilot with taxonomy-backed filters, terms, dated evidence, synchronization, validation, and detail views; later superseded its unscored treatment with ADR 012's dedicated service rubric.
- [x] Expand the pilot to a 36-service, geographically broad baseline using multiple current discovery catalogs and first-party verification for every promoted service.
- [x] Fold inference-service discovery into one Directory surface with systems while preserving separate schemas, filters, detail dialogs, URL state, and score comparability boundaries.
- [x] Add shareable two-to-four-item comparisons scoped to one system-family or inference-service score profile, with aligned decision context and URL restoration.

## Completed on 2026-08-26

- [x] Publish Grok and Microsoft 365 Copilot with explicit consumer, enterprise, agent, API, and model boundaries; retain GroqChat as provisional after its workspace boundary remained unproven.
- [x] Publish Claude, Gemini Apps, Microsoft Copilot, DeepSeek, and Z.ai as evidence-backed general assistants with explicit product boundaries.
- [x] Add assistant systems as a separately scored family with general, enterprise-work, and multi-model-chat roles.
- [x] Publish ChatGPT, Amazon Quick, and T3 Chat as the first evidence-backed assistant comparison across all three roles.
- [x] Add authoritative feed discovery for non-GitHub launches without allowing automation to make editorial conclusions.
- [x] Seed assistant, managed-platform, memory, agent-workflow, and framework candidates from authoritative product boundaries.

## Completed on 2026-08-25

- [x] Add evidence-backed instruction conventions for GitHub Copilot, Gemini CLI, Cline, Cursor, Continue, Roo Code, and Devin Desktop; keep deprecated Cursor and Windsurf paths from becoming duplicate records.
- [x] Harden repository automation with dependency review, npm Dependabot coverage, immutable-action enforcement, a deployment gate, and concise security and contribution guidance.
- [x] Make all active families the clear/default directory state, clear family-scoped roles on family changes, and surface hidden advanced-filter counts.
- [x] Publish GBrain as a local-first external agent-memory service with scoped MIT evidence; confirm GStack remains separately represented as a coding-agent workflow.
- [x] Publish Claude Agent SDK, Google ADK, and Mastra as one-record-per-product framework systems with provider traits, scoped license evidence, and duplicate language-binding dispositions.
- [x] Distinguish mixed-source products from open-core products so an open wrapper around a closed operational runtime is represented accurately.
- [x] Add an unscored Specifications catalog with MCP, A2A, AG-UI, ACP, AGENTS.md, CLAUDE.md, Agent Skills, and Agent Plugins; include taxonomy-backed filters and pinned evidence.
- [x] Publish the static site through a validated, least-privilege GitHub Pages workflow and document repository safeguards.
- [x] Publish Claude Code, Devin, Kiro, OpenHands, OpenClaw, Pi, and Prime Agent with source-model-appropriate license or terms evidence.
- [x] Add the next major coding-agent comparison batch to the provisional queue instead of treating the first expansion as comprehensive.
- [x] Rebrand product-facing copy and metadata from Agent Systems Atlas to AI Systems Atlas, then rename the public repository and Pages site to match.
- [x] Publish DB-GPT and Vanna as reviewed data-analysis systems; exclude SQL Chat on the operational-agent boundary.
- [x] Publish Agno, Haystack, LlamaIndex, and DSPy as reviewed framework systems with pinned license evidence.
- [x] Add a coverage matrix and batch-selection guidance for systematic expansion.
- [x] Replace license-gated inclusion with reviewed source-model and multi-license classification across policy, data, automation, and UI.
- [x] Publish WrenAI as the first reviewed open-core data-analysis agent with scoped Apache, CC BY, and commercial evidence.
- [x] Return ten license-only exclusions to the provisional queue; reserve exclusions for family and role boundaries.
- [x] Define provider relationships as optional reviewed traits in ADR 006, taxonomy, validation, and task-routed documentation.
- [x] Add Claude Agent SDK for Python and DeepSeek Harness to the provisional queue under the initial license-gated policy (later superseded by ADR 007).
- [x] Make discovery recognize agent harnesses and derive candidate families from taxonomy-owned role policy.
- [x] Review the first provider-native batch under the initial operational-source boundary (later superseded by ADR 007).
- [x] Review the first framework batch: publish OpenAI Agents SDK and LangChain with pinned license and provider evidence.
- [x] Introduce durable license-drift incidents, now represented by non-hiding evidence-review signals under ADR 007.
- [x] Preserve candidate and license-review queues as versioned review artifacts.
- [x] Separate editorial `verified_at` from `metadata_verified_at` and field-specific live dates.
- [x] Centralize license identifiers and source-model coherence rules in the taxonomy and validation.
- [x] Tie immutable evidence URLs to recorded Git blob SHAs.
- [x] Expand schema validation across taxonomy axes, queues, dates, URLs, and published copies.
- [x] Add updater regression tests and dependency-free web behavior tests.
- [x] Preserve multi-role finder constraints when opening the directory.
- [x] Backfill live metadata for all active catalog entries.
- [x] Add task-routed documentation for humans and AI agents.

## Backlog hygiene

- Keep items outcome-focused and independently verifiable.
- Link to a policy or ADR instead of duplicating it here.
- Move finished work to the dated completed section; remove obsolete items.
- Keep `Now` to work that can be started today; work with no available action belongs in `Watching`.
- Do not leave completed items in the active sections; they hide the size of the real queue.
