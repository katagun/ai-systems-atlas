# Backlog

This is the ordered source of truth for actionable repository work. Policy and field definitions belong in `docs/`; finding detail stays in the document that established it; completed implementation belongs in Git history.

## Now — recommended sequence

Keep this section to five independently verifiable outcomes that can start without an unresolved product or taxonomy decision.

- [ ] Narrow noisy terms pages with evidence, not heuristics. The checker stores the text behind every terms hash and prints a diff with each drift report, and anchored URLs hash only their section. The first real diffs, reviewed on 2026-09-15 ([#179](https://github.com/katagun/ai-systems-atlas/pull/179), [#182](https://github.com/katagun/ai-systems-atlas/pull/182), [#183](https://github.com/katagun/ai-systems-atlas/pull/183)), came in three shapes, and only one of them wants an anchor. Churn inside the terms body cannot be narrowed: the AWS Service Terms gained a Direct Connect pricing section and dropped a preview label. Pages that state no terms at all want a better URL instead: the Skyvern homepage drifted on its GitHub star badge, and PyPI's langgraph-api page and the Microsoft 365 and Azure product-terms pickers serve a loading shell or a program selector rather than terms. A genuine rewrite, like Baseten's, is the signal working. So add a `TERMS_SECTION_IDS` entry only where a stored diff shows the churn sits outside the terms; the open candidates are pages whose stored text carries marketing chrome, such as Cohere's site navigation and DeepInfra's rotating featured-models menu ([#152](https://github.com/katagun/ai-systems-atlas/pull/152)).
- [ ] Re-surface the three [#165](https://github.com/katagun/ai-systems-atlas/pull/165) `worth_review` signals for a triage decision: AgentsDock (a system in its own right, or a front end over agents already recorded?), Apple's third-generation Foundation Models post (is the reviewable thing the assistant or only a closed model family?), and Norton Neo (a conversational workspace, or a search layer on a browser?). [#188](https://github.com/katagun/ai-systems-atlas/pull/188) merged and replaced the queue, and none of the three left a record in projects, exclusions, or candidates: each becomes a `candidates.json` record checked against first-party sources, or an exclusion with `url` so the next sweep does not re-propose it; a signal is never promoted directly.
- [ ] Work the rest of the 2026-09-15 triage batch. Its forty proposals are on `main` ([#175](https://github.com/katagun/ai-systems-atlas/pull/175)), and the fifteen `out_of_scope` ones are now decided: thirteen became exclusions, `davepoon/buildwithclaude` waits on the skill-pack boundary item below, and `IBM/AssetOpsBench` stays queued because its work-order tools write rather than only measure. That leaves the twenty `review_ready` proposals, each needing the full `docs/CURATION.md` review rather than acceptance of its verdict, and twenty-three `held` candidates (thirteen on the robotics scope decision, nine on the inclusion gate or missing licence evidence, one on batch precedence). Re-verify pinned evidence with `scripts/build_candidate_evidence.py --recheck --baseline-ref <commit before #175>`, since the default run skips blocks already on `main`; the 2026-09-15 pass checked 74 citations and found one drift, `gentleman-programming/gentle-ai`'s README. Of 131 queued candidates, 86 still carry no block.
- [ ] Rebalance the Finder's local priorities against [ADR 030](docs/adr/030-local-first-and-editable-judge-the-content-a-system-keeps.md): a true `local_first` adds 3 to a match score that data sovereignty can move by at most 1, so a local-first record with a low sovereignty score still ranks first, and the agent priority labelled "Local execution and control" reads a trait that no longer judges where execution happens. Test the ranking with records that disagree on the two.
- [ ] Make multi-file canonical updates and generated-tree rebuilds crash-consistent, with staged validation and fault-injection tests at each replacement boundary (`CR-05` in [`docs/CODEBASE_REVIEW_2026-09-05.md`](docs/CODEBASE_REVIEW_2026-09-05.md)).

## Next

Items are grouped by the dependency they resolve. Work top to bottom within a group; groups may proceed independently.

### AI systems papers collection

A sixth unscored collection in the [ADR 008](docs/adr/008-specifications-are-unscored-artifacts.md) shape, exercising the reservation [ADR 021](docs/adr/021-the-research-reference-role-is-removed.md) kept for reference material: primary research papers whose contribution concerns memory, agent, or assistant systems or the infrastructure the catalog's systems are built on. [ADR 022](docs/adr/022-general-pattern-content-is-not-a-collection.md) does not block it — its refusal turned on pattern content having no single steward or reviewable artifact, and a paper has both.

- [x] Review Paper2Agent (`jmiao24/Paper2Agent`) as a scored system — done 2026-09-18, published as `paper2agent`. The Stanford multi-agent framework converts a paper and its codebase into a validated MCP server a user queries through any agent; published in *Nature* on 2026-09-16 ([s41586-026-11044-y](https://www.nature.com/articles/s41586-026-11044-y), arXiv 2509.06917) with code released 2026-09-17 (MIT, 3.0k stars, 431 forks, zero open issues, not archived) — the current viral systems story across Tech Xplore, Yahoo Tech, and Hugging Face. The role decision from the tree: the host, not the repository, runs the coordination loop, so it is a `coding_agent_workflow` on the emdash shape rather than an orchestrator; the shipped verification scripts (verify_workflow.py, verify_mcp_server.py) place it on the scored side of the ADR 031 boundary. MIT verified in the pinned LICENSE blob; Nature and arXiv evidence recorded.
- [ ] Accept (or revise and accept) draft [ADR 033](docs/adr/033-ai-systems-papers-are-unscored-records.md) establishing `directory/papers.json` as a sixth unscored collection in the ADR 008 shape — the draft exists as Proposed; do not confuse it with the deployment-mode record, which took number 034 on renumber. Settle in the ADR: (1) the inclusion boundary — primary research papers on memory, agent, or assistant systems and the retrieval, tool-use, orchestration, context, and evaluation infrastructure they are built on, with surveys, benchmarks, and pattern syntheses excluded in the first pass on the ADR 022 evidence-shape lesson; (2) the evidence rule — the paper's authoritative DOI/arXiv record and venue are the pinned artifact, and a record describes the paper and its relevance to the catalog's systems, never re-derives its results; (3) the licensing posture — record the arXiv distribution licence and publisher terms per record without classifying open or closed; (4) the significance standard — an editorial ecosystem-significance judgement, never popularity or citation counts; (5) `related_systems` links to published records (mem0, letta-code, camel, autogen, swe-agent, openhands, claude-code) so a paper points at the catalog systems built on it. The `research_reference` role stays removed; this is a collection, not a role.
- [ ] Add the Papers schema and pipeline: `directory/papers.json` with the `{"version": "1.0", "verified_at", "papers": [...]}` envelope, a taxonomy `paper_type` group, strict validation, `sync_web_data.py` and `build_share_pages.py` rows, the payload and asset-version scripts, the `docs/DATA_MODEL.md` table and record section, and the `docs/AGENT_DOCS.md` one-commit rule for published files.
- [ ] Surface Papers as a sixth Directory scope under [ADR 013](docs/adr/013-distinct-collections-share-one-directory-surface.md), mirroring the Packs placement: alphabetical scope, search and filters, detail dialog, score-free share pages, no Finder goal, no comparison, no card badges. Exercise the new scope in a browser per `docs/WEB.md`.
- [ ] Curate Papers batch 1 — the ten foundational records: Attention Is All You Need (2017), GPT-3 few-shot learning (2020), RAG (2020), InstructGPT (2022), ReAct (2022), Chain-of-Thought (2022), Reflexion (2023), Generative Agents (2023), MemGPT (2023), Toolformer (2023).
- [ ] Curate Papers batch 2 — the catalog-anchoring records: CoALA (2023), Voyager (2023), AutoGen (2023), CAMEL (2023), HuggingGPT (2023), Lost in the Middle (2023), Self-RAG (2023), SWE-agent (2024), AgentBench (2023), SWE-bench (2023). Later batches already identified: DPR, ColBERT, Gorilla, CodeAct, Tree of Thoughts, WebGPT, Constitutional AI, OpenHands, and the MAST failure taxonomy (Cemri et al., 2025). Two boundary shapes from the reader: papers with companion code — Dream-RSI (Zheng et al., 2026, arXiv 2609.14858, companion repo `zhengkid/Dream-RSI` whose code release is pending, so the record pins the paper and names the repo as evidence rather than treating reference code as a system) — and primary research that studies many harnesses, such as the 176-setting empirical harness comparison in [arXiv 2609.20804](https://arxiv.org/abs/2609.20804) (Fan et al., 2026), which is in scope under "evaluation of these".
- [ ] Write `docs/PAPERS.md` and refresh the docs that enumerate collections: `docs/DATA_MODEL.md`, the `docs/COVERAGE.md` snapshot, `docs/TAXONOMY.md`, `docs/WEB.md`, `ROADMAP.md`'s ecosystem-context section, and the `AGENTS.md` just-in-time table.

### Typed judgments in the pipeline

Ideas from the 2026-09-20 brainstorm on [TypeSafe](https://docs.typesafe.ai/llms.txt), a managed API whose model returns typed answers and probabilities (a yes/no probability, one choice from a set, a score on described levels) rather than generated text. Three limits bind every item. Working rules 8 and 9 stand: a judgment may sort, rank, or flag, and never writes a classification, score, evidence, confidence, trust record, or `verified_at`. The site is static and the API key must stay off the client, so nothing calls the API at read time. The key lives in the machine's environment (an ignored `.env` or the launchd plist), never in the repository, and `verify` never needs the network. Fetched pages remain attacker-influenceable data under [ADR 028](docs/adr/028-attention-sources-are-pointers-not-claims.md); a model that returns no text gives an injected page less to steer, which is a reason to try it and not a reason to trust it. Validate each use against outcomes a human already recorded before anything depends on it.

- [ ] Re-measure the `hn-signals` pre-rank once it has real runs behind it, then decide whether it earns its place. `scripts/rank_signals.py` shipped rank-only on 2026-09-20: `prepare` orders the pending list by one yes/no probability per page — is this a shipped product that is itself a memory, agent, or assistant system — before applying `--limit`, every signal still gets an assessment, and any failure leaves sweep order (see "Pre-ranking the queue" in [`docs/OPERATIONS.md`](docs/OPERATIONS.md)). The spike behind it replayed 265 re-fetched pages from every queue in Git history: 20 seconds in total, about 141,000 tokens and $0.006 per 60-signal queue, and against the 59 readable signals the routine had already judged, all five `worth_review` ranked in the top eight. Three cautions bound that: five positives is a small sample; only 106 of the 265 pages still matched their pinned hash, so the labels were given to text that has since changed; and a companion question about which collection a page belongs to was unreliable — it called the iOS 27 page not a product and a Moon essay a model release — so only the yes/no probability is used. Rank-and-skip was considered and refused: timing the seven scheduled runs showed the routine is blocked rather than slow (next group), so skipping would trade recorded findings for minutes nobody is spending. After thirty days of ranked runs, compare rank against verdict on pages judged the same day they were ranked, and remove the step if the top of the order is not where the `worth_review` signals are. The question also cannot know what the catalog already holds — the top of the unlabelled ranking was pages about Claude, the Gemini app, and the OpenAI Agents API, which the routine calls `out_of_scope` as rehashes — so the decided-records match below is what would make the order sharper.
- [ ] Flag evidence that may not support its claim. Exact quote and hash matching stay in code; add one judgment per citation on whether the pinned text supports what the record says, following the [citation-check cookbook](https://docs.typesafe.ai/cookbooks/citation_check.md), and send low-confidence cases to a human. Fit it to `scripts/build_candidate_evidence.py --recheck` and to trust-record batches, where fabricated delegate research has been the costliest failure.
- [ ] Sort stored terms-drift diffs into the shapes the 2026-09-15 review found — a change inside the terms body, marketing chrome, a page that states no terms, pricing or service-list churn — so the weekly report leads with the diffs that need reading and names candidates for `TERMS_SECTION_IDS`. The label only orders the report; the stored diff stays the evidence, as the first `Now` item requires.
- [ ] Order the candidate queue. Over each queued README, ask the [ADR 031](docs/adr/031-skill-packs-earn-records-by-owned-state-or-enforced-work.md) gate questions (owns state, enforces work), the likely collection, and the likely hold reason in one request, and use the answers only to decide what to review next.
- [ ] Match new signals and candidates to decided records beyond URL keys. `decided_url_keys` in `scripts/sweep_hackernews.py` misses renames, forks, and one product under another repository; the [entity-alignment cookbook](https://docs.typesafe.ai/cookbooks/entity_alignment.md)'s three levels map to same record, distinct, and hand to the curator.
- [ ] Build review worklists for the reconciliations under "Editorial and taxonomy decisions": records whose `current_repo_note` holds reader-facing boundary prose, and `human_editable: true` records that name no stored content a person can edit. The output is a list to review, never an edit.

### Reliability and maintenance

- [ ] Make an `hn-signals` run finish unattended, and make a stuck one visible. Timing the seven scheduled runs from their transcripts on 2026-09-20 showed the routine is blocked, not slow: the 2026-09-14 run judged 36 signals and opened a pull request with 7 minutes of model generation inside a 124-minute span, a `cat` of the bundle waited 24 hours on 2026-09-18, and a `git status` waited 14 minutes on 2026-09-20 while the machine was awake. The runs use the default permission mode against an allowlist of one-off approved commands in the primary checkout's `.claude/settings.local.json`; `uv run *` is on it and `prepare` took 35–41 seconds, and the commands that hung are not. The transcripts do not record the approval prompt itself, so that cause is inferred from the pattern. Three of the seven runs also stopped on the account's session usage limit, once for 5 hours 46 minutes, because the routine runs on the largest model and shares a quota with interactive work. [#260](https://github.com/katagun/ai-systems-atlas/pull/260) told the prompt to use only `prepare`, `finish`, Read, and Edit. Still open, and none of it is repository work: add an `Edit` rule for the worktree's `directory/hn-signals.json` to that allowlist and drop the broad `python3` rules an unattended run should not hold; move the routine to a smaller model or an hour when the quota is fresh; then confirm two consecutive runs reach `finish` with nobody present. `candidate-triage` closed its own bar on 2026-09-15 — started 09:01, committed 40 proposals at 12:03, after the scheduler had reported it `succeeded` while the session was still working ([#173](https://github.com/katagun/ai-systems-atlas/pull/173)) — and deserves the same transcript timing before its three hours are read as work. A heartbeat or bounded runtime is still worth having, because the scheduler's status distinguishes neither a waiting run nor a rate-limited one from a working one.
- [ ] Bring the attention-source sweep wrapper into the repository, with tests. It lives only at `~/.local/bin/atlas-hn-sweep.sh` on one machine, so the step that decides whether a day's signals survive sits outside `verify` entirely — which is exactly where the 2026-09-15 failure happened, and where its fix (reset onto `origin/main`, keeping one recovery ref) still sits, verified by hand rather than by a test. `docs/OPERATIONS.md` documents both wrappers as copy-paste snippets and ships neither. Keep machine-specific paths and `PATH` in the launchd plist; put the git sequence under review.
- [ ] Let the Directory boot with the collections that loaded, treating taxonomy as the only possible hard dependency and making unavailable collections retryable (`CR-06`). Add one failure-and-recovery browser test per boot payload.
- [ ] Replace `https://` prefix checks with one shared absolute-HTTPS parser that requires a host, rejects credentials, and carries collection-specific policy explicitly (`CR-07`). Use the same policy at validation and network boundaries.
- [ ] Choose a review-age threshold and surface it. [#169](https://github.com/katagun/ai-systems-atlas/pull/169) added `scripts/report_review_age.py`, which is read-only and wired into nothing; on 2026-09-14 the oldest editorial date was 22 days old and 148 systems carried only pinned-blob evidence with no dated review. Decide what age counts as stale per collection, then print `--older-than` rows in the weekly refresh summary so a stale review is a visible fact rather than a report someone remembers to run.
- [ ] Decide whether `local_first` needs a third state for records that keep nothing of their own, such as libraries whose storage the application chooses. Today validation accepts only booleans and the comparison table prints "No", which reads as a negative claim about a record that has no data to keep; [ADR 030](docs/adr/030-local-first-and-editable-judge-the-content-a-system-keeps.md) left them false.
- [ ] Exercise the declared Python 3.11 floor in CI, or raise the declared minimum to the version actually supported (`CR-08` in [`docs/CODEBASE_REVIEW_2026-09-05.md`](docs/CODEBASE_REVIEW_2026-09-05.md)). Run browser installation and end-to-end tests only once.
- [ ] Document and test repository rename and transfer handling as recoverable review events that preserve evidence history.
- [ ] Add automated accessibility checks to the existing Playwright suite without adding a shipped runtime dependency.
- [ ] Validate the numeric snapshot in [`docs/COVERAGE.md`](docs/COVERAGE.md) against the canonical files so count drift fails loudly; keep editorial coverage-signal review a separate human task.
- [ ] Disable administrator bypass for the `github-pages` environment in GitHub's UI; no supported API mutation exists.
- [ ] Bring the blocking boot payload back under the 60 KB check in [`docs/WEB.md`](docs/WEB.md). It measured 66.8 KB on 2026-09-16 before the Agent packs collection, 68.3 KB after, and 82.2 KB on 2026-09-20; `app/models.json` (31.0 KB) and `app/systems.json` (24.8 KB) carry most of it, so look first for a detail-only field that reached `BOOT_FIELDS` and at source-catalog growth.
- [ ] Add a pack case to `tests/e2e/share-pages.spec.js` and to manual check 20's Copy link round trip in [`docs/WEB.md`](docs/WEB.md); nothing automated opened a rendered pack share page, which is how a duplicated repository link survived until the whole-branch review of [#208](https://github.com/katagun/ai-systems-atlas/pull/208).
- [ ] Remove the callerless `activateView` aliases (`inference-services`, `local-runtimes`, `agent-packs`) in `web/app.js` together, or give them a caller; the branch is a nested ternary that grows with every collection and `VIEW_IDS` never admits any of them.
- [ ] Reconcile `SEARCH_FIELDS["packs"]` in `scripts/build_web_payload.py` with `PACK_VIEW.searchFields` in `web/app-core.js`: the comment says they match exactly, but the index adds `installs` and `not_a_system`, and the latter's boilerplate ("owns no state") matches across every pack. Decide whether `not_a_system` belongs in search at all.
- [ ] Decide whether the published `packs.json` endpoint gets a `data-versions` stamp like the other published files in `scripts/build_asset_version.mjs`; no page fetches it today, so this is symmetry, not a cache bug.

### Editorial and taxonomy decisions

- [ ] Extend the candidate-triage routine's routing so a review-ready pack can be proposed for `packs.json` without a `held_by` hold; until then packs wait under "ADR 032 pack review".
- [ ] Decide where two kinds of pack sit in the taxonomy, or that they do not. Both pass [ADR 031](docs/adr/031-skill-packs-earn-records-by-owned-state-or-enforced-work.md) and fit no role, so they stay queued rather than being forced into one. `FailproofAI/failproofai` governs other harnesses: `src/hooks/policy-evaluator.ts` emits a `deny` permission decision on `PreToolUse` and `src/hooks/enforcement-capability.ts` traces that verdict to a call site per harness across nine of them, marking unverified rows unknown rather than assuming they block. Nothing in the role list describes a system whose product is constraining another agent's execution; `agent_framework_sdk` is for building agents. `browser-use/video-use` edits video through ffmpeg and a transcription API it calls itself, while `coding_agent_workflow` is defined as turning coding agents into "a repeatable software-delivery system". Decide whether either warrants a role, whether the existing definitions should widen, or whether the inclusion gate's role requirement is the right place to refuse them.
- [ ] Add Elastic License 2.0 as a named license identifier if authoritative scope review confirms the need, then use that decision in Open SWE and the MindsHub review. MindsHub's current repository is a code-free superproject over four submodules under three licenses; review the submodule products, not inherited stars or the former MindsDB tree.
- [ ] Represent a system operator's own models and explicit Azure OpenAI or Bedrock bring-your-own-key routes without misusing `openai_compatible`. Decide whether `model_backends` gets an operator-relative value or vendor-specific identifiers, then reconcile Cursor, Devin Desktop, Antigravity, and the two vendor-specific precedents already recorded: `poolside` and the `vercel_ai_gateway` identifier added for fx in [#211](https://github.com/katagun/ai-systems-atlas/pull/211).
- [ ] Decide whether hosting other vendors' agents is a recordable capability independent of protocol. Test the proposed capability against Devin Desktop, Warp, and the harness layer inside the published `vercel-ai-sdk` record ([#252](https://github.com/katagun/ai-systems-atlas/pull/252)), which runs ten other vendors' coding agents in sandboxes behind one interface and can record that today only as `multi_agent` plus an `external_sandbox` boundary; ACP remains a Specification rather than the capability itself.
- [ ] Write down when a vendored or third-party component's licence is material, then reconcile the two records that decided it differently. `vercel-ai-sdk` lists ISC and is `mixed_open_source` because a path-scoped `LICENSE` file governs a converter vendored inside a core package; `fx` stays Apache-2.0 only although its `THIRD_PARTY_NOTICES.md` carries MIT sound recipes and Unicode data compiled into the binary. Put the rule in [`docs/CURATION.md`](docs/CURATION.md) beside the component-scope paragraph.
- [ ] Reconcile `human_editable` on code-first framework records with [ADR 030](docs/adr/030-local-first-and-editable-judge-the-content-a-system-keeps.md). `vercel-ai-sdk` and smolagents are false because agent definitions are integrator code; LangChain, Pydantic AI, OpenAI Agents SDK, and Strands Agents SDK still read true. Keep true only where the record can name stored content a person edits without writing code, such as prompt files.
- [ ] Make product-boundary notes reader-visible, then give rename and acquisition history a correctly scoped field. `current_repo_note` is repository-scoped in [`docs/DATA_MODEL.md`](docs/DATA_MODEL.md), yet 105 of 174 records inspected on 2026-09-03 used it for reader-facing boundary prose that the app and share pages did not render. Settle rendering first, then migrate non-repository notes without losing history.
- [ ] Add a browser-extension deployment value before publishing nanobrowser; do not misclassify it as `desktop`. Treat its dormancy since November 2025 as maturity evidence, not as the deployment decision.
- [ ] Review the five robot software candidates held under `robot software role decision` — Dora, OM1, LeRobot, openpi, and the Safari SDK — through the full [`docs/CURATION.md`](docs/CURATION.md) workflow now that [ADR 036](docs/adr/036-the-agent-to-physical-world-boundary-is-in-scope.md) puts the physical-world boundary in scope. Put the role question to ADR 011 and ADR 023 directly, naming an operational outcome rather than "a model's output driving a physical actuator"; add actuation and perception trait values and robotics discovery keywords in the same change as the first record that needs them.
- [ ] Land the Robots collection. The decision ADR 015 requires is drafted as [ADR 037](docs/adr/037-robots-are-unscored-records-of-what-a-vendor-documents.md) (Proposed), with the schema, boundary, and comparison policy for AI robots whose first-party documentation either names a learned model or policy or documents a supported way to run the reader's own, and [`docs/ROBOTS.md`](docs/ROBOTS.md) carries the reviewer's workflow. What remains is the plumbing — taxonomy groups, validator, generators, operations scripts, the hidden-while-empty web scope, tests, and the remaining documents — which turns ADR 037 Accepted, and then the first reviews: Spot, Unitree G1, Figure, and 1X NEO wait under `robots collection decision` and are each reviewed against the six conditions, published or kept held with a stated reason. The steps are in [the plan](docs/superpowers/plans/2026-09-20-robots-collection.md).
- [ ] Decide whether the Models collection reaches action-emitting policies. Isaac GR00T, OpenVLA, and Alpamayo wait under `action-policy model boundary`; admitting one amends ADR 025's text-output criterion and depends on the manual model-candidate path below, because none is in models.dev.
- [ ] Review URDF against the specification boundary. `ros/urdfdom` waits under `robot description specification boundary`: the repository is a parser, and the specification page its README names is an end-of-life wiki that batch 39 read but did not pin, so requirement 1 in [`docs/SPECIFICATIONS.md`](docs/SPECIFICATIONS.md) is in doubt before any scope value is.
- [ ] Add a manual model-candidate path that preserves the pinned-source invariants, then review individual Liquid AI LFM releases under [ADR 025](docs/adr/025-model-releases-are-independent-curated-records.md). Do not publish a lab umbrella or score quality, benchmarks, or parameter counts.
- [ ] Decide whether the publishing party belongs among [ADR 020](docs/adr/020-derivative-records-turn-on-operational-boundary.md)'s operational-boundary fields. Brief a skeptic before drafting; do not fall back to repository lineage.
- [ ] Decide whether SeekrFlow's named product boundary supports a separate inference-service record. A price-page component is insufficient unless first-party evidence establishes a service boundary distinct from the published agent runtime.
- [ ] Decide whether managed search and tool APIs belong in any Atlas collection. Exa (exa.ai), a web-search API marketed for agents, fits no current boundary: it is not a memory, agent, or assistant system (a plain API plus client SDK per `docs/CURATION.md`'s scope boundaries), not an inference service (ADR 010 scopes those records to model APIs), and not a local runtime, model, or specification. Tavily, Serper, and Brave Search API face the same question. Options to weigh: a new unscored collection, an ADR that extends the inference-service boundary with a tool-API service type, or an exclusion recording the boundary. Only after the decision can such candidates be queued and reviewed.
- [ ] Decide where TypeSafe sits, as two separate questions. The service: `POST /v1/systemone` is a managed model API with first-party pricing, rate limits, a data-handling statement, and legal terms ([models page](https://docs.typesafe.ai/models.md), read 2026-09-20), so test it against [ADR 010](docs/adr/010-inference-services-are-unscored-service-records.md)'s boundary, which was written for APIs that generate; it returns typed answers and probabilities, not text. The model: `jev-1.13.0` is closed, served only there, and absent from models.dev, so it meets both the manual model-candidate gap above and [ADR 025](docs/adr/025-model-releases-are-independent-curated-records.md)'s text-output criterion, the same criterion the action-policy item tests from the other side. Its `typesafe-ai/skills` repository is a one-skill documentation pack that owns no state and enforces no work, so [ADR 031](docs/adr/031-skill-packs-earn-records-by-owned-state-or-enforced-work.md) decides whether it is a pack record at all. Queue a candidate or write an exclusion with `url` either way. If the pipeline items under "Typed judgments in the pipeline" ship first, the Atlas is a customer of what it reviews: say so in the record and keep the review on first-party terms and documentation only.
- [ ] Settle `second-brain-starter`'s type in its own prose: its `description` reads like a skills bundle while `pack_type` is `vault_bundle`; one clause in `not_a_system` naming the memory templates (SOUL, MEMORY, HEARTBEAT, USER, `daily/`) as the vault the skill maintains would stop the next reviewer reopening it.
- [ ] Widen `agent-toolkit`'s `hosts` beyond Claude Code only when its repository documents an install into Codex or Cursor rather than a compatibility sentence about `npx skills add`; pin that documentation as evidence when it appears.

### Coverage batches

- [ ] Curate the fortieth models.dev queue batch: Amazon Nova (`amazon/nova-2-lite`, `nova-lite`, `nova-micro`, `nova-premier`, `nova-pro`). All five are API-only under Amazon Bedrock and the Nova model terms. Promote or exclude every reviewed candidate in the same change with first-party evidence and the `model_access` score under `docs/MODELS.md`. The OpenAI and leftover clusters stay queued for later.

### Reader experience

- [ ] Give share pages the site's own shell, as [#168](https://github.com/katagun/ai-systems-atlas/pull/168) gave the blog: link `web/styles.css` and `web/fonts.css` under the content stamp, carry the directory header, theme control, and footer, and retire `scripts/page_shell.py`'s private `STYLE` once nothing uses it. Share pages must still show no score and load no application script.
- [ ] Replace the per-collection search boxes with one search across records, specifications, and taxonomy terms that lands in the correct collection and never mixes score profiles under [ADR 013](docs/adr/013-distinct-collections-share-one-directory-surface.md).
- [ ] Show related records and previous/next navigation inside detail dialogs using existing family, role, and successor data.
- [ ] Add a "ships as a library" facet to the Systems filters from the existing `deployment` value `library` and `agent_interfaces` value `api_sdk`. On 2026-09-17 the question of an SDK category was answered with the existing rule that packaging is a trait, not a role ([ADR 003](docs/adr/003-multi-axis-directory.md), [ADR 018](docs/adr/018-operating-party-is-a-trait-not-a-role.md), [ADR 019](docs/adr/019-authoring-surface-is-a-trait-not-a-role.md)); the facet is the reader-facing answer, and 29 framework records plus 40 more across other roles already carry the traits.
- [ ] Surface local-runtime GitHub stars as a card footer signal and sort option; the canonical data and updater already carry `stars` and `stars_verified_at`. Keep it out of card badges: the "Card badges" contract in [`docs/WEB.md`](docs/WEB.md) excludes automated signals. Runtime card footers currently print model formats, so decide what the stars count displaces, as system cards already show stars there.
- [ ] Stop card-badge definitions from being announced twice. Each badge carries its definition in both visually hidden text and a `title`, and Chromium exposes the `title` as a description that some screen readers read again. Keep one source of the definition for assistive technology while pointer users still get the tooltip, and cover it with the badge Playwright spec.
- [ ] Stop share-page eyebrows repeating the collection label (`Agent pack · Agent pack · Vault bundle`, and the same for inference services, local runtimes, and models): `render_page` already prefixes `COLLECTION_LABELS[kind]`, so drop the hard-coded prefix from each `_facts_for` branch in `scripts/build_share_pages.py`.
- [ ] Cap or drop the `installs` fact on pack share pages; `claude-code-tresor`'s is a paragraph of roughly 1.5 KB in one `<dd>`, and the design listed steward, hosts, install mechanism, and licences as the share-page facts.

## Watching

These items have no repository action until their stated trigger occurs. Move one back to `Now` or `Next` only when that trigger is met.

- [ ] Revisit claude-mem's `local_first` if its installer starts writing cloud-sync credentials at sign-in without an explicit hosted-plan choice. [#154](https://github.com/katagun/ai-systems-atlas/pull/154) kept it true because sync begins only after CMEM Pro enrollment, but the interactive installer already requires sign-in and preselects the hosted observer.
- [ ] Revisit vibe-kanban if commits resume or the repository is archived. Its maintainer dissolved, the shipped product retired its board flow, and no current status describes that unarchived state accurately.
- [ ] Revisit dendron if the repository is archived. It is maintenance-only and dormant, but its hosts and extension remain available; its source also contains no model or embedding code, placing it beside ordinary personal knowledge tools rather than agent systems.
- [ ] Revisit plandex if commits resume or the repository is archived. Its hosted tier ended and first-party hosts no longer resolve, while the self-hosted terminal agent remains in an unarchived dormant repository.
- [ ] Revisit LaVague if commits resume or the repository is archived. The genuine browser-agent loop is dormant, packages are stale, and a security report about executing model-derived page content remains unanswered.
- [ ] Revisit Claudable if commits resume or the repository is archived. It is a coding-agent workflow supervisor, but it lacks a correctness gate, current releases, and recent activity.
- [ ] Revisit OpenCursor only after it has its own adoption and correctness gate. Current repository stars and forks belong to an unrelated predecessor, and the rewritten extension has only scaffold-level tests.
- [ ] Resolve GroqChat only when first-party evidence establishes a durable workspace distinct from Groq Playground and GroqCloud.
- [ ] Revisit Flowise if a community fork gains maintainer endorsement or clear succession; the archived record has no declared successor.
- [ ] Revisit Xinference if the commercial edition publishes governing terms.
- [ ] Revisit Microsoft Discovery if product-specific commercial terms are published. The MIT catalog and installer pointer do not govern the closed product.
- [ ] Re-read and scope the Meta Business Agent terms when their browser-only host becomes reachable.
- [ ] Revisit Google Co-Scientist and AlphaProof when either becomes generally available through a self-serve or contractable product boundary.
- [ ] Revisit Meta's announced Harness framework only after a repository, documentation, or first-party product page exists.
- [ ] Revisit fx's `model_backends` when a tagged release ships the configurable model connections merged in `vercel-labs/fx#780` on 2026-09-15 (61 commits past v0.0.10 on 2026-09-17; v0.0.10 was still the latest tag on 2026-09-20): add `openai_compatible` and `local_runtime` with the release notes as evidence and retire the first weakness.
- [ ] Revisit `vercel-ai-sdk` when any of three things changes: the harness packages drop their experimental label, a sandbox provider other than Vercel's can host the bridge-backed harnesses, or the workflow runtime behind the durable agent leaves its beta tag. Each retires a recorded weakness and moves observability and recovery or data sovereignty.
- [ ] Revisit Harvey Tenet only if Harvey publishes weights, a model card with a license, or a callable endpoint. Harvey's 2026-08-20 research preview names a Kimi K3 base post-trained with Fireworks, but neither that post nor Fireworks' 2026-08-26 write-up offers an artifact, license, or API; Hugging Face lists no Harvey organisation and models.dev has no source ID. It therefore fails the identity and evidence gate in [`docs/MODELS.md`](docs/MODELS.md) and cannot be held in `model-dispositions.json`, which requires a snapshot source ID. Today it is a proprietary checkpoint inside the Harvey product, which the reviewed Kimi K3 record's boundary already excludes as a downstream fine-tune; the Harvey product itself would meet the same repo-less queue gap recorded for Elicit. See the dispositions item below.

## Later

- [ ] Re-review exclusions on a cadence now that each carries `verified_at`. The 2026-09-17 pass re-checked all eighty-nine and lifted one (Second Brain LLM Wiki, to `packs.json`); a later pass can sort `directory/exclusions.json` by `verified_at` and re-check only the oldest, starting with the reasons that rest on a movable tree fact rather than a category: Stagehand's removed agent loop, herdr and claude-squad's provider-free manifests, glean's planned AI phase, InsForge's undocumented memory route, LazyCodex's submodule, llamafile's upstream pin, and the availability gates on Google Co-Scientist, AlphaProof, ALE-Agent, and SWE-2. Categorical exclusions (bibliographies, courses, benchmarks, physics and robotics stacks, game engines, SDK bindings, observability products) need only a metadata check for rename, archive, or a changed description.

- [ ] Build an original, dependency-free diagram layer for the Atlas's own taxonomy when explanatory work outranks catalog maintenance. Start with family boundaries, score-profile separation, and the system/service/runtime distinction; source every number or label it illustrative.
- [ ] Review vendor-hosted editions of self-hosted gateways as routing aggregators, starting with Portkey and Helicone; keep the self-hostable proxy software outside the service collection under ADRs 010 and 015.
- [ ] Decide whether domain-specific model APIs belong in Inference Services, using AlphaGenome as the boundary case. Write the general-inference-substrate rule if that is the intended limit.
- [ ] Reassess API clients, adapters, observability SDKs, or a new collection only after a concrete user question justifies reversing their current exclusion. The 2026-09-17 question ("should SDKs be a category?") did not: SDKs that own an agent outcome are already published under `agent_framework_sdk` or as secondary roles, and the answer was a filter facet (see Reader experience), not a collection.
- [ ] Let one implementation own the `?v=` asset-stamp convention. `scripts/build_asset_version.mjs` stamps `web/index.html`, and since [#168](https://github.com/katagun/ai-systems-atlas/pull/168) `scripts/build_blog.py` recomputes the same twelve-character SHA-256 prefix for the stylesheet and fonts a blog page links, so a change to the convention must be made twice and checked twice. Fold the blog into the stamper, or have the stamper publish the versions the Python builders read.
- [ ] Consider making card badges apply their matching filter when clicked. Badges shipped as plain labels; clickable filters need a filter for every badge, a place in the mixed All view (which has no filters), URL state, and phone-safe hit targets beside the card actions.
- [ ] Settle whether an aggregator that resells other aggregators is reviewable and how its operational boundary differs from the upstream service.
- [ ] Reconcile inference-service `operator` values around the legal entity the customer contracts with, rather than mixing brands and entities.
- [ ] Audit models.dev providers for service coverage in boundary-collapsed batches; use it as third-party discovery metadata, never as evidence.
- [ ] Settle Glama's current product boundary before review; its gateway, tool-server index, and chat workspace no longer form an obvious single record.
- [ ] Review Novita AI and Lambda Inference once governing terms establish retention, residency, and delivery boundaries.
- [ ] Review Qwen Chat and Kimi as assistants with separate product-terms and governance passes.
- [ ] Allow pinned license evidence from a repository other than the record's own, covering the GenieX proprietary component and watsonx Orchestrate client without degrading immutable evidence to dated web prose.
- [ ] Review a proprietary or managed self-hosted compatibility gateway; both current gateway records are open source.
- [ ] Decide whisper.cpp under the [ADR 015](docs/adr/015-local-runtimes-are-self-operated-execution-records.md) purpose test and [ADR 017](docs/adr/017-local-runtime-eligibility-ignores-modality.md) modality rule.
- [ ] Extend runtime model-format vocabulary in one deliberate pass when Triton, KServe, Seldon, or another classical-machine-learning server is reviewed; do not add speculative enum values record by record.
- [ ] Decide whether CrewAI covers only its open framework or also the AMP suite; its current `self_hosted` value relies on AMP's on-premise option.
- [ ] Decide whether the Codex record is the terminal agent alone or also the IDE extension and desktop app distributed from the same repository.
- [ ] Re-check Khoj's status and links; its desktop download is missing and the homepage now leads with a different product.
- [ ] Decide whether Perplexity Personal Computer is operationally distinct from the cloud Perplexity Computer record.
- [ ] Curate Claude Mythos 5.1 as the first model reviewed without a models.dev row ([ADR 038](docs/adr/038-reviewed-models-may-precede-their-models-dev-source-row.md)): `init-gap anthropic/claude-mythos-5-1`, first-party evidence only, separate change from the mechanism.
- [ ] Decide how to hold or exclude a release models.dev does not list. `model-dispositions.json` is keyed by snapshot `source_id`, so such a release can be reviewed (ADR 038) but not dispositioned.

## Backlog hygiene

- Order work within each section; the first unchecked item in `Now` is the default next step.
- Keep `Now` at five outcomes or fewer and require each to be startable without an unresolved decision.
- State the outcome and acceptance signal here; link evidence, policy, and design detail instead of duplicating them.
- Merge overlapping batches and make dependencies explicit.
- Move condition-triggered work to `Watching`; keep low-priority but actionable work in `Later`.
- Remove completed items. Git history is the completion record.
