# Research synthesis

## Selection method

The catalog is coverage-weighted rather than a naïve star leaderboard. Popularity is tracked separately, but a selected project must contribute a distinct architectural or operational lesson. Memory, agent, and assistant projects are evaluated only within their separate families and score profiles.

### Memory-system research set

| Project | Primary lesson | Strongest contribution | Principal weakness |
|---|---|---|---|
| Letta Code | Embedded stateful agents | Memory, identity, skills, schedules, dreaming, and Git-tracked agent state | Self-editing memory can drift; current codebase is newer than the historically popular repository |
| Mem0 | External agent memory | Simple production-oriented API, scoped memories, multi-signal retrieval | Opaque extracted facts can become a second source of truth |
| Graphiti | Temporal context graph | Provenance, validity windows, changing truth, and as-of queries | Operational graph/LLM complexity |
| Basic Memory | Human–agent bridge | Shared Markdown, MCP/skills interoperability, semantic and graph recall | Lightweight semantics and local sync burden |
| Khoj | Complete AI second-brain product | Broad source/model support, agents, automation, self-hosting | Database-centric canonical knowledge and weak explicit supersession |
| Logseq | Local graph PKM | User-owned linked knowledge, blocks, backlinks, and queries | Complexity and architectural transition risk |
| Trilium | Hierarchical PKM | Hierarchy, cloning, revisions, encryption, scripting, REST API, large-vault maturity | Less portable database-centric storage |
| AppFlowy | Structured workspace UX | Documents, databases, projects, native cross-platform experience, data control | Heavy system whose AI memory lifecycle remains secondary |
| OpenRecall | Open ambient capture | Local, cross-platform, searchable screenshot history | Privacy, storage, noise, and slower project activity |
| claude-mem | Coding-session memory as a service | Store reachable through an interface, a tool protocol, and a package at once, with provenance as a first-class table | Two storage models coexist; writing memory requires an off-device model call |
| agentmemory | Memory lifecycle mechanics | Supersession, decay, expiry, and eviction implemented separately, running with no key and no model calls | Pinned third-party engine dependency; more open issues than closed |
| memU | Judgement delegated to the caller | Plain markdown on disk, a store that makes no model calls, one backend shared across hosts | Non-canonical licence text no identifier matches; retrieval is cosine similarity alone |
| OpenHuman | Personal memory the owner can read | Editable markdown vault the agent reasons over, with local-only mode enforced at the client construction point | An assistant rather than a service: no external interface lets other software read its memory |
| CodeGraph | Code graph as shared infrastructure | Measured per-language extraction coverage on named public repositories, and uncertain edges marked rather than asserted | Telemetry ships enabled by default; the schema keeps only current state, so history cannot be queried |
| GitNexus | Cross-repository code graph | Ad-hoc graph queries, hybrid keyword and vector search, and contract registries that join microservice graphs | Noncommercial licence bars commercial use and reads as unlicensed to automated detection; the docs contradict each other on incremental indexing |
| OpenViking | Memory as a browsable filesystem | Tiered summaries and retrieval that keeps the path which produced each result | Copyleft server with a permissively licensed client, and an unlicensed Python library between them |
| Supermemory | Open adapters over a closed core | Eight framework adapters and a tool server, all permissive and forkable | The memory engine is absent from the repository the documentation calls open source |
| Onyx | Honest open-core boundary | Fifty-five connectors and a full retrieval stack that run without the enterprise tree | Permission sync and post-query filtering are enterprise-only, which is what mixed-permission data needs |
| AFFiNE | Permissive client on a proprietary server | A genuinely local-first offline path with reusable editor internals | Synchronization, search, and all AI sit in one licensed server, and self-hosting caps at ten seats |
| screenpipe | Ambient capture goes source-available | An agent-facing API and tool server over a local database with retention and export controls | Nothing in the tree is open since June 2026; analytics defaults on despite the local claim |
| Hindsight | Beliefs with evidence, not a fact pile | Consolidation into observations with quoted proof, and a third-party runtime already using it as a backend | Every accuracy figure is vendor-run; the wrapper defaults to the vendor cloud |
| DocsGPT | Enterprise governance not withheld | Role management, single sign-on, and teams in the permissive tree; agents as compatible and tool-server endpoints | The quick-start routes inference through the vendor's shared key |
| MemOS | Memory cubes without reciprocation | Per-user, per-agent isolation, a real service surface, and on-device plugins for three runtimes | No targeted runtime names it as a backend; defaults and benchmarks are vendor-run |
| Memobase | Memory as an editable user profile | A configurable profile schema plus an event timeline, directly editable through the API | Every first-party hosted surface is gone, and development stopped in January |
| Chroma | Vector storage as a component | Reusable embedding store for applications, with broad ecosystem integrations | Not a second brain by itself; ingestion, governance, provenance, and interface layers all remain to be built |
| Cognee | Graph enrichment over mixed sources | Graph-oriented memory pipelines across several data and storage backends | Complex pipeline surface and less transparent canonical memory |
| Foam | Smallest viable local graph | Plain Markdown and Git inside the editor, with low lock-in | No built-in AI memory, and the whole experience is tied to VS Code |
| GBrain | Human-owned memory with production retrieval | Git-versioned Markdown as the system of record over hybrid lexical, vector, and graph retrieval | A very broad CLI, server, graph, and job surface makes setup and upgrades costly |
| Joplin | Offline-first sync and encryption | Mature offline sync with end-to-end encryption and a broad import/export ecosystem | Weak graph semantics, and AI is not the architectural centre |
| LangMem | Memory as explicit application logic | Focused memory primitives that plug into agent graphs | Framework coupling, with nothing user-facing built on top |
| Milvus | Similarity search at scale | Distributed vector search foundation with broad ecosystem integrations | Not a second brain by itself; ingestion, governance, provenance, and interface layers are still required |
| Open Notebook | Source-grounded notebook boundaries | Research workflows scoped to a chosen source set, with flexible model support | Long-term identity and memory are limited, so it suits study more than operational life management |
| Qdrant | Retrieval as a tuned primitive | High-performance similarity search with broad ecosystem integrations | Not a second brain by itself; ingestion, governance, provenance, and interface layers are still required |
| Quivr | Vector-centric RAG as contrast | Composable RAG building blocks with model and vector-store flexibility | Temporal memory is weak, and product engineering is still needed around it |
| Reor | Local AI over Markdown | Local models and semantic linking over Markdown, with a strong privacy posture | Repository archived, leaving a desktop-centric tool with a limited temporal and provenance model |
| SilverBullet | Programmable without surrendering files | Plain Markdown ownership with end-user programming in a self-hosted web experience | The programming model intimidates, and memory extraction is not central |
| SiYuan | Block-level identity | Fine-grained block references with querying and APIs, cross-platform and self-hosted | Complex data model with limited agent memory governance, and some useful features are paid |
| Slite | Agent maintains human-owned knowledge | Editable team documentation and connected sources answered with citations, plus drift detection under human review | Proprietary cloud with service-managed indexing, and export and retention vary by plan |
| Zep Cloud | Governed context graph as a service | Temporal context graph modelling changing facts and relationship history, with managed APIs and governance | Proprietary managed service with no self-hosted equivalent, and canonical memory is neither human-readable nor directly editable |
| Weaviate | Hybrid search with structured filters | Vector search combined with structured filtering and broad ecosystem integrations | Not a second brain by itself; ingestion, governance, provenance, and interface layers are still required |
| OzBrain | Staged writes into shared knowledge | Explicit promotion and role permissions stop agents silently overwriting a team's knowledge, with plain markdown export at any time | Single-founder proprietary SaaS with no self-hosting and no independent adoption evidence |
| Nia | Retrieval layer in front of coding agents | One API, CLI, and tool server over repositories, documentation sites, PDFs, datasets, and local folders | Only the thin clients are open, while the indexer, vector store, and ranking logic stay closed |
| Dendron | Hierarchy and schemas as structure | Enforceable hierarchical naming with lookup, over ordinary Markdown files that outlive the tool | Development ceased in early 2023, and licence metadata contradicts itself across packages |
| ActivityWatch | Activity telemetry as evidence | Low-overhead passive capture of applications and time | Captures activity metadata rather than meaning |
| AnythingLLM | Low-friction local knowledge | Broad provider and deployment support over personal sources | Knowledge stays inside the application rather than the user's files |
| Memos | Capture friction over ontology | Extremely low friction at the moment of thought | Limited graph and memory intelligence |

### Agent-system research set

| Project | Primary lesson | Strongest contribution | Principal weakness |
|---|---|---|---|
| Codex | Controlled coding agency | Repository-scale execution with approvals, sandboxing, IDE/CLI/SDK, and MCP | Strongest model experience is hosted; output still needs review |
| Claude Code | Provider-native coding agency | Mature permissions, hooks, MCP, subagents, and terminal/IDE/SDK surfaces | Proprietary implementation coupled to Anthropic models |
| Devin | Delegated cloud engineering | Long-running observable workspaces, takeover, parallel sessions, API, and team integrations | Proprietary cloud execution reduces inspectability and sovereignty |
| Cline | Human control | Visible plans, diffs, approval-gated tools, browser use, and MCP | IDE-centered; safety rests heavily on operator attention |
| Aider | Auditable pair programming | Git-native edits and broad provider support with a mature operational history | Narrower tool surface and less autonomy than newer harnesses |
| OpenCode | Provider-neutral coding agent | Model portability, multiple interfaces, and an open extension surface | Rapid interface evolution and configuration-dependent safety |
| OpenHands | Coding-agent operations | Self-hosted agent control, automations, execution backends, and ACP interoperability | Safe deployment depends on deliberate sandbox and backend configuration |
| Pi | Minimal extensible harness | Small reusable agent/model/TUI packages with deep extension seams | No built-in permission boundary; sandboxing is external |
| Goose | Local extensible agent | MCP-native local tools for code and general tasks | Reliability and observability vary with extensions and models |
| Kiro | Spec-driven coding | Reviewable specs, steering files, hooks, powers, IDE/CLI, and MCP | Proprietary AWS service with remote model processing |
| GStack | Coding-agent process | Role separation and artifact handoffs from discovery through release | It is a workflow layer rather than a durable knowledge store or full runtime |
| Browser Use | Browser agency | Rich browser-control primitives and local/cloud execution choices | Websites are nondeterministic and remote browsers complicate privacy |
| GPT Researcher | Research agency | Multi-step web research with cited report artifacts | Source judgment and steering still require a human |
| OpenClaw | Personal agent runtime | Local gateway joins durable sessions, tools, schedules, channels, skills, and devices | Host tools and inbound messages create a broad security boundary |
| Prime Agent | Long-running recursive harness | Persistent IPython, subagents, background sessions, goals, schedules, and reversible refinement | Host execution and self-editing durable state require careful trust controls |
| LangGraph | Durable agent runtime | Checkpoints, resumability, explicit graphs, and human-in-the-loop control | Substantial framework and workflow-design complexity |
| Pydantic AI | Typed agent engineering | Contracts, validation, evaluation, observability, and provider portability | Python-focused; execution policy remains application-owned |
| CrewAI | Multi-agent coordination | Accessible role-based crews plus explicit flows | Extra agents can amplify nondeterminism and debugging cost |
| Microsoft Agent Framework | Workflow convergence | Multi-agent workflows, state, observability, and .NET/Python support | Newer consolidated framework with cloud-oriented integrations |
| smolagents | Minimal agent abstraction | Understandable code-agent design and flexible local/sandbox execution | Persistence, recovery, and policy are mostly left to applications |
| Gajae-Code | Subscription-authenticated agency | Runs on plans the developer already pays for, gates mutation behind an approved plan, and relays questions to chat apps | Self-declared experimental beta with no semantic retrieval |
| oh-my-openagent | Third-party orchestration layer | Adds planning, multi-agent teams, and code navigation inside command lines developers already run | Source-available licence bars commercial redistribution; all releases are prereleases |
| Claw Code | Agent-executed codebase as artifact | Safe-by-default permissions and structured diagnostics across a large agent-built Rust workspace | Maintainers direct users elsewhere for real work; no releases or tags exist |
| DeerFlow | Batteries-included agent runtime | Memory, skills, sandboxes, and schedules ship as one operable system across four interfaces | Gateway administration equals host code execution; run history is not durable by default |
| ECC | Process enforced at tool-call time | Blocking hooks demand facts before edits rather than asking a question the model always answers yes | Most of the catalogue is documentation, and the headline control plane is self-declared alpha |
| ruflo | Advertised coordination versus shipped substrate | A genuinely built memory layer with vector index, encryption at rest, and expiry sweeps | The swarm engine it markets is in neither dependency chain of the published package |
| Orca | Supervising real agent processes | Typed, durable inter-agent messages and a terminal daemon that outlives the application | The orchestration layer is behind an experimental flag and absent from the readme |
| Grok Bot | Persistent named agents as a product | Per-action approvals, review rules where requiring approval wins, and peer handoffs between durable bots | One shared computer per user is explicitly not a security boundary, and no administrator audit trail exists yet |
| oh-my-pi | A fork that outgrew its upstream | Native execution core, debugger and language-server integration, shared sessions | Approval defaults to auto-approving shell execution, with no operating-system sandbox |
| oh-my-claudecode | Orchestrating rival vendors' agents | Supervises six competing coding-agent tools as interchangeable workers | The primary surface runs nothing without the host agent and a paid subscription |
| Codewhale | Authorization written as specification | Nine ordered approval layers, and sandbox docs that name what is not wired | Telemetry defaults to on, and the licence still names the pre-rename identity |
| Reasonix | A whole agent in one static binary | Self-contained cross-compiled distribution with reversible file-snapshot checkpoints | The shipped code lives on a rewrite branch; the repository's named branch is abandoned |
| Crush | Source-available coding agency | Language-server operations as agent tools, and one local backend serving several clients | Licence bars competing commercial use; project config is executed shell |
| Qwen Code | A provider fork that outgrew its upstream | Sandboxed execution, subagents, and a client surface wider than the record it descends from | Surface area outruns evidence, and the headline install pipes a hosted script into a shell |
| Continue | A coding agent that ended deliberately | Removed telemetry, authentication, and hosted coupling before freezing at a final release | Read-only and unmaintained; headless mode allows every tool by default |
| Roo Code | The middle link of a fork chain | Per-task checkpoints and a shim letting one core run outside the editor | Shut down and delisted; its command line auto-approves by default |
| Vane | A bounded research loop, self-hosted | An explicit tool registry with a deliberate finish action, and a plan shown to the reader | No tests or CI beyond an image build; dormant, with advertised providers absent |
| dexter | Domain tools carry the specialisation | Nine implemented finance tools and a shell-parsing permission engine that fails closed | No licence file has ever existed; all data flows through one hard-coded vendor |
| deepagents | A harness that earns its own build target | Its own storage protocol, model harness profiles, and three external benchmarks in CI | Inseparable from the framework beneath it, whose vendor tracing is a hard dependency |
| Skyvern | Where the open core stops | A budgeted, retrying step loop with a failure classifier, plus a workflow builder and self-hosted server | Anti-bot and captcha solving are cloud overrides of open stubs; telemetry defaults on |
| CowAgent | A messaging bridge that became a runtime | Standing named agents with their own workspaces, and delegation recorded as durable runs | Argument-level permissions defaulting to full access; no general test suite in CI |
| nanobot | A runtime that is also a library | Version-controlled layered memory, checkpoint recovery, and four surfaces over one engine | Control governs who may talk, not what may run; isolation is Linux-only |
| CAMEL | A research collective's production orchestrator | Coordinator, snapshot, resume, and runtime worker creation, all tested | Release train stalled at alphas; two files of over six thousand lines |
| Open Design | A workflow that owns its own daemon | Data-contract adapters for many vendor tools, and a tool-loop guard with real failure semantics | Silently modified Apache text; a telemetry channel the toggle cannot disable |
| AionUi | Bundled engine plus supervised vendor workers | Scheduler, mailbox, and crash recovery as machinery; runs without any third-party tool | The core is a binary fetched from a second repository at packaging time |
| mini-SWE-agent | How little harness a coding agent needs | A hundred-line tested loop, confirmation by default, and one design across host, container, and sandbox | The shell is the only tool; the default environment has no isolation |
| SWE-agent | A superseded scaffold kept for reproducibility | Tested library loop, replayable trajectories, container by default | Its own maintainers recommend the successor; no distributed package since 2024 |
| agenticSeek | A fully local general work agent, two surfaces of four | A real browser navigation loop, workspace confinement, local models first | No CI or release ever; execution unconfirmed by default; no scheduler or connectors |
| Omnigent | Competing agents as interchangeable executors | Paired harness modules for a dozen vendors, stacked policies in a non-Turing-complete language, ten sandboxes | Pre-release with a thousand open issues in three months; the routing gate fails open by design |
| Open SWE | A coding agent as deployable infrastructure | Five graphs, delivery to a code host, and a reviewer evaluated against golden comments from real repositories | The production runtime is restricted and licence-keyed; models are a closed list |
| Swarms | Dispatch that is real, marketing that is not | Typed order parsing verified by running the shipped package offline against stub workers | Zero passing test runs in thirty; telemetry on by default carries task text and conversation |
| MagenticLite | A browser agent inside a virtual machine | The strongest isolation boundary reviewed, with approval-by-default and a grant table for folders | The rewrite deleted the planning feature its own description still advertises |
| TaskWeaver | Generated Python over live state | Syntax-tree verification before execution, and a kernel that keeps tables alive across turns | Archived with no notice on the readme or the live documentation site; never packaged |
| emdash | Parallel agent runs as a delivery process | One protocol contract driving thirty-eight vendors' tools, with a test gate that provisions native builds so specs actually run | No model client at all, so it is contingent on other vendors' tools |
| stagewise | A coding agent inside its own browser | The live page's console and structure as environment adapters, and an approval default that cannot drift | The licence appends a carve-out for bundled icons the app depends on |
| BeeAI Framework | Tool-use control declared ahead of the run | Requirement rules validated at construction, and one agent served over three protocols | The headline promises multi-agent; coordination is under five per cent of the package |
| grok-cli | Scheduled unattended runs on one provider | Hooks that block at tool-call time, and reproducible signed binaries | File tools resolve absolute paths with no containment; no workflow runs its tests |
| SeekrFlow | Attribution as a platform surface | Context, training-data, and source attribution map a response back to retrieved chunks and training examples | The advertised cross-session memory has no documented surface; commercial terms are unpublished |
| Claude Agent SDK | Embedding a provider's own loop | The production Claude Code loop with file, shell, web, MCP, subagent, and skill tools behind permissions and hooks | Requires Anthropic models, and the Python wrapper bundles a commercially governed executable |
| DB-GPT | Data analysis as executed steps | Plans and runs multi-step SQL and Python over databases, files, and knowledge bases in sandboxes | Broad platform surface, with approval and rollback behaviour varying across agents and flows |
| DeepSeek Harness | Event-sourced sessions as a primitive | Persistence, replay, forks, and search over an event log, with model, tool, storage, and sandbox layers as plugins | Upstream labels it a developer preview with compatibility-breaking changes |
| DSPy | System behaviour as an optimised program | Declarative signatures plus metric-driven optimisers that tune prompts, examples, and weights | A programming framework rather than an operational platform; optimisation needs datasets and discipline |
| Gemini CLI | An open terminal agent behind a provider | Broad terminal tool surface with MCP and extensions | Centred on Gemini model access; recovery and trace ergonomics still evolving |
| Google Agent Development Kit | Provider framework, replaceable runner | Callbacks, tool confirmation, workflow interruption, pause and resume, and session rewind across five languages | Parity varies by language, and major-version changes to events and session schemas carry migration risk |
| Haystack | Explicit control flow in production | Inspectable pipelines with loops, routing, retrieval, and agent steps, plus tool-call metrics and tracing | Serving, MCP, and enterprise operations rely on adjacent or commercial products |
| LangChain | High-level loop over a durable runtime | Prebuilt agent loop with composable model, tool, middleware, and structured-output seams | Durability lives in the separate LangGraph package; abstraction and release cadence add upgrade overhead |
| LlamaIndex | Retrieval and agents in one framework | Deep ingestion, indexing, retrieval, and document-agent primitives with a very broad integration catalog | Package sprawl to maintain, and parsing, extraction, and hosted agents are separate commercial services |
| Mastra | An integrated TypeScript agent stack | Approval, suspension, resumption, tracing, and replay alongside typed tools, memory, and durable workflows | ee/ paths carry commercial restrictions that the documentation's own licence page contradicts |
| Open Interpreter | Local execution without a sandbox | Simple local code and shell execution across many models | Host execution raises risk, with less structured recovery than sandboxed runtimes |
| OpenAI Agents SDK | A small provider-agnostic core | Tools, handoffs, guardrails, approvals, and resumable runs over pluggable sessions, tracing, and sandboxes | OpenAI models and hosted tools remain the most complete path, and some tracing leaves the boundary |
| Vanna | Identity-aware text-to-SQL | Executes and verifies SQL through tools while carrying user identity and access groups into filtering | Repository archived, leaving maintenance and successor path uncertain |
| WrenAI | Governed context before generation | Semantic modelling, dry-plan validation, row limits, and structured errors make query correctness explicit | Agentic mode, UI, and access controls are commercial, and the semantic layer is upkeep before any analysis |
| Claude Cowork | Delegated knowledge work as a boundary | A delegation flow with isolated cloud execution, explicit tool permissions, and persistent files and sessions | Proprietary with no self-hosted path; tools, limits, and retention vary by plan |
| Perplexity Computer | Research foundation extended to action | Scheduled and always-on work across a large connector catalog spanning research, communications, and files | Managed and largely uninspectable, with connector breadth widening the consequential-action surface |
| Kilo Code | Provider choice with explicit permissions | One coding workflow across IDE and terminal with permission rules, skills, custom agents, and session import and export | Auto-approval and remote-control modes materially expand host and account risk |
| Hermes Agent | Personal agent state made durable | Persistent memory, agent-authored skills, schedules, and profiles over local, container, SSH, or hosted backends | Skill and memory writes are autonomous by default unless approval gates are enabled |
| Replit Agent | Workspace and deployment as one product | Checkpoints that expose diffs and roll back files, agent state, tasks, and optionally the development database | Proprietary runtime tied to the managed service, with usage billing shaping sustained autonomous work |
| Cua | The desktop as agent infrastructure | Computer-use tooling, desktop virtualisation, sandbox lifecycle, and evaluation in one stack reusable from several frameworks | Reliability rests on fragile visual interaction, across a broad credential and privacy surface |
| PRAXIST Beta | Research judged by executable evidence | Executable evaluation and durable artefacts inside the loop, with parallel peers synthesised across generations | Beta with a short operating history, and Fair Source terms restrict distribution and some commercial use |
| Open Grok | A provider agent made inspectable | An open implementation of the Grok Build terminal workflow with interactive, headless, and ACP surfaces | Small fork with little adoption, carrying upstream lineage and unguarded host shell access |
| Warp | The terminal itself as the environment | Terminal, editor, plans, diffs, and review in one local interface that also runs third-party CLI agents | Very broad combined surface, and hosted Oz work shifts the data and execution boundary |
| Higgsfield Supercomputer | A work agent grown from media production | Deep image, video, and audio production alongside persistent files, memory, skills, and schedules | Proprietary and credit-metered, with model routing changing the effective provider boundary |
| AdalFlow | Optimised prompts inside the agent loop | A ReAct runtime with function and MCP tools where prompts are optimisable parameters, tracing included | Agent execution shares design attention with optimisation, on slower repository activity than larger frameworks |
| Strands Agents SDK | Minimal scaffolding, model-driven agents | Short agent definitions with explicit tools and prompts, shipped for Python, TypeScript, and its own MCP server | A repository rename left third-party references stale; persistence and permissions stay application-defined |
| Genkit | Flows inspected during development | A local developer UI for running, inspecting, and evaluating flows across several model backends | Python and Dart trail the JavaScript and Go SDKs, and long-running autonomy is less developed |
| AutoGen | Conversational multi-agent orchestration | Layered core, chat, and extension APIs with real code executors, an MCP workbench, and agents exposed as tools | Superseded and in maintenance mode, with new work directed to Microsoft Agent Framework |
| Semantic Kernel | Plugins and kernel functions across languages | Maintained Python, .NET, and Java trees with typed functions, filters, and OpenAPI and MCP plugins | Declared superseded with no stated support horizon, and defaults remain Azure-first |
| Muse Code | Approval staged at the shell command | An OS-enforced sandbox on by default, compound commands reviewed stage by stage, subagents isolated in per-child worktrees | Beta preview with no public repository, provider-native models, and only cooperative subagent cancellation |
| Microsoft Foundry Agent Service | Customer-owned state under a vendor runtime | Threads, files, and vector stores in the customer's own Azure resources, with a directory identity and sandbox per agent | Fixed quotas documented as unraisable, and memory, skills, and resilience still in preview |
| Amazon Bedrock AgentCore | Tool governance sold as a service | Per-session micro virtual machines and a gateway checking every tool call against declarative policy | The allowlist is not containment: the direct invocation API bypasses it, leaving account permissions as the boundary |
| Gemini Enterprise Agent Platform | Managed memory and an agent registry | Memory Bank with consolidation, immutable revisions, per-identity scoping, and expiry, plus an organisation-wide catalog | The policy gateway does not support the platform's own perimeter controls, and no failure-recovery semantics are documented |
| IBM watsonx Orchestrate | Rival vendors' agents behind one registry | Typed registration of external agents, model neutrality across fourteen providers, and documented air-gapped operation | The routing runtime is proprietary, and the vendor's own known-issues list records failed handoffs and misordered tool calls |
| Dify | A canvas that grew an agent runtime | A human-input node making approval a workflow step, with applications exported as versioned declarative files | A modified Apache licence forbidding multi-tenant operation, over a runtime its own docs decline to call a security boundary |
| Langflow | Visual authoring, portable output | Flows versioned as normalised JSON and run by a separate stateless executor outside the builder | No documented approval or pause primitive, and one output mode silently skips events and history writes |
| Botpress Cloud | Escalation as the product | Human handoff as a first-class surface with an escalation agent, an operator console, and SDK interfaces | No self-hosting or residency option, and export is a proprietary archive that omits knowledge files |
| Flowise | A builder wound down deliberately | Mostly Apache-2.0 with in-repository carve-outs and maintainers explicitly encouraging forks | Archived with packages deprecated and no successor or endorsed fork named |
| Microsoft Copilot Studio | A low-code builder as orchestrator | Human escalation as a construct with a transfer step and routing contract, plus child and cross-tenant agent coordination | Vendor-operated with definitions in the vendor's data platform, and transcripts retained only briefly |
| Salesforce Agentforce | Agents as deployable org metadata | Declarative definitions versioned through ordinary release tooling, with actions reusing existing org logic directly | Vendor-operated inside the customer org, and the brand covers several separately sold products |
| Meta Business Agent Platform | Reach defined by the channel | Handoff between agent and the business's own application as a documented capability, with actions through its interfaces and webhooks | One messaging channel on eligible numbers, with several regulated categories excluded outright |
| Exo Harness | Self-modification over an immutable log | A substrate and semantics split keeping event log, sandbox, and secrets immutable while prompts, tools, and model choice stay agent-editable | Early-stage with no releases, and no human approval gate before the agent rebuilds its own runtime |
| Vibe Kanban | A task board over parallel agents | Blocking tool approvals as real machinery, with nine vendors' tools supervised in separate working trees | Archived after the company's closure, and its final change retired the board it is named for |
| Agno | Runtime operations inside the framework | One path from definitions through API runtime, interface, storage, traces, and deployment | A broad combined surface increases conceptual and upgrade complexity |
| BabyAGI | The loop the wave was built on | The create, prioritise, execute, and store cycle that later frameworks extended or set out to fix | Archived, with no approval step, budget cap, or stopping condition, over unvalidated text parsing |

### Assistant-system research set

| Product | Primary lesson | Strongest contribution | Principal weakness |
|---|---|---|---|
| ChatGPT | Mature general assistant | Broad tools, projects, memory, research, and cross-device product maturity | Provider-native service with limited portability and inspectability |
| Claude | Controlled contextual collaboration | Explicit projects, editable memory, citations, connected research, and context import/export | Features and limits vary by plan; all operational state remains service-managed |
| Gemini Apps | Ecosystem-integrated assistance | Deep Research, Google-connected context, Gems, personalization, and scheduled or multi-step work | Consumer, Workspace, and experimental product boundaries are easy to conflate |
| Microsoft Copilot | Ubiquitous personal companion | Reach across standalone apps, Windows, mobile, web, and Microsoft surfaces | The Copilot brand spans many separately governed products and capability sets |
| DeepSeek | Open-model/closed-product boundary | Accessible reasoning, search, files, and synchronized web/mobile chat | Hosted product governance and continuity controls are comparatively limited |
| Z.ai | Regional provider coverage | GLM-native chat, file input, deliberate reasoning, and an integrated coding entry point | Thin public product documentation and limited evidence for durable context or portability |
| Grok | Real-time provider-native assistance | Current-information retrieval, research, files, creation, and optional agentic actions | Overlapping consumer, X, bot, API, and model boundaries require careful separation |
| Amazon Quick | Governed enterprise assistance | Organizational knowledge, analytics, actions, custom agents, and AWS administration | Broad evolving bundle with proprietary cloud operation |
| Microsoft 365 Copilot | Tenant-grounded work assistance | Microsoft 365 context, enterprise search, notebooks, applications, agents, and governance | Licensing and capability surfaces vary across plans, tenant configuration, and the wider Copilot brand |
| T3 Chat | Multi-provider workspace | First-class model choice in a simple persistent client | Narrower tools, governance, and autonomous workflows than major provider assistants |
| LibreChat | Operator-held multi-model workspace | Hash-chained audit log, group and role permissions, and its own agents re-exposed as a model endpoint | Manifests declare a different licence from the repository; memory entries overwrite with no history |

GroqChat remains provisional. Groq's current first-party agreement establishes a governed cloud service with access to multiple model services, but the public product material does not yet establish the durable workspace or boundary from Groq Playground and GroqCloud required for the multi-model chat-client role.
| Perplexity | Answers carrying visible sources | Search-grounded answers and iterative research with citations, over sessions, editable memory, and projects | Proprietary service whose retrieval stack and stored context are opaque, with capabilities varying by plan and platform |
| Venice.ai | Privacy declared per model mode | A cross-provider, cross-modal model catalogue in one workspace with device-held history | Privacy properties differ materially by model mode and upstream routing, and a proprietary service still mediates inference |
| Mistral Vibe | European governance boundary | Enterprise on-premises, private-cloud, and residency-bound deployment for an end-user assistant | Training on user data is opt-out rather than opt-in, and durable project and memory behaviour is thinly documented |
| Meta AI | Distribution ahead of data boundary | One assistant spanning text, voice, live vision, files, research, and images across Meta's apps, web, desktop, and glasses | Terms allow interactions to personalise ads and be reviewed, and deleting messages may not delete Meta's copy |

### Inference-service research set

This set records managed services that accept model-inference requests, reviewed against a dedicated operational profile rather than the family profiles above. The lesson is not architectural: these records teach residency, retention, delivery mode, routing behaviour, and where one service boundary stops and an adjacent product begins.

| Service | Primary lesson | Strongest contribution | Principal weakness |
|---|---|---|---|
| OpenAI API | Data controls behind eligibility | Multi-modal first-party platform with project administration, batch, and documented regional endpoints | Zero retention and regional processing require approval and do not cover every endpoint |
| Anthropic API | A deliberately narrow contract | Messages, streaming, prompt caching, files, and batches with separate commercial retention guidance | Claude families only; zero retention is approval-based with feature-specific exceptions |
| Amazon Bedrock | Inference inside a cloud account | One control plane over several publishers, with regional identity boundaries, inference profiles, and provisioned throughput | Availability and compatibility vary by model, feature, and region; publisher terms stack on the cloud agreement |
| Vertex AI Generative AI | A model garden as platform | First-party, partner, open, and customer-deployed paths under one governance surface | Serving mode, region, and operational responsibility differ so widely that no capability generalises |
| OpenRouter | Retention as a routing input | Fallback, cost, latency, and data-policy constraints in one compatible API, with endpoint-level retention metadata | An added service boundary; effective terms depend on the upstream endpoint chosen |
| GroqCloud | Production and preview separated | Documented lifecycle tiers, token-speed signals, and distinct on-demand, flexible, and batch paths | Selective catalogue where preview models may be discontinued at short notice; narrow regional choice |
| Gemini API | Two doors to one family | Direct generation, embeddings, media, tools, caching, files, and batch, native or compatible | A different governance boundary from the cloud platform; behaviour varies across free, paid, and stateful features |
| xAI API | Retention traded against features | Direct model access with operator-run web and social search tools, and self-service zero retention | Zero retention disables several stateful features; the catalogue is operator-run only |
| Mistral AI Studio | A European direct platform | Proprietary and open families alongside tuning, document recognition, audio, and agent services | Storage behaviour differs across features; partner-hosted and customer-infrastructure paths fall outside the boundary |
| Cohere API | Retrieval as a first-party surface | Generation, embedding, reranking, and classification on one enterprise-oriented platform | Commitments differ across trial, production, private, and partner paths; the catalogue is operator-centred |
| DeepSeek API | Compatible access, thin controls | First-party chat and reasoning models with documented versions, limits, and isolation behaviour | Regional and retention controls less explicit than cloud platforms; own model families only |
| Moonshot AI Open Platform | Direct access as the boundary | Models reached through widely used compatible conventions | Regional controls are not publicly exposed; the same models through routers are a separate boundary |
| Z.ai Model API | One family across modalities | Text, reasoning, coding, vision, image, and video through native or compatible interfaces | Regional and retention controls need contract-level review; operator-run models only |
| AI21 Studio | A narrow first-party catalogue | A focused model family with official compatibility guidance for common client conventions | Narrower than multi-publisher clouds, and public residency detail is limited |
| MiniMax Open Platform | Generative media beyond text | Language, speech, image, music, and video interfaces on one direct platform | Media interfaces raise capability-specific storage and rights questions; regional documentation is limited |
| Perplexity API | Inference bundled with retrieval | Search-grounded answers with citations, plus compatibility endpoints for common patterns | Grounded requests widen the data and source boundary; regional controls are not prominent |
| Alibaba Cloud Model Studio | Region decides the catalogue | First-party and third-party models under region-scoped controls, with several compatible protocols | Model and feature availability vary substantially by region; broad scope needs per-model review |
| Baidu Qianfan ModelBuilder | Build and serve in one place | First-party models with third-party and open ones, tuning, evaluation, and deployed serving | Mainland and international boundaries differ; the detailed operational documentation is in one language |
| BytePlus ModelArk | Endpoints named per geography | Explicit regional endpoints over first-party and selected third-party models | European coverage is narrower than Asian, and spillover behaviour must be checked for residency-sensitive work |
| Microsoft Foundry Models | A catalogue joined to identity | Multi-publisher models integrated with cloud identity, networking, and geography controls | Terms and processing geography vary by deployment type; the product boundary and naming shift quickly |
| OCI Generative AI | Dedicated capacity as a tier | On-demand and dedicated clusters beside imported models and tuning, with stated stateless-inference handling | Model and capacity support vary by region; dedicated clusters need commitments and limit planning |
| Databricks Foundation Model APIs | Inference under data governance | Catalogue governance across pay-per-token, priority, provisioned, and batch modes | Catalogues differ by cloud and region; external model routing adds separate provider terms |
| IBM watsonx.ai Runtime | Hosted and customer models together | First-party, third-party, open, and customer models inside vendor deployment spaces | Region and plan decide feature support; the surface spans several resource types rather than one endpoint |
| Cloudflare Workers AI | Inference beside application code | Serverless invocation from edge functions, with no endpoint capacity to manage for supported models | A curated catalogue, and distributed execution is not the same as fixed-region residency |
| Tencent Cloud TokenHub | Model selection made automatic | First-party models with several third-party families, plus documented regional endpoints and selection modes | Third-party publisher terms apply downstream; predecessor interfaces are mid-migration |
| NVIDIA API Catalog | Evaluation before self-hosting | A broad multimodal catalogue whose packaging carries over to controlled deployments | Hosted catalogue and self-hosted packaging are different boundaries; access is evaluation-oriented |
| Hugging Face Inference Providers | One token, many operators | Documented fastest, cheapest, preferred, and provider-specific selection policies | A proxy boundary in front of upstreams whose residency, retention, and availability stay their own |
| Hugging Face Inference Endpoints | Hub artifacts become deployments | Dedicated autoscaling deployments with explicit cloud, region, hardware, access, and private-network choices | More deployment and compatibility responsibility than a serverless catalogue; regions differ by cloud |
| Together AI | Serverless and reserved together | A broad open-model catalogue on one compatible interface, with dedicated production capacity | Catalogue and capacity guarantees differ between the modes; model terms remain publisher-specific |
| Fireworks AI | Capacity modes for workload shape | Serverless, on-demand, reserved, batch, and tuning behind compatible interfaces | Production controls follow the deployment tier and contract; lifecycle stays model-specific |
| Cerebras Inference | Distinct silicon sold as service | High-throughput hosted inference with shared, batch, priority, and dedicated paths documented | The shared production catalogue is selective; regional placement needs enterprise coordination |
| SambaNova Cloud | Alternative accelerator, familiar interface | Compatible access to a hosted catalogue on the operator's own dataflow hardware | Catalogue and public regional controls are limited; private deployment is a separate commercial arrangement |
| DeepInfra | Task interfaces beyond chat | Text, embeddings, reranking, image, and audio models on serverless or dedicated capacity | Catalogue lifecycle follows upstream availability; enterprise controls need plan-specific confirmation |
| Replicate | Packaging as the product | One prediction lifecycle over a large cross-modal catalogue, with custom packaging as a first-class path | Public models differ in ownership, terms, and upkeep; prediction artefacts need explicit retention review |
| Venice API | Privacy labelled per model | Trusted-execution and end-to-end encryption options with per-model privacy labels that expose boundary differences | Private and anonymised modes have different upstreams; the changing catalogue needs model-level review |
| Stability AI Developer Platform | A single-modality first-party interface | Deep image generation, control, editing, and upscale operations in synchronous and asynchronous workflows | Creative media only; regional and retention controls are less explicit than the feature documentation |
| Ollama Cloud | The local interface pointed elsewhere | The same client, command line, and interface as local execution, with contractual zero retention required of hosting partners | Geography is described rather than selected; on-demand only, curated catalogue, no customer-supplied models |
| Nebius Token Factory | Residency pinned per endpoint | Dedicated endpoints pinned to one region with no multi-region overflow, plus customer weights and adapter lifecycle | Open-weight catalogue only; shared endpoints are not customer-placed, and a recent rename dates third-party guides |
| Baseten | Customer models at the centre | Hosted interfaces and dedicated deployments across clouds and regions, with ownership and destruction stated in the terms | Region availability follows the hosting option; not a route to third-party proprietary frontier models |
| Meta Model API | Training bound to a model identifier | Three interchangeable request formats over one pipeline, plus an unauthenticated per-model status endpoint | Limited-preview terms, one capacity path, a restriction list instead of regional choice, and no single sign-on or audit logging |
| Requesty | Regional routing on every plan | A European routing endpoint at all tiers, with training-permitted models labelled and offered at no cost | The governing terms render only in a browser, and the operator's own model and provider counts disagree |
| Eden AI | Routing across task categories | One contract and request format reaching document, speech, translation, and vision vendors as well as language | The effective data boundary changes with the vendor reached; counts disagree and key-supplied fees are undocumented |
| AI/ML API | A reseller setting its rates | One compatible endpoint over a very broad multimodal catalogue, with per-request spend in response headers | Rates sit above other aggregators with no stated relation to upstream; undated terms and no position on training |
| Nano-GPT | Access without an identity | Accountless, cryptocurrency-payable routing with generated identifiers across about a thousand models | Retention rests on upstream claims by the operator's own admission; the operating company is impersonal |
| Vercel AI Gateway | Routing at provider list price | Stated absence of markup, budgets scoped to team, project, key, or member, and open telemetry drains | A failed customer key falls back to billed platform credentials; some controls are billed per request |
| Cloudflare AI Gateway | A proxy outside the billing path | Free caching, rate limiting, analytics, and data-loss prevention, with customer-supplied provider credentials | No single compatible endpoint across providers could be confirmed; log retention and forwarding need a paid plan |
| Martian Gateway | Optimisation sold beside routing | Inference-time optimisation with a stated quality commitment, and a keyless public model listing | The terms license training on customer inputs, the cited pricing page does not exist, and the product boundary is unclear |
| Chutes | Capacity from a decentralised network | Confidential compute marked on every catalogue model, and a permissively licensed platform core | Open-weight only; no named legal entity, and a pricing path returns a not-found body under a success status |
| TrustedRouter | A privacy claim you can check | Live attestation binding a published image digest to the connection certificate, with signed artifacts in a transparency log | Source-available with no production grant, no third-party audit, coverage below what is advertised, one named individual behind it |
| Volcengine Ark | Exceptions enumerated, not summarised | Dated terms with a no-storage, no-training default and each exception listed, across four serving modes | Licensed for one jurisdiction with one documented region; individual plans authorise training with no withdrawal |
| Zhipu AI Open Platform | Deletion specified concretely | A buffer period followed by irreversible removal of cached and backup copies, with jurisdiction-bound personal data | Anonymised data may train models without separate consent; one endpoint, no region choice, an undated agreement |
| SiliconFlow (mainland China) | Two platforms, neither a superset | Terms against accessing or storing interaction data, with serverless, reserved, dedicated, and private deployment | A separate entity from the international platform; no first-party models, no region parameter, capped batch jobs |
| SiliconFlow (international) | Same brand, different governing terms | Terms against accessing interaction data and against customer training on service content | Older terms, missing batch and tuning documentation, deletion at the operator's discretion, mainland users excluded |
| Qiniu AI Large-Model Inference | A service agreement for routing | A published compensation schedule, per-component status page, and mirrored regional endpoints | Retention undocumented and the referenced data policy unpublished; the entry endpoint does not fix the processing region |
| StepFun Open Platform (mainland China) | Limits published as numbers | Concurrency, request, and token limits across six spend tiers, and a dated deprecation map to replacements | No service agreement, status page, or changelog; one capacity path, and a routing alias fronts an unlisted third-party model |
| StepFun Open Platform (international) | Compliance paperwork, harsher terms | A data-processing agreement with standard contractual clauses that the sibling platform does not publish | A perpetual licence over prompts, outputs, and logs with training permitted and no opt-out; thinner documentation |
| abliteration.ai | Modified weights as the service | Per-endpoint retention with named exceptions, a compatibility matrix marking gaps, and numeric limits at three scopes | Product pages promise zero retention the terms do not; one synchronous host, no agreements or audits, undisclosed base model |

### Local-runtime research set

This set records self-operated software that executes model inference on hardware the operator controls, with no third party holding the serving contract. The lesson is about execution rather than architecture: accelerator and model-format coverage, serving and concurrency behaviour, model lifecycle, and the deployment shape each runtime assumes.

| Runtime | Primary lesson | Strongest contribution | Principal weakness |
|---|---|---|---|
| GenieX | One vendor's accelerator, two paths | Community weights on the neural processor, graphics processor, or central processor, beside pre-compiled vendor bundles, with limits named exactly | One chip family only; interactive serving with no batching and no server authentication; a developer preview |
| llama.cpp | The layer others build on | The broadest backend coverage in the collection, with continuous batching, parallel slots, metrics, and distributed execution | One weight format, no managed model store, and capability varies by build with backends still in progress |
| LM Studio | Engine selection made automatic | Several engines chosen automatically, integrated model management, and a headless mode | A proprietary core with only the command line and libraries open; two weight formats only; no metrics endpoint |
| LocalAI | Many interface contracts, one endpoint | Three different request conventions over text, embeddings, images, and audio, with a gallery and distributed deployment | Behaviour follows whichever backend loads the model; visibility centres on the web interface |
| MLC LLM | Compilation as the deployment step | One compiler and engine reaching desktop graphics processors, phones, and browsers, with sharding, prefix caching, and speculative decoding | Nothing runs unconverted; a narrow architecture list; no metrics, authentication, or container guidance |
| MLX LM | A first-party path for one platform | Generation, quantisation, upload, and fine-tuning in one package, with model-hub integration | One hardware platform only; the bundled server is documented as unsuitable for production; minimal serving surface |
| Ollama | Model lifecycle as the product | Pull, push, create, copy, import, and delete through command line and interface, with compatible endpoints and per-platform hardware floors | Parallel slots rather than continuous batching; gaps in the accelerator support matrix; no metrics endpoint |
| ONNX Runtime GenAI | A generation loop as library | One loop across several accelerators from five language bindings, with constrained decoding and multiple adapters | One graph format only; no server, authentication, or metrics; the batching engine is transitional and not thread-safe |
| OpenVINO Model Server | One vendor's silicon, many protocols | Four different serving conventions over two transports, with paged attention and mature operations | One vendor's targets only; generative models normally require conversion; endpoints ship without access control or encryption |
| SGLang | Prefix reuse as scheduling | Radix-tree prefix caching, continuous batching, and three parallelism dimensions, reaching several accelerator families | Server deployment only, thinner consumer-hardware guidance, and no local model lifecycle management |
| TensorFlow Serving | Version handling done properly | Version policies, labels for staged rollout and rollback, a tunable global batch scheduler, and wire formats others reimplement | One graph format, a tensor-shaped interface with no language-model surface, narrow platform support, dated documentation |
| TensorRT LLM | Two backends under one interface | In-flight batching, paged attention cache, split prefill and decode, and per-model quantisation tables behind one library | One vendor and platform only; a capability confirmed on one backend is not evidence for the other; headline features labelled beta |
| vLLM | Serving capability as the point | Continuous batching, chunked prefill, prefix caching, five parallelism dimensions, and three interface conventions | No desktop packaging; targets beyond two vendors depend on plugins; weights are fixed at launch |
| Xinference | A cluster of interchangeable backends | Supervisor and workers with replica placement, distributed inference, shared cache, default authentication, and federated identity | Behaviour varies by backend; some accelerator support needs the commercial edition; a heavy dependency surface |

## Architectural synthesis for local-first memory

A local-first memory implementation can adopt:

- **from Logseq and Basic Memory:** human-readable knowledge and links;
- **from Trilium:** revisions, structured metadata, automation, and scale discipline;
- **from AppFlowy and Memos:** polished structure plus extremely low capture friction;
- **from Khoj and AnythingLLM:** broad model/source interoperability and useful local AI workflows;
- **from Mem0:** explicit user/session/agent scopes and multi-signal retrieval;
- **from Graphiti:** episodes, provenance, temporal validity, and supersession;
- **from Letta Code:** bounded context, durable identity, skills, scheduled consolidation, and Git-visible changes;
- **from GStack:** role-specialized workflow and evidence-based completion;
- **from OpenRecall and ActivityWatch:** optional ambient evidence streams, never enabled by default.

This research argues against:

- vectors as the only canonical representation;
- untraceable memory extraction;
- silent UPDATE/DELETE of facts;
- mandatory cloud storage;
- unrestricted passive capture;
- agent self-modification without auditability;
- MCP as the only integration path;
- popularity as a substitute for architectural fit.

## Directory policy

The catalog is organized by operational relevance, not by license eligibility. Open-source, open-core, source-available, and proprietary systems can all qualify when their behavior can be reviewed responsibly. Licensing remains prominent, scoped evidence: users can filter by exact license or source model, and inspectability influences confidence and relevant score dimensions. An open-source-only directory is therefore a view over the catalog rather than its boundary. See [ADR 007](adr/007-licenses-are-classification-not-inclusion.md).

License-only exclusions were returned to the review queue. True exclusions now express a role or evidence boundary—for example, a tracing client that does not itself act as an agent—not a preference for one software-distribution model.
| Jan | The open desktop counterpart to hosted chat | Threads, assistants, and models as readable local files, re-exposed through a local compatible server | The licence file is a notice rather than the text, and package manifests disagree with each other |
