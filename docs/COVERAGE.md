# Coverage

Use this document to choose research batches. The canonical project and candidate records remain the source of truth; this is a dated editorial snapshot, not another queue.

## What comprehensive means

AI Systems Atlas aims to be comprehensive within its declared operational scope, not an indiscriminate list of every model, API, prompt wrapper, or branded feature. Coverage is healthy when a user can compare materially different approaches within a supported role, understand source-model and licensing tradeoffs, and see important systems that failed review with a concrete reason.

The operational-system universe is deployable or directly usable memory, agent, and assistant systems. Managed inference services, self-operated local runtimes, and provider-independent model releases each form a separately scored collection with its own rubric; specifications remain separate and unscored. Plain inference clients and observability-only services remain adjacent components. A model repository is evidence for a model-release record under ADR 025, not an operational system by itself.

Measure coverage across three axes:

1. **Operational roles:** enough credible alternatives to expose real architectural choices.
2. **Source models:** open-source, mixed-source, open-core, source-available, and proprietary approaches where they materially exist.
3. **Ecosystem significance:** mature reference systems, important new designs, and provider-native approaches—not popularity alone.

Do not add a new family merely to fit a famous product. Add one only when its primary operational outcome cannot be scored coherently by an existing family.

## Snapshot — 2026-09-12

The reviewed catalog contains 197 systems: fifty-five memory systems, one hundred and twenty-five agent systems, and seventeen assistant systems. Nine records are archived and three are superseded, so 185 satisfy active-choice coverage. The system provisional queue contains forty-three records: twenty-one are actionable and twenty-two are held, thirteen by the robotics scope decision recorded in `BACKLOG.md` and nine by the 2026-09-12 promotion review. Eighty-eight candidates have been excluded with reasons, every one re-verified against current sources on 2026-09-17. The separate collections contain twenty-two unscored specifications, fifty-nine scored inference services, sixteen scored local runtimes, and fifty-three scored model releases; the models.dev queue contains 270 further text-output model candidates awaiting complete review. Every count in this section and the numeric columns of the role table below were recomputed from the canonical files on this date; the coverage-signal prose is older than the counts except where a batch note says otherwise.

### Local runtimes

The local-runtime collection covers self-operated inference software under [ADR 015](adr/015-local-runtimes-are-self-operated-execution-records.md). Its sixteen records span three desktop runners, six server engines, five embedded libraries, and two compatibility gateways, across thirteen open-source projects, two mixed-source products, and one open-core engine.

| Runtime type | Reviewed | Coverage signal |
|---|---:|---|
| Desktop runner | 3 | Adequate across an open runner, a proprietary application, and an on-device NPU product |
| Server engine | 6 | Broad across accelerator-backed serving, an Intel and NPU path, a first-party NVIDIA engine, and a versioned classical-machine-learning server |
| Embedded library | 5 | Adequate across GGUF, Apple silicon, portable ONNX, and compiled mobile and browser targets |
| Compatibility gateway | 2 | Improved; both reviewed gateways are open source, so a proprietary or managed self-hosted gateway remains unreviewed |

Execution now reaches past the desktop and the single server: phones, embedded boards, and the browser are represented, alongside NPU, OpenCL, WebGPU, DirectML, and ONNX paths.

Accelerator coverage now spans CPU, CUDA, ROCm, Metal, Vulkan, SYCL, NPU, and DirectML paths, and format coverage includes ONNX alongside GGUF, safetensors, MLX, and the quantized schemes. Coverage is representative of material execution choices rather than exhaustive. Text Generation Inference was screened and excluded because its repository is archived. Hugging Face Transformers was screened and excluded as substrate: it is the model-definition framework the published engines load their model implementations from, and its own documentation sends production serving to vLLM or SGLang with a Transformers model as the backend, so `transformers serve` does not make it the dedicated serving product ADR 015 admits on separate terms. Jan was screened and routed to the candidate queue as an assistant, because conversations, projects, assistants, agents, and connectors place it on the assistant side of the ADR 015 runtime test.

### Agent packs

The Agent packs collection under [ADR 032](adr/032-agent-packs-are-unscored-records-of-what-a-host-installs.md) opens with four records: claude-code-tresor as a process kit, agent-toolkit and Build with Claude as marketplaces, and Second Brain Starter as a vault bundle. Two of the six repositories reviewed for the collection failed its boundary test 4 from their trees and were held for a scored review under [ADR 031](adr/031-skill-packs-earn-records-by-owned-state-or-enforced-work.md); both reviews are now done. Superpowers is published as a scored coding-agent workflow on `skills/brainstorming/scripts/server.cjs`, the companion HTTP and WebSocket server its brainstorming skill starts, which serves screens, opens the user's browser, and collects the reader's selections. obsidian-claude-pkm is excluded: its four installed shell scripts commit, print, and count, no shipped script returns a blocking result, and its `permissions.deny` list is host configuration rather than a deny path in shipped code. `NVIDIA/skills` and `AI-Research-SKILLs` stay excluded as mirrors, and `knowledge-garden` stays excluded as a personal snapshot. A fifth record, Second Brain LLM Wiki, came from the 2026-09-17 exclusion re-review rather than from the queue: it had been excluded under ADR 031 two days before this collection existed, and its tree — four skill documents, six reference documents, and one idempotent scaffolding script — passes all four boundary tests as a skills bundle. Coverage here is opened, not surveyed; the class is admitted from the queue on the same significance judgement as any other. Scored systems that install as packs carry the `host_pack` deployment mode and are listed in the Packs scope beside the collection; nine records carry it as of 2026-09-18.

### Systems by role

| Role | Reviewed | Active | Coverage signal |
|---|---:|---:|---|
| General work agent | 7 | 7 | Improved across research-first, office-work, and media-production approaches; keep reviewing control, recovery, permission, and execution boundaries |
| Agent framework / SDK | 29 | 25 | Broad across code frameworks and visual builders after ADR 019 widened the definition; two Microsoft predecessors are superseded and one builder is archived |
| Coding agent | 38 | 35 | Broad across local, self-hosted, managed-cloud, terminal-native, and community-fork operation, and the three highest-profile proprietary editors are now reviewed rather than queued |
| Human-first PKM | 11 | 10 | Broad, but proprietary reference products remain provisional |
| AI knowledge app / RAG brain | 10 | 9 | Improved with a proprietary self-maintaining knowledge product; review open-core alternatives |
| Retrieval infrastructure | 4 | 4 | Adequate baseline |
| Data-analysis / text-to-SQL agent | 4 | 2 | Improved; add distinct governed and enterprise approaches |
| Agent memory service | 18 | 18 | Improved with separate open-engine and managed-service boundaries; compare ownership, lifecycle, governance, and retrieval intelligence |
| Ambient capture | 3 | 3 | Thin; source-model diversity is missing |
| Context graph engine | 5 | 5 | Thin |
| Multi-agent orchestrator | 10 | 10 | Improved with a vendor-operated registry platform and a low-code service that escalates to people; open orchestrators remain thin |
| Stateful agent runtime | 18 | 18 | Broad after three vendor-operated platforms joined the self-operated runtimes; compare who holds the operating contract alongside persistence and execution policy |
| Browser/computer agent | 5 | 5 | Improved open baseline; desktop reliability and sandbox boundaries still need broader comparison |
| Coding-agent workflow | 10 | 9 | Improved with a third-party orchestration plugin and an agent-built demonstration; compare process and delivery discipline |
| Human–agent memory bridge | 4 | 4 | Improved from a single record to four, all built on a user-owned Markdown or Obsidian vault shared across agent hosts; a bridge over a non-file workspace remains unreviewed |
| Research agent | 6 | 6 | Now spans open, source-available, and hosted closed-core approaches, but the hosted entry's discovery claims are vendor-reported, and broader production evidence remains thin |
| General AI assistant | 11 | 11 | Representative provider baseline now including European-governed and Meta products; add others only when their workspace, governance, or regional ecosystem is materially distinct |
| Enterprise work assistant | 2 | 2 | Improved Microsoft/AWS baseline; broader enterprise SaaS diversity remains a priority |
| Multi-model chat client | 4 | 4 | Improved across conventional and privacy-oriented hosted approaches; open-client boundaries remain thin |

Archived and superseded systems remain reviewed historical references but do not satisfy active-choice coverage. A superseded record names its successor, so a reader who arrives at a predecessor is pointed at the system that replaced it.

## Catalog sources for coverage audits

`directory/discovery-sources.json` holds authoritative announcement feeds that automation polls for launches. It is not the right home for a catalog, because its schema requires a feed and the updater treats every entry as a stream of dated items.

For checking coverage rather than detecting launches, use [models.dev](https://models.dev/api.json), a community-maintained database of inference providers and models published under an open license from [anomalyco/models.dev](https://github.com/anomalyco/models.dev). A pass on 2026-08-31 found 212 provider entries against 49 reviewed services. That difference overstates the gap: the catalog keys on API endpoints, so one operator appears several times across regional and subscription variants, and Alibaba alone accounts for six entries. Collapse those to service boundaries before treating an entry as a candidate, and treat the catalog as aggregated third-party data that locates candidates rather than as evidence, since every record still needs first-party terms and documentation.

### High-adoption discovery sweep

A 2026-08-31 sweep looked for widely adopted open-source systems the catalog does not hold, prompted by a question about forks and derivatives. Two findings shaped the method.

**Walking fork graphs does not work.** The seventy-three published repositories carry more than half a million forks between them, dominated by personal copies: sorting one popular project's forks by stars returned repositories with 584, 70, and 28 stars. More decisively, the derivative that prompted the question is not a fork in the platform's sense at all, so no amount of graph walking would reach it. Lineage is a review-time boundary question, not a discovery mechanism.

**Adoption finds candidates; the existing rules filter them.** Twenty banded searches keyed to the role vocabulary returned 327 repositories, 279 of them new, active, and not forks. Star count sorts attention but decides nothing: the highest-ranked results included a methodology, several skill libraries, an unrelated automation platform, a database, and an agent-managed museum exhibit. `CURATION.md` already excludes prompt templates and non-operational research inputs, and applying that plus the tool-and-plugin boundary removed the noise. Thirty survivors were queued.

Band the searches. A first pass with an unbanded top-thirty cut silently dropped a twenty-eight-thousand-star coding agent because larger repositories crowded the result window.

### Chinese-operator coverage

A 2026-08-31 pass compared the catalog against models.dev for Chinese operators. Thirty-five catalog entries collapsed to about thirteen operators once regional and subscription variants were folded in, confirming that the catalog keys on endpoints rather than on services.

Two boundary rules were already settled and held. A domestic platform operated by a different legal entity from its international sibling is a separate record: Volcengine Ark's own terms licence it for use only within mainland China, Zhipu's mainland platform and Z.ai are separate companies in separate jurisdictions, and SiliconFlow's international terms exclude users located in mainland China outright. Subscription tiers are not separate records, which the existing BytePlus boundary already stated. Region-scoped endpoints of one platform remain a trait rather than a record, as Alibaba Cloud Model Studio shows.

Remaining unreviewed operators from that comparison include Xiaomi, Bailing, and the long tail of aggregators that resell other aggregators, which needs a boundary rule of its own before any of them is reviewable.

## Research batches

Choose small batches with one coherent boundary question:

1. **Assistant boundary follow-up:** keep GroqChat provisional until first-party evidence establishes a durable end-user workspace distinct from Groq Playground and GroqCloud. Preserve consumer, enterprise, playground, model, API, and agent-mode boundaries rather than comparing transient model benchmarks.
2. **Managed agent platforms:** closed. Under [ADR 018](adr/018-operating-party-is-a-trait-not-a-role.md) the batch was dissolved and routed per record, and the last two are now reviewed. Salesforce Agentforce and the Meta Business Agent Platform are agent frameworks rather than orchestrators: Agentforce's subagents are categories of actions inside one agent rather than peers it delegates to, and Meta's platform coordinates nothing. Foundry Agent Service, Bedrock AgentCore, and the Gemini Enterprise Agent Platform are stateful agent runtimes; watsonx Orchestrate is a multi-agent orchestrator; Copilot Studio joined batch 10.
3. **Coding-agent second pass:** closed for the editor question. Cursor, Google Antigravity, and Windsurf — the last published under its current name, Devin Desktop — are now reviewed, alongside Roo Code and SWE-agent from earlier batches. GitHub Copilot coding agent, Jules, and T3 Code remain, and each is a cloud-delegation or workflow boundary rather than an editor one.
4. **Proprietary memory and knowledge:** NotebookLM, Microsoft Recall, Limitless, and other products that provide a materially different ownership or governance boundary. Zep Cloud and Slite now establish managed agent-memory and self-maintaining knowledge baselines.
5. **Source-model diversity:** screenpipe, AFFiNE, Onyx, and Obsidian. Review product terms and operational evidence without treating license as eligibility.
6. **Thin operational roles:** browser/computer use, research, general work, coding workflows, context graphs, ambient capture, and human–agent bridges. Keep Pletor and Sylph provisional until their license or product-terms evidence meets the full curation standard.
7. **Specification follow-up:** identity and discovery now have OASF and ANP baselines; browser-native tools and agentic transactions now have WebMCP, AP2, UCP, and Commerce ACP baselines. Next evaluate cross-protocol authentication/authorization profiles, workflow-state exchange beyond task messaging, and conformance evidence without absorbing generic web standards.
8. **Instruction-convention follow-up:** evaluate Amazon Q rules, Kiro steering, and JetBrains AI Assistant rules as one bounded batch. Treat workflows, custom modes, and product configuration as separate boundaries rather than stretching the instruction-convention category.
9. **Superseded predecessors:** resolved by [ADR 016](adr/016-superseded-predecessors-keep-their-record.md), which adds the `superseded` status and a validated `superseded_by` link. AutoGen and Semantic Kernel are published under it. Apply the same treatment when a maintainer publishes a succession and the successor is already reviewed.
10. **Low-code agent builders:** done under [ADR 019](adr/019-authoring-surface-is-a-trait-not-a-role.md). Dify, Langflow, and Botpress Cloud are published as agent frameworks, Flowise as an archived one after its maintainers wound it down without naming a successor, and Copilot Studio as a multi-agent orchestrator because it coordinates other agents and escalates to people. A shared authoring surface did not make them alike, and `agent_interfaces` now carries that difference as a filter.
11. **Local-runtime passes two and three:** done. ONNX Runtime GenAI, Xinference, and OpenVINO Model Server closed the portable-library, gateway, and Intel/NPU gaps; MLC LLM, Qualcomm GenieX, and TensorRT-LLM extended execution to mobile, browser, and first-party NVIDIA serving. llamafile was screened out as packaging of the published llama.cpp record rather than a distinct execution boundary. Remaining gap: a proprietary or managed self-hosted gateway.
12. **Assistant regional follow-up:** Meta AI is now reviewed. Qwen Chat and Kimi remain unreviewed, and each needs its own product-terms and governance pass; do not infer an assistant's boundary from its provider's API record.
13. **Inference-service follow-up:** the routing-aggregator gap is closed. Nine records now join OpenRouter — Requesty, Eden AI, AI/ML API, Nano-GPT, TrustedRouter, and the Vercel and Cloudflare gateways as aggregators, Chutes as a managed host — and Unify is excluded after its product and its successor were both retired. Fee models were established for each but are deliberately absent from the records, since ADR 012 keeps volatile prices out of service scoring; where a fee bears on transparency it is recorded as documentation rather than as a price. Novita AI and Lambda Inference remain unreviewed for want of governing terms.
14. **Meta follow-up:** done. Meta Business AI is a marketing umbrella over advertising automation and creative tooling rather than a product. Its one operational product, the Meta Business Agent Platform, publishes real dated terms and is queued as a candidate in batch 2, because promoting it alone would settle that batch's role question by accident.

15. **Queue batch two — harnesses, chat clients, and code graphs:** done. Eight sweep candidates were reviewed against the boundary they each tested, and the star ranking inverted the substance ranking. Superpowers, the most-starred candidate in the sweep at 280,000 stars, is excluded: it declares no executable and no dependencies, and its session hook only prints a skill document as additional context. ruflo is published as a stateful agent runtime rather than the multi-agent orchestrator it markets, because the swarm engine it advertises is a dependency of neither the published package nor its command line. Orca, the least-starred, is the only one with implemented inter-agent handoff, and is published as the orchestrator. ECC is a coding-agent workflow, DeerFlow a stateful agent runtime, CodeGraph and GitNexus context graph engines, and LibreChat fills the thin multi-model chat-client role. GitNexus is the collection's first PolyForm Noncommercial record, which GitHub reports as having no license at all.
16. **Inference-service labs pass:** abliteration.ai is published as a direct model API. Its service type is a genuine editorial call, since it serves operator-derived weights it does not publish, and `model_sources` records only `first_party` for that reason. Liquid AI is not an inference service on its own statement that it does not offer a hosted API, reaching users instead through a gated playground, OpenRouter, Bedrock, and direct weight downloads — but it is not excluded either. It is a lab whose models a reader must serve themselves, which no collection currently represents, so `BACKLOG.md` carries that boundary question rather than recording a decision the Atlas has not made.

17. **Queue batch three — the memory and knowledge boundary:** done. OpenViking and Supermemory are agent memory services, Onyx an AI knowledge app, and AFFiNE a personal knowledge tool, so the sweep's proposed roles held for all four. Its source models did not. Onyx and AFFiNE each report no recognised license to automated detection while carrying a real proprietary tier, and Supermemory reports a permissive license over a repository that does not contain its memory engine at all — its whole tree is client libraries and applications, with no server and no container definition anywhere. The three errors all ran in the permissive direction, which is the concern the source-model diversity item was reaching for. OpenViking was independently confirmed as a selectable memory backend in the published DeerFlow record, which is what settled it as infrastructure rather than a product a person opens.
18. **Queue batch three — the derivative cluster:** done, and the expected shape did not hold. Of four candidates chosen as suspected derivatives, only oh-my-pi is one: it shares early history with the published Pi record and carries that author's copyright, while GitHub does not flag it as a fork. Codewhale is a rename of its own author's earlier project and Reasonix a self-rewrite on an orphan branch, so neither has an upstream to fold into; oh-my-claudecode is an independent port of a project the Atlas does not represent, which left the host-harness question rather than the derivative question to decide it. All four are published. The batch produced the rule itself, recorded as [ADR 020](adr/020-derivative-records-turn-on-operational-boundary.md).

19. **Queue batch four — the coding-agent second pass:** done for four of the six named candidates' cohort, and three of the four turned out to be winding down. Crush is published as the catalogue's first source-available coding agent, licensed on terms that bar competing commercial use and convert to permissive terms two years after each release. Qwen Code clears [ADR 020](adr/020-derivative-records-turn-on-operational-boundary.md) against its published upstream on five of the named fields. Continue and Roo Code are published as archived on their maintainers' own statements; Roo Code's documentation names a community fork and its origin project as alternatives rather than declaring a successor, so ADR 016 does not apply, and that fork is queued as a candidate. The add-on question [ADR 020](adr/020-derivative-records-turn-on-operational-boundary.md) reserved was not engaged: both extension-shipping candidates run their core outside the editor, one through a compatibility shim written for that purpose.
20. **Queue batch four — the research-agent role:** the role is sound but was over-applied once. Vane and dexter are published as research agents and deepagents as a framework, its own architecture document notwithstanding, because it owns a storage protocol, model harness profiles, and benchmark infrastructure of its own. STORM is excluded as a research artifact: its readme presents a research preview that cannot produce publication-ready output, it ships no tests at all, its package still declares alpha status, and its last substantive commit predates this review by more than a year. dexter is the catalogue's second `unclear` licence and the starker case — no licence file has ever existed in the repository, and a single readme sentence is the only artifact asserting terms.

21. **Queue batch five — the browser-agent role:** one publish, two exclusions, one hold. Skyvern is published as open core: every file in its repository is copyleft, and its own code comments say the anti-bot and captcha solvers are cloud overrides of open stubs that return nothing. Stagehand is excluded because its current major version deleted its agent loop on purpose; the migration guide says the orchestrator is gone and the control flow is the caller's, which leaves a driver library the Atlas has no role for. Agent S is excluded on the same test that excluded STORM, with one sharper tell: the loop lives in a demo command line, directly beneath a comment promising a confirmation prompt that does not exist. nanobrowser is a genuine agent held only because no deployment value describes a browser extension; that vocabulary gap is in the backlog.
22. **Queue batch five — the stateful-runtime role:** two publishes, one exclusion, one hold. CowAgent is the renamed chatgpt-on-wechat, and the rename changed nothing; the 2.0 agent core released ten weeks earlier is what earns the role, and it is one record under ADR 016. nanobot was queued as a framework from its tagline, but its readme and code say runtime, and it turned out to be the upstream of the agent module vendored into the published OpenViking record. herdr is excluded as a process supervisor that owns terminals rather than agents and makes no model calls. MindsHub is held: the repository is the former MindsDB database hollowed out to a superproject with no code of its own, the product lives in four submodules under three licences, and one of those licences has no identifier in the taxonomy.

23. **Queue batch six — the coding-workflow cluster and Jan:** three publishes, one hold. Open Design, the most-starred candidate in the queue, is published as a workflow because its daemon spawns and supervises the host agents itself, which keeps it clear of the add-on question [ADR 020](adr/020-derivative-records-turn-on-operational-boundary.md) reserved; its licence file is a silently modified Apache text, recorded as such. AionUi was queued as a workflow but owns an agent loop and real team-coordination machinery across three first-party repositories, so it is published as a general work agent. Jan is published as a chat client distinct from LibreChat on every named field, and not as a local runtime: its engine is the published llama.cpp record, which already names Jan as a downstream boundary. vibe-kanban is held; its maintainer company shut down in April and no published status describes that.
24. **Queue batch six — the backlog closers:** three publishes, one hold with a successor queued. screenpipe advances the source-model diversity item as a source-available record: it relicensed its whole tree to a custom commercial licence in June 2026. Hindsight was independently confirmed as a memory backend in the published oh-my-pi record, the same evidence shape that settled OpenViking. CAMEL passes the research-artifact test that STORM failed, on the strength of a real release train and test suite. SWE-agent also passes that test, but its own readme declares mini-swe-agent its successor, and ADR 016 records a declared successor only once the successor is published, so mini-swe-agent is queued first.

25. **Queue batch seven — the loop owners:** two publishes, one supersession closed, one hold, one exclusion. mini-SWE-agent is published, which under ADR 016 lets SWE-agent be published as superseded by it; the maintainers' boundary statement turned out to live in the successor's documentation, not the predecessor's, and the record says so. agenticSeek is published as a general work agent covering two of the role's four surfaces, at reduced confidence. deep-research is excluded: a fixed recursion in which the model fills a slot rather than choosing a tool, with no test runner, release, or continuous integration. plandex is held on the vibe-kanban shape; its last commit is the October 2025 wind-down and every first-party host fails to resolve.
26. **Queue batch seven — knowledge and retrieval:** two publishes, one out-of-scope exclusion, one hold. DocsGPT is published as a knowledge app and, contrary to the expected shape, is not open core: every feature the vendor's enterprise tier names is in the permissive tree. MemOS passes the research-artifact test on a real release train, but no runtime it targets names it as a backend, which the record carries as a weakness rather than a block. InsForge is excluded as a general backend-as-a-service outside the Atlas's scope. claude-obsidian is held under the skill-pack item as a fourth shape that item did not anticipate, and its facts are recorded there.

27. **Queue batch eight — orchestrators and a coding platform:** three publishes, one exclusion. Omnigent turns out to be first-party to a named company publishing under a neutral organisation, and is published as an orchestrator on the strength of a routing relay that interposes on a harness's own subagent spawn. Swarms passes the test that another orchestrator failed, where the advertised engine was absent from the shipped package: its dispatch was verified by running the published package offline against stub workers. Open SWE is published as mixed-source, because the agent server it pins for production is licensed on restricted terms and gated on a licence key while the repository itself is permissive. claude-squad is excluded on the terminal-manager precedent: its dependency manifest names no provider or network client at all.
28. **Queue batch eight — research-adjacent agents:** two publishes, one exclusion, one hold. MagenticLite is published after confirming it has severed its dependency on the vendor's superseded framework entirely, and it runs its browser inside a virtual machine, the strongest isolation boundary reviewed. TaskWeaver is published as archived; it passes the correctness-gate test with a real test workflow, but the repository is archived with no notice on either the readme or its still-live documentation site. MiroThinker is excluded as a model release with an evaluation harness, its test configuration pointing at a directory that was never written. LaVague is held on the dormancy shape, with an unaddressed security report noted.

29. **Queue batch nine — the coding tail:** three publishes with two role corrections, one hold. emdash was proposed as a coding agent but has no model client anywhere in its tree, driving thirty-eight vendors' tools over one protocol contract, so it is published as a workflow. stagewise was proposed as a workflow describing a product that no longer exists, its toolbar having been removed in November 2025, so it is published as a coding agent; its licence is the canonical copyleft text with a seventeen-line addendum carving out bundled icons the shipped application depends on, which neither its readme nor automated detection reflects. grok-cli clears the derivative rule against the published community agent for the same model family on capabilities and canonical data, while losing on interfaces. OpenCursor is held: its repository was created in 2022 as an unrelated proxy and rewritten in place this July, so its adoption figures belong to a product that no longer exists, and it has no continuous integration at all.
30. **Queue batch nine — memory and frameworks:** two publishes with one role inversion, one exclusion, one hold. BeeAI Framework was proposed as an orchestrator on the strength of a readme headline, which the curation rules say not to classify on; its coordination layer is under five per cent of the package, so the roles are the inverse of two published orchestrators, decided the same way. Memobase is published despite every first-party hosted surface being unreachable, because the self-hosted path that constitutes the product is intact. MiroFlow is excluded on its own evidence rather than by inheritance from the model release that pointed at it: no tests of any kind, two workflows that never execute code, no releases, and benchmark configurations outnumbering product ones many times over. dendron is held, and is the first hold with a published maintainer declaration, which raises whether the Atlas needs a status for this case at all.

31. **Queue batch ten — the tail turns:** six exclusions, five holds, no publishes, and the shape of the queue changed rather than the standard. A triage sweep of eight suspected non-systems confirmed five: two reading lists, an index of other people's knowledge gardens, a teaching course, and a general database whose readme never uses the words memory, agent, or tool server across eleven hundred lines while its repository description calls it a memory backbone. A sixth exclusion came from full review: a memory release with no continuous-integration directory at all, a year-old release, and a package name on the index that belongs to a different project the Atlas already publishes. One candidate survived triage and is queued for full review as retrieval infrastructure.
32. **Queue batch ten — three questions the holds have now earned.** Zoo Code fails [ADR 020](adr/020-derivative-records-turn-on-operational-boundary.md)'s field test against the archived Roo Code record on every named field, but that record is archived and supersession is blocked by the predecessor's own wording, so the rule as written hides an actively released agent behind a record readers are told not to start from; both of ADR 020's worked cases had living ancestors, so this is a gap rather than a decision, and it is held rather than settled by publishing. The skill-pack item gained three more occupants, one of which clears the research-artifact test outright yet executes its research pipeline entirely through the host model. And the taxonomy's research-reference role, which names exactly the material this sweep excluded, still has no occupants while the curation rules route that material elsewhere.

33. **Seekr, reviewed by name:** one publish, one queue, one out-of-scope name collision. The name resolves to three unrelated things, and only the vendor platform is a system: SeekrFlow is published as a stateful agent runtime beside the three cloud-vendor runtimes, on a documentation set of two hundred and seventy-two pages, a versioned API, and a published price list. Its source model is mixed, on the same shape as watsonx Orchestrate — an Apache-licensed Python client whose wheel ships the unmodified licence text, over a closed hosted core. Two evidence gaps are recorded rather than waved past: the word memory appears nowhere in the documentation index while the product page advertises agents that retain memory across sessions, and the terms the sign-up flow requires agreement to describe the vendor's consumer search application instead of the platform, with the real agreements behind a Trust Center access request. Confidence is medium for that reason. seekr-hatchery, the same vendor's Apache-2.0 coding-agent workflow, is queued rather than published: it is genuinely operational, with worktree isolation and a read-only repository mount inside its sandbox, but twelve stars, one fork, no watchers, and five contributors who all work for the vendor put it an order of magnitude below the catalogue's lowest repository-backed record. The OSINT toolkit of the same name is not an agent system and is not recorded.

34. **The proprietary editors, reviewed by name:** three publishes, one exclusion, one queue, and the batch's real work was boundary drawing rather than scoring. Cursor is one record covering the editor, the Agents Window, the CLI, Cloud Agents, customer-operated workers, and the vendor-managed automations, because the vendor documents them as one agent core reached through different clients and a session hands off between them without changing rules, hooks, or MCP configuration; its git forge is excluded as adjacent infrastructure, and Grok Bot is already a separate published record. Google Antigravity is published as its own record rather than folded into Gemini CLI, and the review found the consequence in the other direction: Google stopped serving Gemini CLI's free and consumer-subscription tiers on 2026-06-18 while directing those users to Antigravity, which the published Gemini CLI record now says. Supersession was considered and rejected — the repository is unarchived, shipped code during this review, and Google's own post commits to continuing support for licensed customers. The Antigravity Python SDK is queued rather than folded in, because including an Apache-2.0 wrapper over a closed runtime would flip the record's source model on a question that deserves its own review.

35. **Windsurf, which is a rename:** one publish under a different name. Windsurf resolved the batch's sharpest question by not being a live brand: windsurf.com redirects to the Devin Desktop product page, the vendor's own answer is that "Devin Desktop is the new name for Windsurf" and that "only the name and branding are changing", and its platform terms name the product "Devin Desktop (fka Windsurf)". That is the rename case [ADR 016](adr/016-superseded-predecessors-keep-their-record.md) reserves, not supersession, so it is one record carrying the note. It is nonetheless distinct from the published Devin record on four of [ADR 020](adr/020-derivative-records-turn-on-operational-boundary.md)'s five named fields — interfaces, deployment, canonical data, and the decisive one, execution boundary, where Devin is cloud sandboxes and Devin Desktop is the host — and the vendor draws the same line itself, selling the cloud agent as a separate stock-keeping unit. The record's most distinctive fact is not the rename: it is an Agent Client Protocol host that runs five other vendors' agents as first-class sessions, and states that its own privacy policy and legal terms do not apply to them. The taxonomy has no way to record that, which is the batch's one real vocabulary gap.

36. **Self-improving harnesses, asked for by name and mostly absent:** one publish, one inference service, nine exclusions, and a premise that did not survive contact. Meta has shipped no self-updating harness. HyperAgents is a Meta research publication whose repository has no continuous-integration directory, no test suite, no releases, and no package manifest against 2,705 stars, and Dr. Zero's self-evolution happens during training rather than at run time; both are excluded on the STORM and MiroThinker precedents, and the environments platform beside them is excluded on the benchmark boundary rather than on quality. Sakana AI is the inverse case: its best-known agentic work is paper code — the Darwin Gödel Machine is published from the first author's personal account with two tool tests, and the AI Scientist has no test file at all and was quietly relicensed away from open source — while the one thing it engineered as software, ShinkaEvolve, arrived with roughly seventy test files and a coverage-gated pipeline and is published. Its role was the contested call: the default path has the model filling one slot in a caller-configured pipeline, which is the shape the deep-research exclusion turned on, so it is published as a framework on the DSPy precedent rather than as the workflow its headless mode resembles. Sakana Fugu is published as an inference service and is a direct model API rather than a routing aggregator, on the abliteration.ai reasoning: the caller names the operator's own model identities and the orchestrated pool is undisclosed by design. Its data governance is the lowest reviewed in the collection for a documented reason rather than an absence — training on customer content is the default posture and the opt-out is explicitly not retroactive.

37. **The Alpha lineage, swept once:** twelve exclusions, two candidates, no publishes, and a lineage that splits three ways rather than one. The systems that opened the modern wave are almost entirely unadoptable, and the batch says so: AlphaGo, AlphaZero, and MuZero were never released, AlphaCode and AlphaMissense are archived repositories holding a dataset and a reference implementation whose weights were never published, and AlphaStar's repository contains the offline-RL code with its own readme confirming no online training code was provided. The scientific half is out on outcome and scope rather than on licence: AlphaFold is one model plus its runner, and the boundary held through two licence changes in two months, because AlphaFold 3's code was relicensed to Apache-2.0 on 2026-06-09 and its weights de-gated to a direct download on 2026-07-23 while the non-commercial parameter terms stayed put. Boltz is the control: MIT code and MIT weights, commercial use explicitly permitted, excluded by the same sentence of the same rule, which is what applying [ADR 007](adr/007-licenses-are-classification-not-inclusion.md) symmetrically looks like. The game engines are the sharpest local-runtime near-misses reviewed so far and both fail on purpose rather than capability: KataGo loads user-supplied weights across six backends, gained a direct ONNX loader in v1.18.0, and ships a JSON analysis engine documented for tool authors, while Leela Chess Zero's accelerator coverage is broader than several published runtimes and its Python binding exposes the backend directly — and each exists to play a game. The agentic half turned on availability alone, and this is where the premise of a single lineage broke: AlphaEvolve reached general availability on Google Cloud on 2026-07-09 with a documented API and a first-party client library, so it is queued as a candidate rather than excluded, while Co-Scientist is a Generative AI Preview whose access runs through a Google account team and AlphaProof is reachable only through a five-institution research programme. OpenEvolve is the one open descendant that reaches the Atlas, and it arrives through the agent door rather than the game or protein door: 7,319 stars, Apache-2.0, a real test and continuous-integration path, and a generate-execute-evaluate-retry loop with cascade stages and a MAP-Elites archive, queued on the ShinkaEvolve precedent. Note its repository moved from `codelion/openevolve`, and `evolutionaryscale/esm` likewise now redirects. Screened without individual records: AlphaZero, MuZero, AlphaStar, AlphaCode, AlphaMissense, and AlphaGeometry on the model, dataset, and research-code call; Leela Zero, ELF OpenGo, MiniGo, muzero-general, and Stockfish's NNUE evaluation on the domain-engine call; ColabFold, Chai-1, and the ESM line under the OpenFold entry; and OpenAlpha_Evolve and LLM4AD_Next, the first fifteen months cold with no release, the second presenting as an algorithm-design platform whose review should follow OpenEvolve's. One boundary question survived the batch rather than being settled in it, and `BACKLOG.md` carries it: AlphaGenome clears the inference-service inclusion conditions on a literal reading — Apache-2.0 SDK on PyPI, free self-serve API key, dated terms, redistributable non-commercial weights — and was screened out on the argument that no Atlas record could route a workload to a genomics endpoint. No vocabulary obligation opened, because nothing was admitted; had KataGo been, the taxonomy has no runtime type for a domain engine and no API-style identifier for GTP.

38. **Autonomous science systems, declined as a role:** one publish, two holds, three new exclusions with no existing entry re-narrated, and a boundary test that was derived from the evidence and then refuted by it. Screening ran three buckets — shipped products, published pipelines, and probes at the edge where a system merely executes code — and the test it produced, that a system owns its loop, closes that loop on a measurement it did not author, and offers something adoptable, turned out to convict the inspectable and acquit the opaque: both of its verdicts came from reading source, sustaining the Sakana loops and failing `SamuelSchmidgall/AgentLaboratory`, which owns a loop but scores it with `get_score(...)`, an LLM prompted as "an expert reward model" over its own output, while the one product the role would actually hold publishes no source for the system itself. [ADR 023](adr/023-autonomous-science-systems-are-not-a-role.md) records the decline and the three conditions that would reopen it. Kosmos is the single publishable member and is published as a research agent beside PRAXIST: hosted at $200 a run, terms effective 2025-10-30, an Apache-2.0 client over a closed core, and seven claimed discoveries that exist only in a vendor-authored arXiv preprint with no peer review, which the record carries in its weaknesses and confidence rather than in a reliability score. Microsoft Discovery is held rather than excluded because inclusion-gate condition 4, which asks for authoritative licence or terms sources, is unmeetable from what the vendor publishes: generally available since 2026-06-02, no product-specific terms on any first-party page, and a pricing page that renders `$-` as corroboration of the same absence — and Google Co-Scientist is held because peer review in *Nature* on 2026-05-19 is evidence of capability rather than of an operational boundary, with access an interest form behind a sign-in wall and a loop that closes on its own agents' tournament ranking. The published pipelines produced the sharpest finding, which is that the maintenance axis and the loop axis point in opposite directions: `gomesgroup/coscientist` is not the system in its *Nature* paper, being 21 entries, mostly result CSVs, and three code files whose entire tool set is `eval()`, `random.randint()`, and a stop command, while `zou-group/virtual-lab`, the only packaged and maintained candidate, ships a library that simulates agent *meetings* through `Agent` and `run_meeting` rather than owning the discovery loop it is known for, which was human-run SLURM scripts. Those two and `SamuelSchmidgall/AgentLaboratory` are the batch's three new exclusion entries, so every candidate screened here is recorded in a machine-readable file rather than in this note alone; Elicit, screened as a boundary probe against the research-agent line, has no repository and therefore cannot wait in the candidate queue, and is recorded in `BACKLOG.md` beside the Liquid AI item that describes the same shape. Both Sakana repositories moved off Apache-2.0 on 2025-12-19 onto byte-identical bespoke use-restricted licences, and carry zero tests, zero continuous integration, and zero releases, which sustains the producthood reasoning batch 36 recorded rather than reopening it. One incidental finding sits outside the decision: `platform.futurehouse.org` now returns HTTP 301 to `platform.edisonscientific.com`.

39. **The robotics scope decision, screened and not yet settled either way:** twenty-three candidates across five classes — robot software stacks, policy and action models, simulation and benchmark environments, robotics interchange contracts, and commercial hardware products — were found through the mandated topic sweeps (`topic:robotics`, `topic:embodied-ai`, `topic:vision-language-action`, `topic:ros2`, `topic:robot-learning`), vendor and follow-up searches (`org:physical-intelligence`, `org:google-deepmind robotics`, `org:unitreerobotics`, `org:boston-dynamics`, `urdf robot description ros`, `org:ros urdf`), and direct visits to `bostondynamics.com`, `unitree.com`, `figure.ai`, and `1x.tech`. Thirteen are held under `triage.held_by: "robotics scope decision"` and ten are new exclusion entries (`directory/exclusions.json` 55 → 65); the candidate queue moved from 55 to 68 records. For the fourteen candidates gated on family and role fit rather than on the Models, specification, or condition-4 hardware-evidence boundaries, the line that separated held from excluded is a model in the decision loop: `OpenMind/OM1` and `huggingface/lerobot` are held because their own documentation names an LLM or VLM component whose output reaches a physical actuator, and `dora-rs/dora` is held on the adjacent property of composing a dataflow graph that mixes model nodes (LLM, YOLO) with a ROS2 bridge to robot software — a reach to a physical actuator its README does not itself state — while `ros-navigation/navigation2`, `autowarefoundation/autoware`, and the six simulation and benchmark candidates (`google-deepmind/mujoco`, `isaac-sim/IsaacLab`, `mani-skill/ManiSkill`, `ARISE-Initiative/robosuite`, `Lifelong-Robot-Learning/LIBERO`, `bulletphysics/bullet3`) are excluded on inclusion-gate condition 2 because no model-driven decision loop appears anywhere in their evidence — classical costmap-and-behavior-tree planning, fixed perception-control loops, and physics/training substrates that other software trains or evaluates a model against, never a model operating its own loop; `commaai/openpilot` is excluded on narrower ground alone, since it does train and run models — its README states "we use your data to train better models and improve openpilot for everyone" and it vendors a machine-learning framework (`tinygrad_repo`) at its repository root — but neither its README nor any other reviewed evidence documents a language model, tool-calling, or delegated-task framing. `Physical-Intelligence/openpi` and `google-deepmind/gemini-robotics-sdk` (the Safari SDK) are held on the same rule, tested as software rather than as model artifacts, because each names itself as tooling for named model checkpoints rather than as one model — openpi's own description calls it "the packages to run or fine-tune" three named checkpoint families, and the Safari SDK's calls itself "full lifecycle tooling for Google DeepMind's Gemini Robotics models" — while `NVlabs/alpamayo`, `NVIDIA/Isaac-GR00T`, and `openvla/openvla`, each named in its own description as one model, are held instead against the Models collection (ADR 025), because neither of the two `model_types` nor any of the five `model_modalities` names a policy whose primary output is a robot action or predicted trajectory rather than text — `google/gemini-robotics-er-1.6-preview`, already queued in `directory/model-candidates.json`, is the control case showing the gap. `ros/urdfdom` is held under the specification boundary rather than the family gate: it does not cleanly fit the admission sentence in `docs/SPECIFICATIONS.md` — none of the boundary parties it enumerates (agents, tools, clients, users, repositories, extension packages) names a robot or hardware description boundary — and none of the ten `specification_scopes` values names one either; `foxglove/mcap` is excluded on the refusal sentence as, in its own words, "a logging library and file format" with no agent-facing contract. The four commercial hardware products — Spot, Unitree G1, Figure, and 1X NEO — are held on inclusion-gate condition 4 alone, each for a distinct, checkable reason (a Contact Sales gate, a warranty page naming no software licence, a stale generic 2023 website ToU, and a 404'd terms page respectively); none is excluded instead — the exclusion schema does not forbid a repo-less entry, and ten existing entries are themselves already repo-less — because condition 4 is genuinely unmeetable from what each vendor publishes, the same distinction ADR 023 drew for Microsoft Discovery. Three vocabularies were checked against this boundary and none can express it: `execution_boundaries` and `agent_capabilities` carry no perception or actuation value, all ten `specification_scopes` describe agent-to-software boundaries, and `model_types` reaches an embodied-reasoning model that emits text but not an action-emitting policy. Against [ADR 023](adr/023-autonomous-science-systems-are-not-a-role.md)'s three-part reopening test, applied here to embodiment rather than to autonomous science, the decision does not reopen: the second condition is plausible — `agent_system` has no role for an outcome shaped like "operates physical robot hardware" — but as tested it is stated as a mechanism, "a model's output driving a physical actuator," the exact shape ADR 023 refused elsewhere ("a role named after a mechanism is a trait axis wearing a role's name"), so a later ADR needs to settle the domain phrasing rather than inherit this one; the first condition is not established, and structurally cannot be by a candidate — `CANDIDATE_REQUIRED` in `scripts/validate_directory.py` carries no `source_model` and no `licenses` field, the two things condition 4 of the five-condition gate requires, and all five candidates this batch pointed at (Dora, OM1, LeRobot, openpi, the Safari SDK) still carry `review_required: ["licensing", "classification", "traits", "editorial_score"]`, with openpi's own finding recording checkpoints downloaded from an external bucket rather than bundled and the Safari SDK's recording a Trusted Tester programme gate — exactly the licensing facts that review has not yet performed; and the third condition was never tested for a closed system — all five candidates are public repositories whose README the property was read from, so the sweep showed the property is legible where a system is inspectable by construction, and the four genuinely opaque candidates in this batch, the hardware products, were held on condition 4 alone and never run through the model-in-the-loop test, leaving "applies equally to open and closed systems" unexercised. No vocabulary or record changes follow; the missing pieces are named rather than assumed away. Two incidental findings sit outside the decision: `figure.ai` is served through a CDN cache that reads consistently within a single verification session but embeds a shared footer component's randomly seeded SVG gradient identifier that regenerates whenever the cache re-renders from origin on its own schedule, so no fetch of it is safe to pin as a durable citation, found after an earlier citation for the same page had recorded a hash that matched nothing on a later check; and `ros/urdfdom`'s README names `ros.org/wiki/urdf` as the page carrying the specification's details; that page carries the ROS wiki's own end-of-life banner, which states the wiki "will also be EOL and transition to being an archive" and directs maintainers to migrate wiki content into their package README files, so the page was read but not pinned as evidence: it is an archive-bound third-party page rather than the format's maintained authoritative home.

40. **General-work, browser, and memory discovery plus a research-input cleanup:** eleven operational candidates were queued from current first-party product pages and repositories, and five older false positives were excluded. Genspark AI Workspace, Manus, Notion Agents, Lindy, and Asana AI Teammates enter as general-work candidates whose exact product boundaries, governing terms, and execution controls still require full review; Gumloop enters with the established low-code builder question; Fellou and Aera Browser extend the thin browser/computer-use queue; and the repositories for Nerve, MindMemOS, and Memoria add one stateful runtime and two agent-memory designs with inspectable run paths. None was promoted from discovery metadata. The resolved tail is deliberately different: `aristoapp/awesome-second-brain` and `TeleAI-UAGI/Awesome-Agent-Memory` curate other systems, `NirDiamant/Agent_Memory_Techniques` explicitly publishes an educational notebook cookbook rather than production software, and `oldwinter/knowledge-garden` is the maintainer's personal Obsidian vault and reusable template rather than the software operating it. `WujiangXu/A-mem` is the paper-reproduction repository and points to the separately maintained operational package already queued as `agiresearch/A-mem`, so it is recorded as research code rather than a duplicate system. These five are now durable exclusions, moving the queue from 68 to 74 after the eleven additions and the exclusion file from 65 to 70.

41. **A-MEM's operational repository separated from its paper code:** `agiresearch/A-mem` is published as the adoptable memory-system boundary after the duplicate `WujiangXu/A-mem` paper repository was excluded in the preceding batch. The operational repository supplies a source-installable Python package, tests, an MIT licence, a documented `AgenticMemorySystem` API, semantic retrieval with linked-neighbor expansion, and OpenAI and local Ollama execution. The score does not inherit the paper's evaluation claims: review of the implementation found that the documented main constructor resets an in-memory Chroma collection on every start even though separate persistent retriever classes exist, that lexical-ranking imports do not reach the retrieval path, and that the package remains at 0.0.1 with no tagged release and no commit since 2025-12-12. Those facts keep maturity, interoperability, and memory intelligence modest while the fully local path supports data sovereignty. The promotion moves the reviewed catalog from 174 to 175 and the provisional queue from 74 to 73; the exclusion count stays at 70.

42. **Model queue batch one — the releases readers compare:** twenty-four publishes, two holds, and ten new licence identifiers, chosen as the current flagship release from each major developer across both distribution shapes rather than by any quality ranking. The reviewed Models collection moves from seven to thirty-one and the queue from 351 to 327. Every licence conclusion was read from the governing document at a pinned commit or on the developer's canonical page, and four of them contradict what automated detection or the imported `reported_license` field says. Gemma 4 31B IT is plain Apache-2.0: Google's Gemma Terms of Use page states "For Gemma 4 terms, see the Gemma 4 license", its appendix defines Gemma as versions 1 through 3n, and the Gemma 4 licence page is the unmodified Apache text, so the release is open source with no prohibited-use addendum. Kimi K2 Thinking and MiniMax-M2 both present as MIT and are not: each licence reproduces the MIT grant and then requires products above one hundred million monthly active users or a revenue threshold to display the model's name, which is a conditional attribution obligation the OSI text does not carry, so both are source-available under their own identifiers — and MiniMax's document lives only in a GitHub repository the weight repository links to, with no licence file beside the weights at all. Llama 4 Maverick's European Union multimodal exclusion is in the Acceptable Use Policy rather than the Community License text, which a full-text search of the pinned licence file confirmed; the Llama 3.3 policy carries the same clause but it is inoperative for a text-only release. GLM-4.5 declares MIT for its weights only in model-card metadata while the companion code repository is Apache-2.0, recorded as a clarity deduction rather than a contradiction. The batch's second finding is lifecycle: a release's first-party API is often gone while its weights remain. DeepSeek discontinued the `deepseek-chat` and `deepseek-reasoner` names on 2026-07-24, Moonshot discontinued the whole kimi-k2 series on 2026-05-25, Mistral retired the Magistral Small 2506 endpoint on 2025-11-30 and deprecated Devstral Small 2507, and Claude Sonnet 4.5 and Opus 4.8 are documented as legacy with retirement floors. Those records carry `developer_api` only where the developer's own page still serves the exact release, and their serving-reach scores say so. The two holds are the same shape from the closed side: Claude Opus 4.1 was retired on 2026-08-05 and its model page now returns 404, and Grok 4.1 Fast has no first-party model page in xAI's current catalogue, so neither has an authoritative URL to cite; both stay queued, and the batch reviewed their documented replacements, Claude Opus 4.8 and Grok 4.6, instead. Grok 4.6 is the collection's lowest score because xAI publishes no dated snapshot ID, no per-model lifecycle status, and no retirement-notice policy, and because its enterprise terms and acceptable-use policy sit behind a bot check that returns 403 to any non-browser fetcher, which the evidence-link report will flag on its next run; the pages were read with browser headers and quoted in the review notes. No score in the batch measures output quality, benchmark rank, parameter count, price, latency, or throughput.

43. **Model queue batch two — the newest lines readers compare:** thirteen publishes and three new licence identifiers, chosen as the current-generation flagship from each major developer rather than by any quality ranking. The reviewed Models collection moves from thirty-one to forty-four and the queue from 327 to 314. Four licence conclusions break from their batch-one precedents. Qwen3.8-Max weights are not Apache-2.0: the checkpoint carries a custom licence with a 100-million-MAU attribution duty and a separate-licence gate for model-as-a-service businesses above trailing revenue, so it is source-available under its own identifier. DeepSeek-V4-Pro is not under the DeepSeek Model License 1.0: the repository carries the verbatim MIT text, so it is open source. Kimi K3's bespoke licence adds a model-as-a-service revenue gate requiring a separate agreement before any commercial use, going beyond the K2 Modified MIT text under its own identifier. Mistral Medium 3.5 is not Apache-2.0 either: its repository carries a Modified MIT text with a 20-million-dollar monthly-revenue bar, source-available under its own identifier, while Mistral Small 4 is card-only Apache-2.0 like the Magistral precedent. GLM-5 repeats the GLM-4.5 posture — MIT declared in card frontmatter with no licence file — and Llama 4 Scout carries byte-identical Maverick terms including the European Union multimodal exclusion. The batch's second finding is identity: Anthropic's versioning policy makes post-4.6 dateless IDs pinned snapshots rather than moving aliases, so Claude Opus 5 and Sonnet 5 are fixed releases with retirement floors, while GPT-6 Astra exposes only the undated alias with no dated snapshot to pin, recorded as a lifecycle deduction. The third finding is lifecycle at both ends: Gemini 3 Pro Preview was shut down on 2026-03-09 with a named successor and is recorded as a retired release, and the DeepSeek-V4-Pro preview was superseded by the 0813 official release with the API ID slated for routing to V4.1 Flash. Distribution follows the conventions now written into `docs/MODELS.md`: Vertex AI is first-party serving for Google's own models and third-party hosting for others, NVIDIA's trial endpoint is developer-API evidence for NVIDIA's own models while NIM pages are third-party evidence for everyone else's, and aggregator listings without host documentation do not count. The evidence-link checker now retries bot-walled 403s with browser headers and records a visible warning instead of a conclusive failure. No score in the batch measures output quality, benchmark rank, parameter count, price, latency, or throughput.

44. **Model queue batch three — stables, variants, and the line-update question deferred:** nine publishes and two new licence identifiers, covering the current stable heads (Gemini 3.8 Flash, Gemini 3.1 Pro Preview), the GPT-5.5 Pro and Instant variants, the GLM-5.3 generation, the Apache-2.0 Qwen3.8-27B sibling and the experimental Flash-Next preview, and the official DeepSeek V4 Flash release. The reviewed Models collection moves from forty-four to fifty-three and the queue from 281 to 272, with five more dispositions (two retired Qwen3.8 artifacts, two superseded V3 snapshots, and the customtools endpoint folded into its preview record). Two licence conclusions break new ground. GLM-5.3 ships a real license file with a model-as-a-service security-review gate above a revenue threshold, source-available under its own identifier — unlike both the MIT GLM-5 and the MIT 5.3-Flash. Qwen3.8-Flash-Next carries a Community 1.0 license whose commercial gate has no revenue floor, stricter than the Max sibling license and unlike the Apache-2.0 27B sibling, under its own identifier. DeepSeek-V3.1 was researched and then excluded rather than reviewed: an August 2025 snapshot superseded twice over on a line reviewed at V3.2, which is exactly the retired-snapshot shape the alias discriminator reserves for exclusion. The batch surfaced one open question it did not settle: when a reviewed line moves (the Qwen3.8-max-0902 snapshot, the V4-Pro-0813 official release), whether the new snapshot gets its own record, displaces the old one, or waits is undecided, so both stay queued. GPT-5.5 Instant tested the discriminator's edge and held: a launch-dated release served only through a rotating alias is reviewable with a lifecycle deduction, because the release is the fixed identity even though the alias moves. No score in the batch measures output quality, benchmark rank, parameter count, price, latency, or throughput.

45. **Line re-reviews, not second records:** two reviewed lines moved and were re-reviewed in place under the new rule, with the snapshot source IDs excluded as folded in. The qwen3.8-max endpoint now serves the September 2 snapshot by default with a dated parameter on both first-party clouds; the license, weights, prices, and context are unchanged, so only the boundary, snapshot evidence, and lifecycle score moved. The DeepSeek-V4-Pro-0813 official release replaced the preview checkpoint with MIT terms intact, gained exact-checkpoint hosting, and reversed the planned September API routing to V4.1 Flash — which the old boundary wrongly stated as fact — so the URL, boundary, evidence, and lifecycle score moved with it.

46. **Promotion review of the triaged queue — twenty-two publishes and nine holds:** every `review_ready` candidate from the 2026-09-12 triage batch was reviewed in full through `scripts/promote_system_candidate.py`, which leaves family, role, traits, score and prose empty in the draft so the automated proposal is never copied, and validates the proposed projects, licence evidence and remaining queue together before writing. Twenty-two were published, and five were classified differently from their automated proposal on their own evidence: Memorix as a human–agent memory bridge rather than a coding agent, since its coding tool is one consumer of the shared memory; vibecode-pro-max-kit as a coding-agent workflow rather than a memory service; Second Brain as a general work agent despite its name; Compartment as an agent memory service rather than retrieval infrastructure; and MateClaw as a stateful agent runtime rather than an orchestrator. OpenEvolve is published on the ShinkaEvolve precedent that queued it in batch 37, so that batch's test now requires it to be queued or published and never excluded. Three findings shaped records rather than inclusion. OpenCursor's repository dates from 2022 and its star count predates the current product, which began in July 2026, so maturity is scored low and the repository note says so. Plandex has had no commit since its hosted service began winding down on 2025-10-03, and Claudable none since March 2026; both stay active because status follows a maintainer's declaration, with the silence carried in weaknesses and maturity. MindsHub's MIT top-level licence understates what it ships: at the commits it pins, the frontend is AGPL-3.0, the agent backend is proprietary, and the core engine is Elastic License 2.0 recorded as `LicenseRef-Commercial`, so it is mixed-source with four licences. Nine were held rather than published. Three have no licence file anywhere in the repository — AdaL CLI, MindMemOS, and the live BabyAGI repository, whose archived predecessor keeps its record — so inclusion condition 4 cannot be met. Revezone has had no push since 2024-01-10 and still describes itself as beta, and seekr-hatchery keeps its earlier vendor-only adoption hold. Four are held for a maintainer decision rather than on missing evidence: xerj's README asks agents installed in other people's codebases to open a feedback pull request, which inflates contributor and fork signals; Lurnby is a reading and spaced-repetition tool whose fit to the human knowledge role was flagged for a second opinion; super-simple-software-factory is operational but its maintainer calls it a starting point rather than a product; and LaVague has an open report of code execution through eval of model output derived from page content, unanswered since 2026-06-12. Each draft passed the tool's check against the post-apply catalog. Drafting agents' claims were re-verified directly rather than trusted: GitHub metadata for stars, push dates, archive flags and licence endpoints; every licence at the commit actually pinned; and a sample of more than fifty quotations fetched from the cited repository, where phrases found only in the Atlas's own taxonomy or in a paraphrase were kept out of record prose.

47. **Local-first and editability re-review:** eleven records whose `local_first` or `human_editable` value disagreed with the newly written trait definitions were re-reviewed against their own repositories and documentation, and seven changed. AnythingLLM's desktop documentation says everything is saved locally by default, AdalFlow writes checkpoints and traces to local default paths with no vendor service, and Letta Code now starts new interactive users in local mode, so all three became local-first. Letta Code, AppFlowy, deepagents' terminal agent, and Khoj's memory settings each give a person a direct surface to change stored content, so all four became editable; claude-mem stopped being editable because its worker exposes only deletion and its viewer only reads. Four kept their values: Hindsight's recommended path is a self-hosted server with an embedded local database, claude-mem's cloud sync starts only after an explicit hosted-plan enrollment, Vane can delete or regenerate but never edit a message, and BeeAI Framework persists nothing of its own. Two cases exposed limits in the definitions rather than errors in the records: Cursor, like Claude Code, keeps the working tree local but cannot run without its vendor's backend, and a library whose storage the application decides has no default data to keep, which is why framework records are almost uniformly not local-first.

48. **Model queue batch four — the GPT scale variants readers compare:** six publishes and no new licence identifiers, covering same-line cost/scale variants of already-reviewed releases rather than new lines. GPT-5 Mini and Nano are each a dated August 2025 snapshot behind their alias, each with a second managed path on Azure Foundry — whose reasoning-models page names the exact model with dated feature support — and each already scheduled for shutdown on 2026-12-11 with a named 5.6 successor, recorded as lifecycle notes rather than blocks. GPT-4.1 Mini and Nano are dated April 2025 snapshots with 1M-token context and no reasoning step; Nano shuts down on 2026-10-23 with gpt-5.6-luna named, while Mini carries no API deprecation entry after OpenAI's February 2026 retirement scoped it to ChatGPT with the API explicitly unchanged. GPT-4o Mini is a dated July 2024 snapshot, active with no deprecation entry, with fine-tuning and larger-model distillation documented as its distinct strength. gpt-oss-20b is the Apache-2.0 sibling of the reviewed 120b weights at a pinned Hugging Face commit, with a 16 GB self-operation envelope and consumer-hardware fine-tuning as its distinct strength. The reviewed Models collection moves from fifty-three to fifty-nine and the queue from 295 to 289; newer lines (`gpt-5-pro`, `gpt-5.1`, `gpt-5.2`) stay queued for a later lines batch. No score in the batch measures output quality, benchmark rank, parameter count, price, latency, or throughput.

49. **Model queue batch five — the newer GPT lines readers migrate toward:** seven publishes and no new licence identifiers, covering the successor spine from GPT-5 Pro through the current 5.6 heads. GPT-5 Pro is a dated October 2025 snapshot, Responses-only with fixed high reasoning effort, shutting down December 2026 into gpt-5.6-sol pro mode. GPT-5.1, 5.2, and 5.4 are dated November 2025, December 2025, and March 2026 snapshots respectively, all active with no API retirement entry while their chat and codex variant endpoints already shut down; GPT-5.2's own page positions it as the previous flagship behind GPT-6 Astra. The 5.6 Sol, Terra, and Luna tiers are reviewed as launch-dated releases under the GPT-6 Astra precedent: tier names with no dated snapshot to pin, each carried on OpenAI's API and an Azure availability row, each with the unpinnable-rotation lifecycle deduction. The reviewed Models collection moves from fifty-nine to sixty-six and the queue from 289 to 282; codex variants and 5.4 scale variants stay queued for later batches. No score in the batch measures output quality, benchmark rank, parameter count, price, latency, or throughput.

50. **Model queue batch six — the MiniMax M2 successor line:** four publishes, two exclusions, one hold, and five new licence identifiers, and the batch's finding is that no two successors share M2's terms. M2.1 repeats the Modified-MIT shape but drops the 100M-MAU/$30M-ARR thresholds, so any commercial use triggers attribution. M2.5 splits the regime: a bespoke MiniMax Model License dated 2026-02-13 governs the weights beside the shards while a second unconditional Modified-MIT governs the repository code. M2.7 moves to a non-commercial license requiring prior written authorization for any commercial use, and M3 to a community license with a $20M-revenue authorization gate and a sub-threshold notice path. The two `-highspeed` IDs are excluded as same-performance speed-tier aliases with no weights, license, repository, or fixed identity. M2-Her is held: a live dialogue endpoint with no published weights, license, or pinned release. The reviewed Models collection moves from sixty-six to seventy and the queue from 282 to 275. No score in the batch measures output quality, benchmark rank, parameter count, price, latency, or throughput.

51. **Model queue batch seven — the Kimi K2 successor line:** three publishes and two exclusions with no new licence identifiers, because all three successors carry the K2 Modified MIT text verbatim modulo the display string. Kimi K2.5, K2.6, and K2.7-Code each publish 64 compressed-tensors shards with the license file beside the weights; K2.5's platform endpoint already sunset on August 31, 2026 while K2.6 and K2.7-Code remain served, and K2.7-Code stands as its own coding line rather than a K2.6 snapshot. The `turbo` and `-highspeed` IDs are excluded as speed-tier aliases with no weight repository, license, or fixed identity; the platform lists turbo only as a deprecated API row and describes highspeed as the same K2.7-Code model at higher throughput. The reviewed Models collection moves from seventy to seventy-three and the queue from 275 to 270. No score in the batch measures output quality, benchmark rank, parameter count, price, latency, or throughput.

52. **Model queue batch eight — the GPT-5.4 scale variants:** three publishes and no new licence identifiers, mirroring batch four on the reviewed 5.4 line. GPT-5.4 Mini and Nano are dated March 2026 snapshots with chat, responses, and batch endpoints; GPT-5.4 Pro is a dated March 2026 snapshot with a 1M-token context window served Responses-only. All three carry a second managed path on Azure Foundry and no API retirement entry. The reviewed Models collection moves from seventy-three to seventy-six and the queue from 270 to 267. No score in the batch measures output quality, benchmark rank, parameter count, price, latency, or throughput.

53. **Model queue batch nine — the Codex variants:** six publishes and one hold with no new licence identifiers, closing the GPT family. Five lines retired on July 23, 2026 are recorded as retired releases under the Gemini-preview precedent: each has a dated launch, a dedicated model page, and a deprecation row naming its substitute, even though every alias rotated with no pinnable snapshot. The live GPT-5.3-Codex line is reviewed as a launch-dated release with the unpinnable-rotation deduction. GPT-5.3-Codex-Spark is held: a research preview with no model page and design-partner-only API access. The reviewed Models collection moves from seventy-six to eighty-two and the queue from 267 to 260. No score in the batch measures output quality, benchmark rank, parameter count, price, latency, or throughput.

54. **Model queue batch ten — the Qwen alias sweep:** one publish, eighteen exclusions, and one hold with one new licence identifier. All 41 Alibaba IDs resolve into three shapes: undated serving-tier heads that move between dated snapshots (excluded with pointers at their current equivalents, confirmed by the repeated first-party "functionally equivalent to the snapshot model" discriminator and by the absence of every one of these names across the 465-model Qwen org), the Qwen3.6-Max preview (held per the 3.8-max-preview precedent), and the Qwen3.8-Flash production API line (reviewed: single invocation ID with text, image, and video input and 1M context, governed by the Model Studio Service Agreement). The reviewed Models collection moves from eighty-two to eighty-three and the queue from 260 to 240. No score in the batch measures output quality, benchmark rank, parameter count, price, latency, or throughput.

55. **Model queue batch eleven — the Qwen3 weight generation:** six publishes and no new licence identifiers. All six carry the verbatim Apache License 2.0 text with the Alibaba appendix and no custom clauses: the dense 32B, the 30B-A3B and 235B-A22B MoE releases, the two distinct Qwen3-Next post-trained checkpoints, and the QwQ-32B reasoning release. Each is pinned to its Hugging Face commit with first-party Model Studio or DashScope API serving. The reviewed Models collection moves from eighty-three to eighty-nine and the queue from 240 to 234. No score in the batch measures output quality, benchmark rank, parameter count, price, latency, or throughput.

56. **Model queue batch twelve — Qwen3 coder and VL weights:** four publishes and two holds with one new licence identifier. The 30B and 480B Qwen3 coders and the Qwen2.5 32B coder carry verbatim Apache-2.0, while the 72B Qwen2.5-VL carries the custom Qwen License Agreement with its commercial-use authorization gate. The two Qwen3-VL-235B releases are held: their repositories carry an Apache badge but no weight-scope license file, and README claims are insufficient evidence. The reviewed Models collection moves from eighty-nine to ninety-three and the queue from 234 to 228. No score in the batch measures output quality, benchmark rank, parameter count, price, latency, or throughput.

57. **Model queue batch thirteen — the Qwen3.5 and Qwen3.6 weights:** seven publishes and no new licence identifiers. All seven carry the verbatim Apache License 2.0 text with no custom clauses: the dense 9B, 27B, and 3.6-27B releases, the 35B and 3.6-35B MoE releases, and the 122B and 397B MoE flagships. Each is pinned to its Hugging Face commit; all but the 9B carry first-party Model Studio serving. The reviewed Models collection moves from ninety-three to one hundred and the queue from 228 to 221. No score in the batch measures output quality, benchmark rank, parameter count, price, latency, or throughput.

58. **Model queue batch fourteen — the Cohere Command R and Aya weights:** seven publishes and no new licence identifiers. All seven carry CC-BY-NC-4.0 with the Labs acceptable-use addendum: the Command R, R+, and R7B refreshes, the Arabic R7B release (whose changelog misnames its own ID, recorded in the boundary), the 32B Aya Expanse research release, and the two Aya Vision multimodal releases. The reviewed Models collection moves from one hundred to one hundred seven and the queue from 221 to 214. No score in the batch measures output quality, benchmark rank, parameter count, price, latency, or throughput.

59. **Model queue batch fifteen — the Cohere Command A variants:** five publishes and no new licence identifiers. Command A+ and North Mini Code break the family pattern with declared Apache-2.0 grants (no LICENSE file ships, so both records rest on concordant card and docs declarations); Reasoning, Translate, and Vision repeat CC-BY-NC-4.0 with the Labs addendum. The reviewed Models collection moves from one hundred seven to one hundred twelve and the queue from 214 to 209. No score in the batch measures output quality, benchmark rank, parameter count, price, latency, or throughput.

60. **Model queue batch sixteen — the Mistral dated flagships:** six publishes and one new licence identifier. Large 2411 and Ministral 8B carry the research-only Mistral Research License; Large 2512 and the two Smalls are card-only Apache-2.0; Medium 2505 is API-only with no weights license at all. The reviewed Models collection moves from one hundred twelve to one hundred eighteen and the queue from 209 to 203. No score in the batch measures output quality, benchmark rank, parameter count, price, latency, or throughput.

61. **Model queue batch seventeen — Codestral, Devstral, and Nemo:** four publishes and one new licence identifier. Codestral carries the non-production MNPL with its license file deleted from the repo; Devstral 2 a Modified MIT file; Nemo a four-surface declared Apache-2.0; Devstral Medium is API-only with no weights at all. The reviewed Models collection moves from one hundred eighteen to one hundred twenty-two and the queue from 203 to 199. No score in the batch measures output quality, benchmark rank, parameter count, price, latency, or throughput.

62. **Exclusion re-review, 2026-09-17 — one lift in eighty-nine:** every exclusion now carries `excluded_at`, backfilled from the commit that introduced it, and `verified_at`, the date a reviewer last re-read the reason against the source. The first full pass re-checked all eighty-nine. The eleven repo-less products were re-fetched first-hand: none has gained a repository, API, weights, or general availability, and Unify's homepage has been repurposed as a research lab. Live metadata for all eighty repositories found no rename, no new archive beyond the one already recorded, and thirty-five with commits or releases since their exclusion; the twelve whose reasons rested on a tree fact that could move — Stagehand's deleted agent loop, herdr's and claude-squad's provider-free manifests, LazyCodex's submodule, llamafile's upstream pin, Agent S's demo loop, glean's planned AI phase, InsForge's undocumented memory route, and the rest — were re-read from their manifests, migration guides, and workflows, and every one still holds. The lift is Second Brain LLM Wiki, excluded under ADR 031 two days before the Agent packs collection existed and never re-examined under ADR 032; it enters `packs.json` as a skills bundle. Reasons were amended only where the facts moved without changing the decision.

63. **Model queue batch eighteen — the Xiaomi MiMo lines:** five publishes and one exclusion with no new licence identifiers. The three weight releases carry MIT declarations without LICENSE files; the Pro and Omni V2 lines are proprietary API-only records; UltraSpeed is excluded as a limited-time experience mode of the reviewed V2.5-Pro line. The reviewed Models collection moves from one hundred twenty-two to one hundred twenty-seven and the queue from 199 to 193. No score in the batch measures output quality, benchmark rank, parameter count, price, latency, or throughput.

64. **Agent packs — the two ADR 031 holds:** one publish and one exclusion, split on whether the pack's own program runs. Superpowers is published as a coding-agent workflow on `skills/brainstorming/scripts/server.cjs`, a zero-dependency HTTP and WebSocket server the brainstorming skill tells the host to start, which serves and live-reloads screens, launches the user's browser through `child_process.execFile`, and collects the reader's selections for the session — the shape ADR 031 already held decisive for vibecode-pro-max-kit's Chrome-driving scripts. Its session hook and its OpenCode, Pi, and Hermes plugins stay distribution machinery, and the port, token, and process files under `.superpowers/brainstorm/` are plumbing for that server rather than memory, so the record carries no `persistent_state`. obsidian-claude-pkm is excluded: there is no `PreToolUse` hook and no shipped script that returns a blocking result, and the only denial in the tree is a three-entry `permissions.deny` array the host's own permission engine evaluates, which is installation rather than operation. Adoption decided neither, and the earlier exclusion's lesson that 288,000 stars establish no operational boundary still holds; what changed is the server the first review never opened.

65. **Model queue batch nineteen — ByteDance Seed 1.x:** four publishes and two exclusions with no new licence identifiers. No weights exist for any of the six: the 1.6, Flash, Vision, and 1.8 lines are proprietary API-only records with dated snapshots, while character and evolving are excluded as aliases without fixed identity. The reviewed Models collection moves from one hundred twenty-seven to one hundred thirty-one and the queue from 193 to 187. No score in the batch measures output quality, benchmark rank, parameter count, price, latency, or throughput.

66. **Model queue batch twenty — ByteDance Seed 2.x:** six publishes and no new licence identifiers. Exhaustive first-party enumeration finds no public weights for any Seed 2.x flagship either, so all six are proprietary API-only records: the Code, Lite, Mini, and Pro lines of the February 2026 generation plus the Pro and Turbo tiers of the June 2026 generation, Turbo verified as a distinct tier rather than an alias. The reviewed Models collection moves from one hundred thirty-one to one hundred thirty-seven and the queue from 187 to 181. No score in the batch measures output quality, benchmark rank, parameter count, price, latency, or throughput.

67. **Model queue batch twenty-one — StepFun and Upstage:** five publishes and one hold with no new licence identifiers. StepFun weights are Apache-declared without license files; the three Solar lines are closed API models with tokenizer-only artifacts at best. The 2603 agent variant is held for want of any reviewable artifact. The reviewed Models collection moves from one hundred thirty-seven to one hundred forty-two and the queue from 181 to 175. No score in the batch measures output quality, benchmark rank, parameter count, price, latency, or throughput.

68. **Model queue batch twenty-two — DeepSeek follow-ups:** three publishes and two exclusions with no new licence identifiers. OCR-2 is Apache-2.0, the R1 distill carries MIT with Qwen provenance, and Vision-Exp is its own multimodal line with a documented retirement; the flash preview and 0423 snapshot fold into reviewed lines. The reviewed Models collection moves from one hundred forty-two to one hundred forty-five and the queue from 175 to 170. No score in the batch measures output quality, benchmark rank, parameter count, price, latency, or throughput.

69. **Model queue batch twenty-three — Sonar and Laguna:** eight publishes and one new licence identifier. Perplexity contributes four proprietary API-only search releases facing a September 2026 support date; Poolside contributes Apache-2.0 flagships and OpenMDW-1.1 mid tiers with third-party hosting. The reviewed Models collection moves from one hundred forty-five to one hundred fifty-three and the queue from 170 to 162. No score in the batch measures output quality, benchmark rank, parameter count, price, latency, or throughput.

70. **Model queue batch twenty-four — Arcee Trinity:** four publishes and no new licence identifiers. All four carry OpenMDW-1.1 license files with stale Apache claims elsewhere on vendor pages: the evolving Large preview, the official Thinking release, the Mini reasoning line, and the download-only Nano preview. The reviewed Models collection moves from one hundred fifty-three to one hundred fifty-seven and the queue from 162 to 158. No score in the batch measures output quality, benchmark rank, parameter count, price, latency, or throughput.

71. **Model queue batch twenty-five — Tencent Hy and Sakana:** three publishes and three exclusions with one new licence identifier. Hy3 and Hy4-preview are Apache-declared open weights while Hy3-preview carries the geo-fenced community license; all three Sakana IDs are unreviewable aliases with no fixed identity or artifact. The reviewed Models collection moves from one hundred fifty-seven to one hundred sixty and the queue from 158 to 152. No score in the batch measures output quality, benchmark rank, parameter count, price, latency, or throughput.

72. **Model queue batch twenty-six — Ornith and Sarvam:** four publishes and three holds with no new licence identifiers. Four Ornith lines are MIT-claimed without license files; the 31B name and both Sarvam lines are held for thin or file-less license evidence. Held IDs stay in the published source snapshot, so the Sarvam 105B e2e fixture is unaffected. The reviewed Models collection moves from one hundred sixty to one hundred sixty-four and the queue from 152 to 145. No score in the batch measures output quality, benchmark rank, parameter count, price, latency, or throughput.

73. **Model queue batch twenty-seven — Apertus and older Llamas:** seven publishes with two new licence identifiers. Both Swiss AI Apertus base lines are Apache-2.0 with the license text at pinned commits and an advisory usage policy. The four Llama 3.1 and 3.2 releases and Llama Guard 3 8B are source-available under the community license for their release; the 3.1 identifiers are added to the taxonomy. Llama Guard 3 qualifies as a text-output release: it generates verdicts as an autoregressive language model rather than a non-text classifier head. The reviewed Models collection moves from one hundred sixty-four to one hundred seventy-one and the queue from 145 to 138. No score in the batch measures output quality, benchmark rank, parameter count, price, latency, or throughput.

74. **Model queue batch twenty-eight — Muse and Grok:** nine publishes, one hold, and one new licence identifier. Muse Glimmer 30B is the first Apache-2.0 open-weights Muse release; the three Muse Spark checkpoints are API-only records under the new Meta Model API Terms identifier. Five Grok lines are API-only records under the existing xAI enterprise terms, with host-documented paths for the 4.20 snapshots and Grok Build 0.1; the 4.1 Fast reasoning name is held for lack of a first-party model page. The reviewed Models collection moves from one hundred seventy-one to one hundred eighty and the queue from 138 to 128. No score in the batch measures output quality, benchmark rank, parameter count, price, latency, or throughput.

75. **Model queue batch twenty-nine — Gemma and Gemini 2.0:** eight publishes with two new licence identifiers. The Gemma 3 instruction trio is governed by the restricted Gemma Terms of Use with its incorporated prohibited-use policy; the Gemma 4 small and MoE sizes are Apache-2.0 like the reviewed 31B, with the 26B A4B also documented on the Gemini API. The Gemini 2.0 Flash pair are shut-down API-only lines under the existing Gemini API terms, reviewed as retired releases with named successors. The reviewed Models collection moves from one hundred eighty to one hundred eighty-eight and the queue from 128 to 120. No score in the batch measures output quality, benchmark rank, parameter count, price, latency, or throughput.

76. **Model queue batch thirty — Deep Research and Nano Banana:** eight publishes with no new licence identifiers. The Deep Research agent pair are active preview releases on the Interactions API; the two Nano Banana preview lines are shut down and reviewed as retired; the remaining image models span deprecated and GA states, all under the existing Gemini API terms. The reviewed Models collection moves from one hundred eighty-eight to one hundred ninety-six and the queue from 120 to 112. No score in the batch measures output quality, benchmark rank, parameter count, price, latency, or throughput.

77. **Model queue batch thirty-one — Gemini flash lines:** eight publishes with no new licence identifiers. The Gemini 2.5 Flash-Lite and 3.1 Flash-Lite lines are stable GA releases, the 3.1 Flash-Lite Preview is shut down and reviewed as retired, the 2.5 Computer Use line is a dated preview, and the 3.5, 3.6, and 3.7 Flash releases are active GA lines, all under the existing Gemini API terms. The reviewed Models collection moves from one hundred ninety-six to two hundred four and the queue from 112 to 104. No score in the batch measures output quality, benchmark rank, parameter count, price, latency, or throughput.

78. **Model queue batch thirty-two — Google special-purpose lines:** four publishes with no new licence identifiers. The 3.1 Flash Live and 3.5 Live Translate previews and the 3.5 Transcribe Live line are served through the Live API, and the Robotics-ER 1.6 preview is shut down and reviewed as retired, all under the existing Gemini API terms. The embedding pair and the Lyria music pair generate no language-model text output and stay queued as unscored source records. The reviewed Models collection moves from two hundred four to two hundred eight and the queue from 104 to 100. No score in the batch measures output quality, benchmark rank, parameter count, price, latency, or throughput.

79. **Model queue batch thirty-three — Nemotron open weights:** seven publishes, one hold, and two new licence identifiers. The Llama-based Nemotron family and both Nano lines are governed by the NVIDIA Open Model License, a revocable NVIDIA instrument distinct from the Nemotron Open Model License on the 3 Nano record; the Mini 4B release sits under the older NVIDIA Community Model License with conflicting license pointers opening a durable review incident. Mistral-Nemotron is held because no verifiable weights or governing license text exist. The reviewed Models collection moves from two hundred eight to two hundred fifteen and the queue from 100 to 92. No score in the batch measures output quality, benchmark rank, parameter count, price, latency, or throughput.

80. **Model queue batch thirty-four — Nemotron 3.x:** five publishes, one hold, and two new licence identifiers. The Super line reuses the Nemotron Open Model License, the Omni line sits under the new NVIDIA Open Model Agreement, the Cascade line reuses the Open Model License, and the Ultra and Lightning lines are governed by the OpenMDW Agreement 1.1 already classified as open source. VoiceChat is held because the queued row points at a gated early-access service rather than the open 11B release. The reviewed Models collection moves from two hundred fifteen to two hundred twenty and the queue from 92 to 86. No score in the batch measures output quality, benchmark rank, parameter count, price, latency, or throughput.

81. **Model queue batch thirty-five — NVIDIA safety and embed lines:** four publishes with no new licence identifiers. All four content-safety models emit text verdicts and qualify as language-model releases; they reuse the Open Model License, the Nemotron Open Model License, and the OpenMDW-1.1 identifier, with Gemma and Llama base layers recorded. The embed and rerank lines output vectors and logits rather than text and stay queued as unscored source records. The reviewed Models collection moves from two hundred twenty to two hundred twenty-four and the queue from 86 to 82. No score in the batch measures output quality, benchmark rank, parameter count, price, latency, or throughput.

82. **Model queue batch thirty-six — Anthropic:** six publishes, three holds, and two exclusions with no new licence identifiers. The Opus 4.5 snapshot, the 4.6 and 4.7 pinned releases, and the Fable 5, Fable 5.1, and Mythos 5 lines become records under the existing Anthropic commercial terms. The pre-4.6 alias and the first-generation Haiku snapshot are excluded, and the retired 3.x lines with gone model pages are held. The reviewed Models collection moves from two hundred twenty-four to two hundred thirty and the queue from 77 to 71. No score in the batch measures output quality, benchmark rank, parameter count, price, latency, or throughput.

83. **Model queue batch thirty-seven — GLM 4.x:** seven publishes and one hold with no new licence identifiers. The GLM-4.5-Air, 4.5V, 4.6, 4.6V, 4.6V-Flash, 4.7, and 4.7-Flash releases carry frontmatter-only MIT declarations without LICENSE files, following the family posture of the reviewed GLM-5 record. FlashX is held because its weight repository is gated and no governing license text is accessible. The reviewed Models collection moves from two hundred thirty to two hundred thirty-seven and the queue from 70 to 63. No score in the batch measures output quality, benchmark rank, parameter count, price, latency, or throughput.

84. **Model queue batch thirty-eight — Mistral:** five publishes, one hold, and two exclusions with no new licence identifiers. Devstral Small 2, Magistral Small 2509, and the Ministral 3 instruct trio are Apache-2.0 weight releases; the ministral-14b name is an alias of the 14B release, ministral-3b never had a weight release, and the Pixtral Large 25.02 ID is a dated API refresh held for a line-level review. The reviewed Models collection moves from two hundred thirty-seven to two hundred forty-two and the queue from 63 to 55. No score in the batch measures output quality, benchmark rank, parameter count, price, latency, or throughput.

For each batch, promote or exclude every reviewed candidate in the same change, update this snapshot only when counts materially change, and follow `CURATION.md` for evidence and scoring.
